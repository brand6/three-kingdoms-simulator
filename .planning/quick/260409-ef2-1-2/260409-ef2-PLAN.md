---
mode: quick
plan: 260409-ef2
type: execute
autonomous: true
files_modified:
  - three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
  - three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
requirements:
  - UI-02
must_haves:
  truths:
    - 任务领取弹窗里的任务描述下方不再出现两行多余空白。
    - 任务卡首行的任务名、来源、请求方三块信息更易扫读，且不再使用竖线分隔。
  artifacts:
    - path: three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
      provides: 任务卡标题行与正文行距的新排版实现
    - path: three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
      provides: 锁定任务卡无竖线分隔且无多余空白行的回归校验
  key_links:
    - from: three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
      to: three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
      via: _build_card_content / _card_header_text / _card_body_text rendered text assertions
      pattern: HeaderLabel|BodyLabel|来源：|请求方：
---

<objective>
微调月初任务领取卡片的排版：去掉任务描述后的两行空白，并把标题首行的三段信息拉开，不再使用 `｜` 分隔。

Purpose: 让任务卡更紧凑、可扫读，延续 Phase 03 已确定的“任务名 + 来源 + 请求方”信息结构，但修掉当前过挤和多余空白的问题。
Output: 一个更新后的 `TaskSelectPanel.gd` 排版实现，以及一个锁定新 UI 合同的月任务 HUD 回归脚本。
</objective>

<context>
@.planning/STATE.md
@three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
@three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
@.planning/quick/260407-rz5-task-panel-confirm-ui/260407-rz5-PLAN.md

<interfaces>
Current task card contract in `TaskSelectPanel.gd`:

```gdscript
func _build_card_content(candidate: Dictionary, repository: Node, selected: bool) -> VBoxContainer
func _card_header_text(candidate: Dictionary, repository: Node) -> String
func _card_body_text(candidate: Dictionary) -> String
```

Current regression reads the first card by collecting `HeaderLabel` + `BodyLabel` parsed text and asserts the first line contains `来源：` and `请求方：`.

Keep intact:
- 任务卡仍然是 Button 选择交互，不改确认按钮门控。
- 首行仍保留“任务名 / 来源 / 请求方”三块信息。
- 来源机构继续优先使用 `authority_institution_name`，请求方继续优先使用 `request_character_id` 回退 `issuer_character_id`。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重排任务卡首行与正文间距</name>
  <files>three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd</files>
  <action>在 `TaskSelectPanel.gd` 中只调整任务卡内容布局：把当前单个 `HeaderLabel` 里的 `任务名｜来源：…｜请求方：…` 改成更易扫读的三段式排版（可用独立 label/container，或等价的稳定布局），明确拉开任务名、来源、请求方之间的视觉距离，并彻底移除 `｜` 分隔符；同时检查 `RichTextLabel` 与 `_card_body_text()` 的组合，消除任务描述后方多出的两行空白，让“目标 / 预计奖励 / 机遇和风险”之间只保留正常单行间距。不要改任务筛选逻辑、来源/请求方解析逻辑、确认按钮行为或卡片选中样式基线。</action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path(r'three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd').read_text(encoding='utf-8'); assert '｜' not in t, 'vertical separator still present'; assert '来源：' in t and '请求方：' in t"</automated>
  </verify>
  <done>任务卡首行不再依赖竖线拼接，正文三段信息之间的空白恢复为正常紧凑排版。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新月任务 HUD 回归以锁定新排版合同</name>
  <files>three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd</files>
  <behavior>
    - Test 1: 首张任务卡第一扫描行仍包含任务名、来源、请求方，但不再包含 `｜`。
    - Test 2: 首张任务卡正文中“目标：”后不出现连续两行空白。
    - Test 3: 现有月初任务选择与确认流程回归继续通过，没有因为排版重构破坏交互。
  </behavior>
  <action>更新 `phase21_monthly_hud_regression.gd`，让它围绕这次 quick task 的 UI 合同断言：读取首张任务卡渲染文本后，确认首行保留三块核心信息但不再含 `｜`；对正文文本加入“目标段后无双空行/连续三次换行”的检查，防止描述下方再次冒出多余留白；保留原有月初任务选择、确认按钮门控、月报/任命串联等关键断言，避免本次排版微调破坏既有月任务闭环。</action>
  <verify>
    <automated>pwsh -NoProfile -Command "python -c \"from pathlib import Path; t=Path(r'three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd').read_text(encoding='utf-8'); required=['来源：','请求方：']; missing=[s for s in required if s not in t]; assert not missing, missing; assert '｜' not in t, 'regression still expects vertical separator'\"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase21_monthly_hud_regression.gd'"</automated>
  </verify>
  <done>回归脚本能直接证明新排版生效，且任务领取月初闭环没有被这次 UI 微调破坏。</done>
</task>

</tasks>

<verification>
- 静态检查 `TaskSelectPanel.gd` 与回归脚本，确认不再保留 `｜` 分隔符相关实现或旧断言。
- 运行 `phase21_monthly_hud_regression.gd`，确认任务卡首行与正文排版更新后，整个月任务领取流程仍然通过。
</verification>

<success_criteria>
- 任务描述下面不再出现用户指出的两行空白。
- 任务标题行的三段信息明显分开，且不再用 `|/｜` 视觉分隔。
- 改动范围收敛在任务领取卡片排版与对应回归，不扩散到其他面板或系统逻辑。
</success_criteria>

<output>
After completion, create `.planning/quick/260409-ef2-1-2/260409-ef2-SUMMARY.md`
</output>
