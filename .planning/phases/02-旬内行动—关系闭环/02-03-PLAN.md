---
phase: 02-旬内行动—关系闭环
plan: "03"
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - three-kingdoms-simulator/scenes/main/MainScene.tscn
  - three-kingdoms-simulator/scripts/ui/MainHUD.gd
  - three-kingdoms-simulator/themes/PrototypeTheme.tres
autonomous: true
requirements:
  - ACTN-01
  - ACTN-02
  - UI-01
  - UI-02
  - UI-04
  - RELA-02
must_haves:
  truths:
    - The player can open the action menu from the HUD and inspect the five fixed categories without leaving the main scene.
    - The player can see action cost, target type, expected effect, and disabled reason inside a two-step menu flow.
    - The player can inspect relationship context from the main flow and use it to plan the next xun.
  artifacts:
    - path: three-kingdoms-simulator/scenes/main/MainScene.tscn
      provides: Popup nodes and enabled buttons for action, relation, result, and end-xun flows.
    - path: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      provides: Input handling and rendering logic for Phase 2 action/relation UI.
    - path: three-kingdoms-simulator/themes/PrototypeTheme.tres
      provides: Focus, active, and disabled styles reused by new Phase 2 overlays.
  key_links:
    - from: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      to: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      via: action/relation query calls and execute-action trigger
      pattern: get_available_phase2_actions|get_relation_overview|execute_phase2_action
    - from: three-kingdoms-simulator/scenes/main/MainScene.tscn
      to: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      via: named popup/dialog nodes referenced by onready paths
      pattern: ActionMenuPopup|RelationPopup|ActionResultDialog
---

<objective>
Wire the Phase 2 HUD surfaces for action selection, target selection, result feedback, and relation inspection without leaving the main scene.

Purpose: Fulfill the HUD-first product promise and make the backend action loop actually playable within three clicks.
Output: Enabled action and relation buttons, popup/dialog nodes in `MainScene`, and `MainHUD` logic that renders menu details, disabled reasons, relation context, and result feedback.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-旬内行动—关系闭环/02-CONTEXT.md
@.planning/phases/02-旬内行动—关系闭环/02-UI-SPEC.md
@.planning/phases/02-旬内行动—关系闭环/02-RESEARCH.md
@three-kingdoms-simulator/scenes/main/MainScene.tscn
@three-kingdoms-simulator/scripts/ui/MainHUD.gd
@three-kingdoms-simulator/themes/PrototypeTheme.tres

<interfaces>
Expected from Plan 02:

```gdscript
func get_available_phase2_actions() -> Array[Phase2ActionSpec]
func get_relation_overview() -> Array[RuntimeRelationState]
func execute_phase2_action(action_id: String, target_character_id: String = "") -> ActionResolution
func get_latest_action_resolution() -> ActionResolution
```

Existing node anchors in `MainScene.tscn`:

```text
BottomBar/BottomBarContent/NavigationRow/ActionButton
BottomBar/BottomBarContent/NavigationRow/RelationButton
BottomBar/BottomBarContent/NavigationRow/EndTurnButton
```

The new UI must extend this shell instead of replacing it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the floating action menu and target-selection flow inside MainScene</name>
  <read_first>
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    three-kingdoms-simulator/themes/PrototypeTheme.tres
    .planning/phases/02-旬内行动—关系闭环/02-UI-SPEC.md
    .planning/phases/02-旬内行动—关系闭环/02-CONTEXT.md
  </read_first>
  <files>
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    three-kingdoms-simulator/themes/PrototypeTheme.tres
  </files>
  <action>Honor D-04, D-05, D-06, D-07, and the approved UI contract exactly. In `MainScene.tscn`, enable `ActionButton` and add a popup node named `ActionMenuPopup` positioned above it, containing a left category rail and right action list. Also add a modal `TargetPickerDialog` for target-required actions. In `MainHUD.gd`, wire `ActionButton.pressed` to open the popup via Godot popup APIs, keep the five fixed categories visible in order `成长 / 关系 / 政务 / 军事 / 家族`, and render action rows with the exact fields `动作名称`, `AP`, `精力`, `目标类型`, `效果摘要`, and disabled reason text. Keep `巡察` hidden when permission-locked per D-11, but keep visible disabled rows for AP/energy/target failures per D-12 with the exact short-copy reasons from research. For `拜访`, open `TargetPickerDialog` and list each target as `目标名 / 所属势力 / 所在城市 / 关系摘要 / 预估效果`; for `训练 / 读书 / 休整 / 巡察`, execute directly from the popup without scene changes. In `PrototypeTheme.tres`, add or reuse accent/focus styles so the active category and selected action match the approved accent color `#2F5D62` and disabled rows use the existing disabled text color.</action>
  <acceptance_criteria>
    - `MainScene.tscn` contains nodes named `ActionMenuPopup` and `TargetPickerDialog`.
    - `MainScene.tscn` no longer has `ActionButton` marked `disabled = true`.
    - `MainHUD.gd` contains the exact category labels `成长`, `关系`, `政务`, `军事`, and `家族`.
    - `MainHUD.gd` contains the exact disabled reason strings `AP 不足`, `精力不足`, `当前地点不可执行`, and `暂无可拜访对象`.
    - `MainHUD.gd` contains the exact node or logic names `ActionMenuPopup` and `TargetPickerDialog`.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$scene = Get-Content 'three-kingdoms-simulator/scenes/main/MainScene.tscn' -Raw; $hud = Get-Content 'three-kingdoms-simulator/scripts/ui/MainHUD.gd' -Raw; $theme = Get-Content 'three-kingdoms-simulator/themes/PrototypeTheme.tres' -Raw; $ok = $scene.Contains('ActionMenuPopup') -and $scene.Contains('TargetPickerDialog') -and -not ($scene -match 'ActionButton[\s\S]*?disabled = true'); $ok = $ok -and $hud.Contains('成长') -and $hud.Contains('关系') -and $hud.Contains('政务') -and $hud.Contains('军事') -and $hud.Contains('家族') -and $hud.Contains('AP 不足') -and $hud.Contains('精力不足') -and $hud.Contains('暂无可拜访对象') -and $hud.Contains('ActionMenuPopup') -and $hud.Contains('TargetPickerDialog'); $ok = $ok -and $theme.Contains('colors/Accent'); if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>The HUD can open a lightweight action popup, show the required metadata and disabled reasons, and route target-required actions into a separate modal instead of a third in-panel step.</done>
</task>

<task type="auto">
  <name>Task 2: Wire relation inspection and action-result feedback into the main HUD flow</name>
  <read_first>
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    .planning/phases/02-旬内行动—关系闭环/02-UI-SPEC.md
    .planning/phases/02-旬内行动—关系闭环/02-RESEARCH.md
  </read_first>
  <files>
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
  </files>
  <action>Enable `RelationButton` and add two more HUD overlays in `MainScene.tscn`: `RelationPopup` and `ActionResultDialog`. In `MainHUD.gd`, wire `RelationButton` to open `RelationPopup` with a readable list of key characters showing exact columns `人物名`, `关系标签`, `好感`, `信任`, `敬重`, `戒备`, `所属势力`, and `可互动状态`. Use the runtime relation data from `GameRoot.get_relation_overview()` and preserve D-01's HUD-first flow by keeping the main scene underneath. After every action execution, open `ActionResultDialog` and render the result in the exact order required by the UI contract: `行动名 -> 成败结论 -> 原因说明 -> 数值变化 -> 关系变化 / 新线索`. Also update the existing right-side summary text so `TaskBody`, `EventBody`, or `RelationSummaryBody` visibly reflect the most recent result. Keep `CharacterButton`, `FactionButton`, `ClanButton`, and `LogButton` disabled in this phase unless a task explicitly needs them.</action>
  <acceptance_criteria>
    - `MainScene.tscn` contains nodes named `RelationPopup` and `ActionResultDialog`.
    - `MainScene.tscn` no longer has `RelationButton` marked `disabled = true`.
    - `MainHUD.gd` contains the exact relation column labels `人物名`, `好感`, `信任`, `敬重`, `戒备`, and `所属势力`.
    - `MainHUD.gd` contains the exact result-order labels `成败结论`, `原因说明`, `数值变化`, and `关系变化` or `新线索`.
    - `MainHUD.gd` updates at least one existing summary body (`TaskBody`, `EventBody`, or `RelationSummaryBody`) after action execution.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$scene = Get-Content 'three-kingdoms-simulator/scenes/main/MainScene.tscn' -Raw; $hud = Get-Content 'three-kingdoms-simulator/scripts/ui/MainHUD.gd' -Raw; $ok = $scene.Contains('RelationPopup') -and $scene.Contains('ActionResultDialog') -and -not ($scene -match 'RelationButton[\s\S]*?disabled = true'); $ok = $ok -and $hud.Contains('人物名') -and $hud.Contains('好感') -and $hud.Contains('信任') -and $hud.Contains('敬重') -and $hud.Contains('戒备') -and $hud.Contains('所属势力') -and $hud.Contains('成败结论') -and $hud.Contains('原因说明') -and $hud.Contains('数值变化') -and ($hud.Contains('关系变化') -or $hud.Contains('新线索')) -and ($hud.Contains('TaskBody') -or $hud.Contains('EventBody') -or $hud.Contains('RelationSummaryBody')); if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>The player can inspect relationship context and receives structured action-result feedback directly inside the HUD loop, with no map-first detour and no silent outcomes.</done>
</task>

</tasks>

<verification>
All new Phase 2 UI must remain in the existing `MainScene` shell and expose the exact popup node names so the later integration regression can drive them headlessly.</verification>

<success_criteria>
- The player can reach action selection and relation inspection from the main HUD within three clicks.
- The action popup shows all required metadata and disabled reasons.
- The relation popup surfaces the directional values needed to plan the next xun.
- Every executed action opens a readable result dialog and refreshes HUD summary text.
</success_criteria>

<output>
After completion, create `.planning/phases/02-旬内行动—关系闭环/02-03-SUMMARY.md`
</output>
