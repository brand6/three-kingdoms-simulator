---
phase: 02-旬内行动—关系闭环
plan: "05"
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - three-kingdoms-simulator/scripts/runtime/Phase2ActionMenuConfig.gd
  - three-kingdoms-simulator/data/config/phase2_action_menu_config.tres
  - three-kingdoms-simulator/scripts/runtime/CharacterSelectorRow.gd
  - three-kingdoms-simulator/scripts/runtime/CharacterProfileViewData.gd
  - three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd
  - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
  - three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd
autonomous: true
requirements:
  - ACTN-02
  - ACTN-04
  - RELA-02
  - UI-04
gap_closure: true
must_haves:
  truths:
    - 玩家无论当前身份为何，都能在菜单数据中看到五个基础行动，而不是因身份限制被直接过滤掉。
    - 身份限制动作会返回明确禁用原因，且 AP、精力、地点、无目标等禁用原因仍保持可区分。
    - 拜访与关系入口可以消费同一套角色选择行数据，并在选中后读取角色信息面板所需的明细数据。
  artifacts:
    - path: three-kingdoms-simulator/scripts/runtime/Phase2ActionMenuConfig.gd
      provides: 配置化行动可见性与菜单顺序契约。
    - path: three-kingdoms-simulator/data/config/phase2_action_menu_config.tres
      provides: 五个基础行动的身份可用性、排序和锁定文案配置。
    - path: three-kingdoms-simulator/scripts/runtime/CharacterSelectorRow.gd
      provides: 通用角色选择表格行 DTO。
    - path: three-kingdoms-simulator/scripts/runtime/CharacterProfileViewData.gd
      provides: 角色信息面板 DTO。
    - path: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      provides: 通用角色选择与角色详情查询 API。
  key_links:
    - from: three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd
      to: three-kingdoms-simulator/data/config/phase2_action_menu_config.tres
      via: 配置驱动的菜单顺序与身份禁用规则
      pattern: phase2_action_menu_config|当前身份不可执行
    - from: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      to: three-kingdoms-simulator/scripts/runtime/CharacterSelectorRow.gd
      via: get_character_selector_rows
      pattern: get_character_selector_rows
    - from: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      to: three-kingdoms-simulator/scripts/runtime/CharacterProfileViewData.gd
      via: get_character_profile_view_data
      pattern: get_character_profile_view_data
---

<objective>
把 Phase 2 的行动可见性与角色选择数据改成可配置、可复用的后端契约，只修复已诊断 gaps，不重做整期功能。

Purpose: 先修正“身份限制动作被隐藏”和“目标选择/关系流程缺少通用数据契约”这两个根因，让后续 UI 修复有稳定接口可接。
Output: 配置化的五行动菜单规则、通用角色选择 DTO、角色信息 DTO，以及更新后的 catalog/GameRoot/test 回归。
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-旬内行动—关系闭环/02-CONTEXT.md
@.planning/phases/02-旬内行动—关系闭环/02-UAT.md
@.planning/debug/identity-action-hidden.md
@.planning/debug/visit-target-selector-ui.md
@.planning/debug/uat5-relation-button-panel.md
@three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd
@three-kingdoms-simulator/scripts/autoload/GameRoot.gd
@three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd

<interfaces>
Existing contracts to preserve:

```gdscript
class_name Phase2ActionSpec
var id: String
var display_name: String
var category_id: String
var ap_cost: int
var energy_delta: int
var target_type: String
var effect_summary: String
var disabled_reason: String
```

```gdscript
func get_available_phase2_actions() -> Array
func get_relation_overview() -> Array
func execute_phase2_action(action_id: String, target_character_id: String = "") -> Variant
```

New contracts this plan must define for downstream UI work:

```gdscript
class_name CharacterSelectorRow
var character_id: String
var display_name: String
var faction_name: String
var city_name: String
var favor: int
var trust: int
var respect: int
var vigilance: int
var interaction_status: String
var selectable: bool
var disabled_reason: String
```

```gdscript
class_name CharacterProfileViewData
var character_id: String
var display_name: String
var identity_label: String
var faction_label: String
var city_label: String
var office_label: String
var favor: int
var trust: int
var respect: int
var vigilance: int
var obligation: int
var notes: Array[String]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写出配置化行动菜单与通用角色浏览契约</name>
  <files>
    three-kingdoms-simulator/scripts/runtime/Phase2ActionMenuConfig.gd
    three-kingdoms-simulator/data/config/phase2_action_menu_config.tres
    three-kingdoms-simulator/scripts/runtime/CharacterSelectorRow.gd
    three-kingdoms-simulator/scripts/runtime/CharacterProfileViewData.gd
  </files>
  <action>先补接口与配置层，避免后续 UI 继续从 `MainHUD.gd` 猜结构。按 `02-CONTEXT.md` 中 **D-11R / D-14R / D-15R** 创建 `Phase2ActionMenuConfig.gd`（typed Resource），至少暴露 `rules` 数组，每条 rule 含 `action_id`、`menu_order`、`allowed_identity_types`、`locked_reason`。同步创建 `phase2_action_menu_config.tres`，把五个基础行动按用户期望首屏顺序固化为 `训练 / 读书 / 休整 / 拜访 / 巡察`；`巡察` 对未授权身份不再隐藏，而是返回锁定文案 `当前身份不可执行`。同时创建 `CharacterSelectorRow.gd` 与 `CharacterProfileViewData.gd`，都用 `static create(...)` 模式，保持与 Phase 2 既有 typed DTO 模式一致。这个任务用于关闭 gap 2、4、5 的接口空洞，并明确取代旧实现里“身份锁定直接隐藏”的路径。</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$ok = (Test-Path 'three-kingdoms-simulator/scripts/runtime/Phase2ActionMenuConfig.gd') -and (Test-Path 'three-kingdoms-simulator/data/config/phase2_action_menu_config.tres') -and (Test-Path 'three-kingdoms-simulator/scripts/runtime/CharacterSelectorRow.gd') -and (Test-Path 'three-kingdoms-simulator/scripts/runtime/CharacterProfileViewData.gd'); if (-not $ok) { exit 1 }; $cfg = Get-Content 'three-kingdoms-simulator/scripts/runtime/Phase2ActionMenuConfig.gd' -Raw; $res = Get-Content 'three-kingdoms-simulator/data/config/phase2_action_menu_config.tres' -Raw; $row = Get-Content 'three-kingdoms-simulator/scripts/runtime/CharacterSelectorRow.gd' -Raw; $profile = Get-Content 'three-kingdoms-simulator/scripts/runtime/CharacterProfileViewData.gd' -Raw; $ok = $cfg.Contains('extends Resource') -and $cfg.Contains('rules') -and $res.Contains('train') -and $res.Contains('study') -and $res.Contains('rest') -and $res.Contains('visit') -and $res.Contains('inspect') -and $res.Contains('当前身份不可执行') -and $row.Contains('class_name CharacterSelectorRow') -and $profile.Contains('class_name CharacterProfileViewData'); if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>五个基础行动已有独立配置来源，且后续 UI 所需的通用选择行/角色详情 DTO 已存在并可直接引用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用配置替换隐藏规则并暴露通用选择/详情 API</name>
  <files>
    three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd
  </files>
  <behavior>
    - Test 1: `xun_yu` 打开菜单时仍能拿到 `inspect` 行，但该行 `disabled_reason == 当前身份不可执行`。
    - Test 2: AP、精力、地点、无目标四类禁用原因仍保持原有文案，不被身份禁用覆盖或吞掉。
    - Test 3: `GameRoot.get_character_selector_rows("visit")` 与 `get_character_selector_rows("relation")` 都返回 `CharacterSelectorRow` 数组，且不包含主角自己。
    - Test 4: `GameRoot.get_character_profile_view_data("chen_gong")` 返回可供详情面板直接渲染的关系数值与身份/地点文本。
  </behavior>
  <action>修改 `Phase2ActionCatalog.gd`，不要再在 `_is_permission_locked()` 路径上 `continue` 过滤动作；改为读取 `phase2_action_menu_config.tres` 的规则，始终返回五个基础行动，并按 `menu_order` 排序。这里以 **D-11R** 为准：身份不满足时设置 `disabled_reason = 当前身份不可执行`，同时保留 AP、精力、地点、无目标等动态禁用原因；优先级应为“身份锁定先挡住执行，但动作仍可见”。在 `GameRoot.gd` 新增 `get_character_selector_rows(context_id: String) -> Array` 与 `get_character_profile_view_data(character_id: String) -> Variant`：前者按 **D-15R** 统一供 `visit` 与 `relation` 使用，后者按 **D-14R** 把角色基础信息 + 关系值 + 说明文案一次组装给 UI。更新 `phase2_action_resolver_test.gd` 先写失败断言，再改实现通过，确保这次 gap closure 真正替换旧的“隐藏 inspect”口径，而不是只改 UI 表面显示。</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase2_action_resolver_test.gd'; exit $LASTEXITCODE"</automated>
  </verify>
  <done>catalog、GameRoot 与回归测试都已切到“配置驱动 + 身份禁用可见 + 通用角色浏览 API”新契约，后续 UI 不再依赖旧的隐藏式菜单规则。</done>
</task>

</tasks>

<verification>
本计划的最终门槛是 `phase2_action_resolver_test.gd`：如果身份锁定动作仍被隐藏，或通用选择/详情 API 不存在，后续 UI 计划一律不得开始。</verification>

<success_criteria>
- 五个基础行动的顺序与身份限制来自配置文件，而不是硬编码隐藏逻辑。
- 身份限制动作可见但不可执行，且理由明确。
- 拜访与关系入口共用同一套角色选择行数据与角色详情数据接口。
</success_criteria>

<output>
After completion, create `.planning/phases/02-旬内行动—关系闭环/02-05-SUMMARY.md`
</output>
