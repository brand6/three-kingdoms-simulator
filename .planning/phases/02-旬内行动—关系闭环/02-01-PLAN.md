---
phase: 02-旬内行动—关系闭环
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - three-kingdoms-simulator/scripts/runtime/Phase2ActionSpec.gd
  - three-kingdoms-simulator/scripts/runtime/ActionResolution.gd
  - three-kingdoms-simulator/scripts/runtime/RuntimeRelationState.gd
  - three-kingdoms-simulator/scripts/runtime/XunSummaryData.gd
  - three-kingdoms-simulator/scripts/runtime/GameSession.gd
  - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
autonomous: true
requirements:
  - ACTN-04
  - RELA-01
  - CHAR-03
must_haves:
  truths:
    - HUD-facing systems have typed action, relation, and xun-summary contracts instead of ad hoc dictionaries.
    - The current session can store directional relationship values and per-xun action history without mutating source definitions.
    - Phase 2 systems can query the protagonist's city targets and seeded key relationships from one runtime source.
  artifacts:
    - path: three-kingdoms-simulator/scripts/runtime/Phase2ActionSpec.gd
      provides: Typed action metadata contract shared by UI and resolver.
    - path: three-kingdoms-simulator/scripts/runtime/RuntimeRelationState.gd
      provides: Directional relation-value container with favor/trust/respect/vigilance/obligation fields.
    - path: three-kingdoms-simulator/scripts/runtime/XunSummaryData.gd
      provides: Per-xun summary container for action log and delta reporting.
    - path: three-kingdoms-simulator/scripts/runtime/GameSession.gd
      provides: Mutable storage for relation states, action history, and latest xun summary.
    - path: three-kingdoms-simulator/scripts/autoload/DataRepository.gd
      provides: Seed helpers for Phase 2 target lookups and relation bootstrap.
  key_links:
    - from: three-kingdoms-simulator/scripts/autoload/DataRepository.gd
      to: three-kingdoms-simulator/scripts/runtime/GameSession.gd
      via: bootstrap seeding of relation and target data
      pattern: bootstrap_session|set_relation_state
    - from: three-kingdoms-simulator/scripts/runtime/GameSession.gd
      to: three-kingdoms-simulator/scripts/runtime/RuntimeRelationState.gd
      via: dictionary-backed directional relation storage
      pattern: RuntimeRelationState
---

<objective>
Define the Phase 2 runtime contracts and session storage that all later action, relation, and summary work will build against.

Purpose: Prevent shallow Phase 2 execution by giving the resolver, HUD, and xun-summary flow one typed source of truth.
Output: New action/relation/summary DTOs plus a `GameSession`/`DataRepository` foundation that can seed and serve Phase 2 runtime state.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-旬内行动—关系闭环/02-CONTEXT.md
@.planning/phases/02-旬内行动—关系闭环/02-UI-SPEC.md
@.planning/phases/02-旬内行动—关系闭环/02-RESEARCH.md
@three-kingdoms-simulator/scripts/autoload/DataRepository.gd
@three-kingdoms-simulator/scripts/runtime/GameSession.gd
@three-kingdoms-simulator/data/generated/190/characters.json
@three-kingdoms-simulator/data/generated/190/cities.json

<interfaces>
From `three-kingdoms-simulator/scripts/runtime/GameSession.gd`:

```gdscript
var scenario_id: String = ""
var current_year: int = 0
var current_month: int = 0
var current_xun: int = 0
var protagonist_id: String = ""
var character_states: Dictionary = {}

func set_character_state(character_id: String, state: RuntimeCharacterState) -> void
func get_character_state(character_id: String) -> RuntimeCharacterState
```

From `three-kingdoms-simulator/scripts/autoload/DataRepository.gd`:

```gdscript
func get_character(id: String) -> Variant
func get_faction(id: String) -> Variant
func get_city(id: String) -> Variant
func bootstrap_session(scenario_id: String, protagonist_id: String) -> GameSession
```

Keep Definition/Runtime separation intact: Phase 2 runtime mutations must land in `GameSession` or new runtime DTOs, never inside the JSON-backed definition objects.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Write the shared Phase 2 contracts first</name>
  <read_first>
    .planning/phases/02-旬内行动—关系闭环/02-CONTEXT.md
    .planning/phases/02-旬内行动—关系闭环/02-RESEARCH.md
    three-kingdoms-simulator/scripts/runtime/GameSession.gd
  </read_first>
  <files>
    three-kingdoms-simulator/scripts/runtime/Phase2ActionSpec.gd
    three-kingdoms-simulator/scripts/runtime/ActionResolution.gd
    three-kingdoms-simulator/scripts/runtime/RuntimeRelationState.gd
    three-kingdoms-simulator/scripts/runtime/XunSummaryData.gd
  </files>
  <action>Per D-03 and D-08..D-13, create four typed `RefCounted` contracts before any behavior code lands. `Phase2ActionSpec.gd` must expose these exact fields: `id`, `display_name`, `category_id`, `ap_cost`, `energy_delta`, `target_type`, `effect_summary`, `required_permission_tags`, `disabled_reason`, and `hidden_when_locked`. `ActionResolution.gd` must expose `action_id`, `title`, `success`, `reason_text`, `target_character_id`, `stat_deltas`, `relation_deltas`, `clue_text`, and `summary_line`. `RuntimeRelationState.gd` must expose directional keys `source_character_id`, `target_character_id`, `favor`, `trust`, `respect`, `vigilance`, and `obligation`. `XunSummaryData.gd` must expose `xun_label`, `action_lines`, `stat_delta_totals`, `relation_change_lines`, and `prompt_lines`. Use typed constructors or static `create(...)` helpers so downstream plans can instantiate these without guessing field names.</action>
  <acceptance_criteria>
    - `three-kingdoms-simulator/scripts/runtime/Phase2ActionSpec.gd` contains `class_name Phase2ActionSpec` and the exact field names `display_name`, `category_id`, `target_type`, `required_permission_tags`, and `hidden_when_locked`.
    - `three-kingdoms-simulator/scripts/runtime/ActionResolution.gd` contains `class_name ActionResolution` and the exact field names `success`, `reason_text`, `stat_deltas`, `relation_deltas`, `clue_text`, and `summary_line`.
    - `three-kingdoms-simulator/scripts/runtime/RuntimeRelationState.gd` contains `class_name RuntimeRelationState` and the exact numeric fields `favor`, `trust`, `respect`, `vigilance`, and `obligation`.
    - `three-kingdoms-simulator/scripts/runtime/XunSummaryData.gd` contains `class_name XunSummaryData` and the exact fields `action_lines`, `stat_delta_totals`, `relation_change_lines`, and `prompt_lines`.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$files = 'three-kingdoms-simulator/scripts/runtime/Phase2ActionSpec.gd','three-kingdoms-simulator/scripts/runtime/ActionResolution.gd','three-kingdoms-simulator/scripts/runtime/RuntimeRelationState.gd','three-kingdoms-simulator/scripts/runtime/XunSummaryData.gd'; foreach ($file in $files) { if (-not (Test-Path $file)) { exit 1 } }; $ok = (Select-String -Path 'three-kingdoms-simulator/scripts/runtime/Phase2ActionSpec.gd' -Pattern 'class_name Phase2ActionSpec|display_name|category_id|target_type|required_permission_tags|hidden_when_locked').Count -ge 6; $ok = $ok -and (Select-String -Path 'three-kingdoms-simulator/scripts/runtime/ActionResolution.gd' -Pattern 'class_name ActionResolution|success|reason_text|stat_deltas|relation_deltas|clue_text|summary_line').Count -ge 7; $ok = $ok -and (Select-String -Path 'three-kingdoms-simulator/scripts/runtime/RuntimeRelationState.gd' -Pattern 'class_name RuntimeRelationState|favor|trust|respect|vigilance|obligation').Count -ge 6; $ok = $ok -and (Select-String -Path 'three-kingdoms-simulator/scripts/runtime/XunSummaryData.gd' -Pattern 'class_name XunSummaryData|action_lines|stat_delta_totals|relation_change_lines|prompt_lines').Count -ge 5; if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>The repository has explicit typed Phase 2 contracts that later plans can reference directly instead of inventing dictionaries on the fly.</done>
</task>

<task type="auto">
  <name>Task 1: Extend session storage and repository bootstrap for Phase 2 runtime data</name>
  <read_first>
    three-kingdoms-simulator/scripts/runtime/GameSession.gd
    three-kingdoms-simulator/scripts/autoload/DataRepository.gd
    three-kingdoms-simulator/data/generated/190/characters.json
    three-kingdoms-simulator/data/generated/190/cities.json
    .planning/phases/02-旬内行动—关系闭环/02-RESEARCH.md
  </read_first>
  <files>
    three-kingdoms-simulator/scripts/runtime/GameSession.gd
    three-kingdoms-simulator/scripts/autoload/DataRepository.gd
  </files>
  <action>Keep D-03's existing bootstrap flow, but extend it for Phase 2. In `GameSession.gd`, add storage plus helpers for `relation_states`, `current_xun_action_history`, and `latest_xun_summary`, with exact helper methods `set_relation_state(key: String, state: RuntimeRelationState)`, `get_relation_state(key: String) -> RuntimeRelationState`, `append_action_resolution(result: ActionResolution)`, `clear_xun_action_history()`, and `get_relation_keys_for_character(character_id: String) -> Array[String]`. In `DataRepository.gd`, add exact helper methods `get_characters_in_city(city_id: String) -> Array[CharacterDefinition]` and `get_faction_characters(faction_id: String) -> Array[CharacterDefinition]`. Update `bootstrap_session(...)` to seed the directional Phase 2 relation table from research using the exact eight rows listed in `02-RESEARCH.md`, store them in the session, and leave definition JSON untouched. Do not add new external data files in this phase; the relation seed belongs to runtime bootstrap for the prototype sample.</action>
  <acceptance_criteria>
    - `GameSession.gd` contains the exact fields `relation_states`, `current_xun_action_history`, and `latest_xun_summary`.
    - `GameSession.gd` contains the exact helper names `set_relation_state`, `get_relation_state`, `append_action_resolution`, `clear_xun_action_history`, and `get_relation_keys_for_character`.
    - `DataRepository.gd` contains the exact helper names `get_characters_in_city` and `get_faction_characters`.
    - `DataRepository.gd` bootstrap code contains the seed IDs `cao_cao`, `chen_gong`, `xun_yu`, `le_jin`, and `yuan_shao` and writes `RuntimeRelationState` objects into the session.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$session = Get-Content 'three-kingdoms-simulator/scripts/runtime/GameSession.gd' -Raw; $repo = Get-Content 'three-kingdoms-simulator/scripts/autoload/DataRepository.gd' -Raw; $ok = $session.Contains('relation_states') -and $session.Contains('current_xun_action_history') -and $session.Contains('latest_xun_summary') -and $session.Contains('func set_relation_state') -and $session.Contains('func get_relation_state') -and $session.Contains('func append_action_resolution') -and $session.Contains('func clear_xun_action_history') -and $session.Contains('func get_relation_keys_for_character'); $ok = $ok -and $repo.Contains('func get_characters_in_city') -and $repo.Contains('func get_faction_characters') -and $repo.Contains('RuntimeRelationState') -and $repo.Contains('cao_cao') -and $repo.Contains('chen_gong') -and $repo.Contains('xun_yu') -and $repo.Contains('le_jin') -and $repo.Contains('yuan_shao'); if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>`GameSession` can now own Phase 2 relation and action-history runtime state, and `DataRepository` can seed/query the small sample set needed for menu filtering and relation UI.</done>
</task>

</tasks>

<verification>
Contracts must exist before any resolver or UI work begins. `GameSession` and `DataRepository` must expose the exact helper names above so later plans can build without exploring the codebase.</verification>

<success_criteria>
- Phase 2 no longer depends on raw dictionaries for action and relation state.
- Session bootstrap seeds directional relationships for the current sample cast.
- Later plans can ask for city/faction character lists and mutate session-owned relation/action state without touching definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-旬内行动—关系闭环/02-01-SUMMARY.md`
</output>
