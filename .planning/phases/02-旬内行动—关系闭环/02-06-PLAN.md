---
phase: 02-旬内行动—关系闭环
plan: "06"
type: execute
wave: 4
depends_on:
  - 02-05
files_modified:
  - three-kingdoms-simulator/scenes/main/MainScene.tscn
  - three-kingdoms-simulator/scripts/ui/MainHUD.gd
  - three-kingdoms-simulator/scripts/ui/CharacterSelectorDialog.gd
  - three-kingdoms-simulator/scripts/ui/CharacterProfilePanel.gd
  - three-kingdoms-simulator/scripts/tests/phase2_hud_menu_selector_regression.gd
autonomous: true
requirements:
  - ACTN-01
  - ACTN-02
  - UI-01
  - UI-02
  - RELA-02
  - RELA-03
  - UI-04
gap_closure: true
must_haves:
  truths:
    - 点击“行动”后，主 HUD 内会出现向上浮起的菜单，首屏直接可见 `训练 / 读书 / 休整 / 拜访 / 巡察` 五项。
    - 每项行动都能在菜单内看到说明、消耗与禁用态；选中行动后只在右侧展开二级内容，而不是先按分类过滤。
    - 拜访与关系都走同一个可排序表格选择器，关系选择后进入角色信息面板而不是旧的 `RelationPopup`。
  artifacts:
    - path: three-kingdoms-simulator/scenes/main/MainScene.tscn
      provides: 新的浮动行动菜单、通用选择器、角色信息面板节点锚点。
    - path: three-kingdoms-simulator/scripts/ui/CharacterSelectorDialog.gd
      provides: 可排序表格选择器组件。
    - path: three-kingdoms-simulator/scripts/ui/CharacterProfilePanel.gd
      provides: 角色信息面板组件。
    - path: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      provides: 行动/拜访/关系三条入口的新 HUD 流程。
    - path: three-kingdoms-simulator/scripts/tests/phase2_hud_menu_selector_regression.gd
      provides: 首屏五行动、选择器排序、关系详情流程回归。
  key_links:
    - from: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      to: three-kingdoms-simulator/scripts/ui/CharacterSelectorDialog.gd
      via: visit 与 relation 共用 configure/open/confirm 流程
      pattern: CharacterSelectorDialog|get_character_selector_rows
    - from: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      to: three-kingdoms-simulator/scripts/ui/CharacterProfilePanel.gd
      via: 关系入口选中角色后打开详情面板
      pattern: CharacterProfilePanel|get_character_profile_view_data
    - from: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      to: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      via: 行动行渲染、选择器数据、角色详情和执行调用
      pattern: get_available_phase2_actions|get_character_selector_rows|get_character_profile_view_data|execute_phase2_action
---

<objective>
把当前 category-first HUD 改成用户期望的上浮/分层菜单，并把拜访与关系统一到一个可排序的通用选择器流程。

Purpose: 关闭 UAT gap 1、4、5，让玩家在同一 HUD 中直接看到五行动、复用表格选择器，并从关系入口顺畅进入角色信息。
Output: 新的浮动行动菜单结构、通用排序选择器、角色信息面板，以及证明这条 HUD 流程的自动回归。
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-旬内行动—关系闭环/02-CONTEXT.md
@.planning/phases/02-旬内行动—关系闭环/02-UAT.md
@.planning/debug/action-menu-popup-gap.md
@.planning/debug/visit-target-selector-ui.md
@.planning/debug/uat5-relation-button-panel.md
@three-kingdoms-simulator/scenes/main/MainScene.tscn
@three-kingdoms-simulator/scripts/ui/MainHUD.gd

<interfaces>
Expected from 02-05:

```gdscript
func get_available_phase2_actions() -> Array
func get_character_selector_rows(context_id: String) -> Array
func get_character_profile_view_data(character_id: String) -> Variant
```

```gdscript
class_name CharacterSelectorRow
var display_name: String
var faction_name: String
var city_name: String
var favor: int
var trust: int
var respect: int
var vigilance: int
var interaction_status: String
var selectable: bool
var disabled_reason: String
```

The old flow to replace:

```gdscript
func _on_action_button_pressed() -> void
func _refresh_action_menu() -> void
func _refresh_target_picker() -> void
func _on_relation_button_pressed() -> void
```

Do not keep `RelationPopup` as the primary relation entry after this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 抽象通用可排序角色选择器与角色信息面板组件</name>
  <files>
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/ui/CharacterSelectorDialog.gd
    three-kingdoms-simulator/scripts/ui/CharacterProfilePanel.gd
  </files>
  <action>创建两个可复用 UI 组件，而不是继续在 `MainHUD.gd` 里动态堆按钮。`CharacterSelectorDialog.gd` 必须渲染表格形态：顶部是可点击排序表头 `角色 / 势力 / 所在地 / 好感 / 信任 / 敬重 / 戒备 / 状态`，下方是可滚动行列表；点击任一表头时在升序/降序间切换，并按该列重排 `CharacterSelectorRow`。`CharacterProfilePanel.gd` 负责展示 `CharacterProfileViewData`：角色名、身份、势力、地点、官职、好感/信任/敬重/戒备/义务，以及 1~3 行说明文本。把这两个组件都挂到 `MainScene.tscn` 里，作为 HUD overlay 常驻节点，不再新增场景跳转。这个任务只负责通用组件与节点锚点，不改旧行动流程。</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$scene = Get-Content 'three-kingdoms-simulator/scenes/main/MainScene.tscn' -Raw; $selector = Get-Content 'three-kingdoms-simulator/scripts/ui/CharacterSelectorDialog.gd' -Raw; $profile = Get-Content 'three-kingdoms-simulator/scripts/ui/CharacterProfilePanel.gd' -Raw; $ok = $scene.Contains('CharacterSelectorDialog') -and $scene.Contains('CharacterProfilePanel') -and $selector.Contains('角色') -and $selector.Contains('势力') -and $selector.Contains('所在') -and $selector.Contains('好感') -and $selector.Contains('信任') -and $selector.Contains('敬重') -and $selector.Contains('戒备') -and $selector.Contains('状态') -and $selector.Contains('sort') -and $profile.Contains('favor') -and $profile.Contains('trust') -and $profile.Contains('obligation'); if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>主场景内已有通用表格选择器和角色信息面板两个复用组件，后续拜访/关系入口可以直接接它们。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 重接 HUD 菜单与关系流程，并用回归测试锁定新交互</name>
  <files>
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    three-kingdoms-simulator/scripts/tests/phase2_hud_menu_selector_regression.gd
  </files>
  <behavior>
    - Test 1: 点击 `行动` 后，首屏立即看到 `训练 / 读书 / 休整 / 拜访 / 巡察` 五项，而不是先看到分类轨。
    - Test 2: 行动菜单保留上浮/分层结构：**左侧固定为五个基础行动按钮，不是五大类**；右侧是当前选中行动的详情/二级操作区；禁用项仍可见并显示原因。
    - Test 3: 点击 `拜访` 后打开通用选择器；点击不同表头会改变第一行排序结果；确认目标后才执行 `visit`。
    - Test 4: 点击 `关系` 后打开同一个选择器；确认角色后打开 `CharacterProfilePanel`，不再打开旧 `RelationPopup`。
  </behavior>
  <action>改写 `MainHUD.gd` 与 `MainScene.tscn`，直接替换旧的 category-first 行为，并以 `02-CONTEXT.md` 中 **D-05R / D-07R / D-14R / D-15R** 为准。`ActionButton` 打开一个锚定到底部按钮上方的浮动菜单；菜单**左列只显示五个基础行动**，顺序必须与配置一致：`训练 / 读书 / 休整 / 拜访 / 巡察`，绝不能渲染成 `成长 / 关系 / 政务 / 军事 / 家族` 五大类轨道。右列显示当前选中行动的 `说明 / AP / 精力 / 目标类型 / 效果摘要 / 禁用原因 / 主操作按钮`。不要再让 `_on_action_button_pressed()` 强制落到 `成长` 分类；也不要再用 `PHASE2_CATEGORIES` 作为首屏 UI。五大类如仍需保留，只能作为 action metadata 或未来扩展标签，不得成为当前首屏点击层。`拜访` 的主操作按钮改为打开 `CharacterSelectorDialog`（context=`visit`）；`关系` 按钮也打开同一个 `CharacterSelectorDialog`（context=`relation`），选中后调用 `get_character_profile_view_data()` 打开 `CharacterProfilePanel`。从 `MainHUD.gd` 与 `MainScene.tscn` 移除或停用旧 `RelationPopup` 主流程，避免执行器继续沿错误方向维护。新建 `phase2_hud_menu_selector_regression.gd`，启动真实 `MainScene`，断言五行动首屏可见、表头排序会改变首行、`关系` 入口最终打开角色信息面板。验证命令使用显式 Godot console 路径。</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase2_hud_menu_selector_regression.gd'; exit $LASTEXITCODE"</automated>
  </verify>
  <done>HUD 已切成“上浮五行动 + 通用排序选择器 + 角色信息面板”流程，且自动回归能证明 visit/relation 共用同一组件链路。</done>
</task>

</tasks>

<verification>
这份 gap plan 只有在新 HUD 回归通过后才算完成；只改视觉不改自动回归，视为未关闭 UAT gaps。</verification>

<success_criteria>
- 行动按钮弹出的是首屏五行动上浮菜单，而不是旧的分类先行菜单。
- 身份、资源与目标不足都能在菜单里直接看到禁用态。
- 拜访与关系共用同一个可排序表格选择器。
- 关系入口最终落到角色信息面板，不再使用旧的 `RelationPopup` 主流程。
</success_criteria>

<output>
After completion, create `.planning/phases/02-旬内行动—关系闭环/02-06-SUMMARY.md`
</output>
