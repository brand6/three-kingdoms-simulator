# Phase 2: 旬内行动—关系闭环 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-05
**Phase:** 02-旬内行动—关系闭环
**Areas discussed:** 行动菜单形态

---

## 行动菜单形态

| Option | Description | Selected |
|--------|-------------|----------|
| 中央覆盖面板 | 保留顶部状态与底部导航，只把中间主内容切成行动菜单。 | |
| 右侧抽屉 | 从右侧滑出行动菜单，主画面仍可见，但会挤压右侧上下文信息。 | |
| 整页切换 | 进入独立行动页，空间最大，但会削弱 HUD 常驻感。 | |
| 自定义 | 从底部向上展开一个小的浮动菜单面板，更轻量，也方便切换。 | ✓ |

**User's choice:** 从底部向上展开一个小的浮动菜单面板。
**Notes:** 用户强调要“更轻度一点”，保留主 HUD 常驻感。

---

## 行动菜单第一层

| Option | Description | Selected |
|--------|-------------|----------|
| 先看五大分类 | 第一层只列成长、关系、政务、军事、家族五类。 | |
| 直接看全部行动 | 打开就列出所有可执行行动，再用标签区分分类。 | |
| 最近常用+分类 | 顶部放推荐或最近行动，下方再放完整分类。 | |
| 自定义 | 第一层只列大类，点击后向右侧展开子类。 | ✓ |

**User's choice:** 第一层只列大类，点击后向右展开子类，再根据动作情况打开相关界面或修改主界面显示。
**Notes:** 用户明确采用“先大类、后子类”的层级结构。

---

## 动作文档对齐

| Option | Description | Selected |
|--------|-------------|----------|
| 仅拜访/巡察进后续界面 | 简单动作直接结算，复杂动作再进入目标或详情。 | |
| 全部先开详情 | 所有动作先打开详情或确认页。 | |
| 全部可直接执行 | 所有动作点一下就结算。 | |
| 自定义 | 行动相关内容参照 `design/行动菜单结构设计 v1.md`。 | ✓ |

**User's choice:** 行动相关内容参照 `design/行动菜单结构设计 v1.md`。
**Notes:** 这份文档成为本 phase 的新增 canonical ref。

---

## 浮层与三级结构的结合方式

| Option | Description | Selected |
|--------|-------------|----------|
| 浮层只做分类入口 | 浮层只显示一级分类，分类后的动作列表放回主界面。 | |
| 浮层承载完整三级菜单 | 浮层内完成分类、动作、目标三层流程。 | |
| 浮层先选类再开独立动作面板 | 浮层选分类后再打开专门的行动面板。 | |
| 自定义 | 浮层完成分类、动作两个流程；若有第三层目标选择，则打开目标弹窗。 | ✓ |

**User's choice:** 浮层完成前两层流程，第三层目标选择单独用弹窗。
**Notes:** 这是本 phase 行动交互的核心结构约束。

---

## Phase 2 首批动作范围

| Option | Description | Selected |
|--------|-------------|----------|
| 先做 5 个基础行动 | 先围绕需求里的 visit/train/study/inspect/rest 跑通完整闭环。 | ✓ |
| 先做 12 个核心行动 | 按压缩版原型动作清单一次铺开更多分类。 | |
| 直接按 14 到 15 个动作铺开 | 尽量贴近文档最终推荐版。 | |

**User's choice:** 先做 5 个基础行动。
**Notes:** 用户选择最小动作集验证闭环，其他动作后置。

---

## 不可执行动作的显示方式

| Option | Description | Selected |
|--------|-------------|----------|
| 显示但灰掉并写原因 | 保留动作并显示条件不足原因。 | |
| 完全隐藏 | 不展示当前不可执行动作。 | |
| 仅显示可执行动作 | 只保留当前能点的动作。 | |
| 自定义 | 身份权限带来的动作隐藏；其他条件不足导致的动作灰掉并写原因。 | ✓ |

**User's choice:** 身份权限动作隐藏，其他条件不足动作灰显并说明原因。
**Notes:** 用户特别举例了 `AP` 不足这种资源条件。

---

## `inspect` 的中文动作映射

| Option | Description | Selected |
|--------|-------------|----------|
| 巡察 | 作为政务类基础动作处理。 | ✓ |
| 侦察 | 作为军事类基础动作处理。 | |
| 查看情报 | 作为信息类低成本行动处理。 | |

**User's choice:** 巡察。
**Notes:** Phase 2 会把 `inspect` 具体落成 `巡察`。

---

## 一级分类显示范围

| Option | Description | Selected |
|--------|-------------|----------|
| 五大类都显示 | 成长、关系、政务、军事、家族都出现。 | ✓ |
| 只显示当前有动作的分类 | 菜单更干净，但后续扩展变化更明显。 | |
| 五大类都显示但未开放类灰掉 | 保留结构，同时突出未开放状态。 | |

**User's choice:** 五大类都显示。
**Notes:** 用户保留整体信息架构稳定性，哪怕 Phase 2 只实装五个动作。

---

## the agent's Discretion

- 浮层的具体位置、尺寸、动画和内部控件组织。
- 目标弹窗的具体版式与字段呈现。
- 暂未实装动作或分类在菜单中的空状态文案。

## Deferred Ideas

None.
