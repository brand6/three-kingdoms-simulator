---
phase: 02-旬内行动—关系闭环
plan: "07"
type: execute
wave: 5
depends_on:
  - 02-04
  - 02-06
files_modified:
  - three-kingdoms-simulator/scenes/main/MainScene.tscn
  - three-kingdoms-simulator/scripts/ui/MainHUD.gd
  - three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd
autonomous: true
requirements:
  - CORE-04
  - CORE-05
  - UI-04
gap_closure: true
must_haves:
  truths:
    - 玩家第一次点击“结束本旬”时，就能看到一个尺寸合理的确认框，并且确认/取消按钮可见。
    - 确认框修复后，不影响原有旬总结、AP 重置和时间推进流程。
    - 自动回归能捕获“首次打开过大且按钮不可见”的布局 bug，而不仅仅检查 visible 状态。
  artifacts:
    - path: three-kingdoms-simulator/scenes/main/MainScene.tscn
      provides: 内容驱动的小型 EndXunDialog 初始尺寸。
    - path: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      provides: 首次打开前重算尺寸并以内容尺寸弹出确认框的逻辑。
    - path: three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd
      provides: 首次打开尺寸/按钮可见性 + 三次旬推进的回归。
  key_links:
    - from: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      to: three-kingdoms-simulator/scenes/main/MainScene.tscn
      via: EndXunDialog 的打开方式与节点尺寸约束
      pattern: EndXunDialog|reset_size|popup_centered
    - from: three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd
      to: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      via: 首次点击结束本旬后的对话框尺寸与按钮断言
      pattern: get_ok_button|get_cancel_button|size
---

<objective>
只修复“结束本旬”首次确认弹窗的首开尺寸与按钮可见性 bug，并用现有旬推进回归把它锁死。

Purpose: 关闭 UAT gap 7，确保第一次点击就得到稳定、可读、可确认的旬结束体验。
Output: 修正后的 `EndXunDialog` 弹出方式、首次打开布局回归，以及仍然通过的三旬推进链路。
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-旬内行动—关系闭环/02-UAT.md
@.planning/debug/end-xun-confirm-first-open.md
@three-kingdoms-simulator/scenes/main/MainScene.tscn
@three-kingdoms-simulator/scripts/ui/MainHUD.gd
@three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd

<interfaces>
Existing flow to preserve:

```gdscript
func _on_end_turn_button_pressed() -> void
func _on_end_xun_confirmed() -> void
func _show_xun_summary(summary: Variant) -> void
```

Current risky path to replace:

```gdscript
_end_xun_dialog.popup_centered_ratio(0.35)
```

The regression must continue to prove:

```gdscript
190年 1月 第1旬 -> 190年 1月 第2旬 -> 190年 1月 第3旬 -> 190年 2月 第1旬
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把结束本旬确认框改成内容驱动的小弹窗</name>
  <files>
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
  </files>
  <action>修复首开布局，而不是只改一个常量。把 `MainScene.tscn` 中 `EndXunDialog` 的尺寸意图收敛为轻量确认框；在 `MainHUD.gd` 新增专门的打开 helper，在每次显示前先同步正文、调用 `reset_size()`，然后用 `popup_centered(Vector2i(420, 180))` 或同级别的内容尺寸弹出，明确避免继续使用 `popup_centered_ratio(0.35)`。同时确保 OK/Cancel 按钮文本在首次打开就可见，不依赖第二次打开后的缓存布局。不要改坏 `_on_end_xun_confirmed()` 之后的总结、时间推进与 AP 重置流程。</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$hud = Get-Content 'three-kingdoms-simulator/scripts/ui/MainHUD.gd' -Raw; $scene = Get-Content 'three-kingdoms-simulator/scenes/main/MainScene.tscn' -Raw; $ok = -not $hud.Contains('popup_centered_ratio(0.35)') -and $hud.Contains('reset_size') -and $hud.Contains('popup_centered(') -and $scene.Contains('EndXunDialog'); if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>结束本旬确认框首次打开前会重算尺寸，并以轻量内容尺寸弹出；旧的按视口比例放大路径已移除。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展旬推进回归，锁定首次打开尺寸与按钮可见性</name>
  <files>
    three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd
  </files>
  <behavior>
    - Test 1: 第一次点击 `结束本旬` 后，`EndXunDialog` 立即可见，且 `get_ok_button()` 与 `get_cancel_button()` 都是可见状态。
    - Test 2: 第一次点击时确认框尺寸保持轻量，宽度不超过 520、高度不超过 280，避免首开巨大化。
    - Test 3: 在新增断言后，三次真实旬推进、旬总结出现、AP 重置与月份进位仍然全部通过。
  </behavior>
  <action>直接扩展 `phase2_xun_loop_regression.gd`，不要新建重复回归。让它在第一次调用 `_on_end_turn_button_pressed()` 后，先检查 `_end_xun_dialog.size`、`_end_xun_dialog.get_ok_button().visible`、`_end_xun_dialog.get_cancel_button().visible`，再继续确认并跑完原有三次旬推进流程。回归失败条件必须覆盖这次 UAT 现象：首次打开过大、按钮不可见、但第二次打开恢复正常。验证命令继续使用显式 Godot console 路径。</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase2_xun_loop_regression.gd'; exit $LASTEXITCODE"</automated>
  </verify>
  <done>自动回归现在不仅证明旬推进存在，还会在首次打开确认框布局异常时立即失败，防止 bug 再回归。</done>
</task>

</tasks>

<verification>
只有当 `phase2_xun_loop_regression.gd` 同时证明“首开确认框正确”与“三旬推进稳定”时，这个 gap 才算关闭。</verification>

<success_criteria>
- 第一次点击“结束本旬”就能看见确认与取消按钮。
- 确认框尺寸明显小于旧版比例弹窗，不再占用大面积视口。
- 修复后旬总结、AP 重置、月份进位链路仍然稳定。
</success_criteria>

<output>
After completion, create `.planning/phases/02-旬内行动—关系闭环/02-07-SUMMARY.md`
</output>
