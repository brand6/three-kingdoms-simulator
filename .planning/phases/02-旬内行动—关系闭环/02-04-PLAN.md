---
phase: 02-旬内行动—关系闭环
plan: "04"
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
  - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
  - three-kingdoms-simulator/scripts/ui/MainHUD.gd
  - three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd
autonomous: true
requirements:
  - CORE-04
  - CORE-05
  - UI-04
  - ACTN-05
  - RELA-03
must_haves:
  truths:
    - The player can end the current xun, see a readable summary, and continue into the next xun without instability.
    - At least three consecutive xun transitions work, including the rollover from xun 3 to the next month.
    - The xun-end summary explains state and relationship changes using the real action history from this xun.
  artifacts:
    - path: three-kingdoms-simulator/scripts/autoload/TimeManager.gd
      provides: Stable xun-advance and formatted-label methods for repeated transitions.
    - path: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      provides: End-xun orchestration, AP reset, and latest summary generation.
    - path: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      provides: End-xun confirmation and summary-dialog rendering.
    - path: three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd
      provides: Headless regression for multi-xun stability and summary visibility.
  key_links:
    - from: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      to: three-kingdoms-simulator/scripts/autoload/TimeManager.gd
      via: end-xun time advance and HUD label refresh
      pattern: advance_xun|initialize|get_current_label
    - from: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      to: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      via: end-xun confirmation and latest summary retrieval
      pattern: end_current_xun|get_latest_xun_summary
---

<objective>
Complete the playable Phase 2 loop by adding xun advancement, xun-end summary feedback, and a multi-xun headless regression.

Purpose: Turn isolated action execution into a stable one-xun planning loop that the player can repeat and trust.
Output: Working `结束本旬` flow, xun summary generation, repeated time progression, and an integration regression proving three stable transitions.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-旬内行动—关系闭环/02-CONTEXT.md
@.planning/phases/02-旬内行动—关系闭环/02-UI-SPEC.md
@.planning/phases/02-旬内行动—关系闭环/02-RESEARCH.md
@three-kingdoms-simulator/scripts/autoload/GameRoot.gd
@three-kingdoms-simulator/scripts/autoload/TimeManager.gd
@three-kingdoms-simulator/scripts/ui/MainHUD.gd

<interfaces>
Expected from Plan 02:

```gdscript
func execute_phase2_action(action_id: String, target_character_id: String = "") -> ActionResolution
func get_latest_action_resolution() -> ActionResolution
```

Expected from Plan 01:

```gdscript
class_name XunSummaryData
var xun_label: String
var action_lines: Array[String]
var stat_delta_totals: Dictionary
var relation_change_lines: Array[String]
var prompt_lines: Array[String]
```

Existing from `TimeManager.gd`:

```gdscript
func initialize(start_year: int, start_month: int, start_xun: int) -> void
func get_current_label() -> String
```

This plan must extend these methods without breaking the Phase 1 time-label contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement end-xun orchestration, AP reset, and summary generation</name>
  <read_first>
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    three-kingdoms-simulator/scripts/autoload/TimeManager.gd
    three-kingdoms-simulator/scripts/runtime/GameSession.gd
    three-kingdoms-simulator/data/generated/190/characters.json
    .planning/phases/02-旬内行动—关系闭环/02-RESEARCH.md
  </read_first>
  <files>
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    three-kingdoms-simulator/scripts/autoload/TimeManager.gd
  </files>
  <action>Extend `TimeManager.gd` with exact methods `advance_xun() -> void` and `get_xun_label(year: int, month: int, xun: int) -> String`, preserving the visible format `190年 / 1月 / 第1旬`. Implement xun rollover rules as exact logic: `1 -> 2`, `2 -> 3`, `3 -> next month 1`; when `current_month` exceeds `12`, increment year and reset month to `1`. In `GameRoot.gd`, add exact methods `end_current_xun() -> XunSummaryData` and `get_latest_xun_summary() -> XunSummaryData`. `end_current_xun()` must: build a summary from `current_session.current_xun_action_history`, total the stat deltas, aggregate relation change lines, append at least one planning prompt line, reset the protagonist AP back to the base value from `CharacterDefinition.status_values.ap`, clear the per-xun action history, advance `TimeManager`, and keep accumulated fame/merit/energy/stress/relation changes intact. The summary must name the finishing xun using the old time label before advancing, not the next xun label.</action>
  <acceptance_criteria>
    - `TimeManager.gd` contains exact methods `advance_xun` and `get_xun_label`.
    - `GameRoot.gd` contains exact methods `end_current_xun` and `get_latest_xun_summary`.
    - `GameRoot.gd` reads the protagonist base AP from `CharacterDefinition.status_values.ap` when resetting AP.
    - `GameRoot.gd` clears `current_xun_action_history` after producing the summary.
    - `GameRoot.gd` preserves relation and cumulative stat state across xun transitions.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$time = Get-Content 'three-kingdoms-simulator/scripts/autoload/TimeManager.gd' -Raw; $root = Get-Content 'three-kingdoms-simulator/scripts/autoload/GameRoot.gd' -Raw; $ok = $time.Contains('func advance_xun') -and $time.Contains('func get_xun_label') -and $root.Contains('func end_current_xun') -and $root.Contains('func get_latest_xun_summary') -and $root.Contains('status_values') -and $root.Contains('clear_xun_action_history'); if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>The runtime can now end a xun, reset AP correctly, advance time stably, and capture an explanation-oriented summary from real action history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the end-xun HUD flow and prove three stable transitions with a headless regression</name>
  <read_first>
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd
    D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe
  </read_first>
  <files>
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd
  </files>
  <behavior>
    - Test 1: pressing `结束本旬` opens a confirmation dialog instead of advancing immediately.
    - Test 2: after confirmation, the HUD shows a xun summary dialog that includes action lines, stat deltas, and relation change lines.
    - Test 3: the real HUD survives three consecutive xun transitions and the time label reaches `190年 / 2月 / 第1旬` from the Phase 1 default start.
    - Test 4: after each xun end, protagonist AP resets to `3` while fame/merit and relation changes persist.
  </behavior>
  <action>In `MainHUD.gd`, enable `EndTurnButton`, route it to an end-xun confirmation dialog, and only call `GameRoot.end_current_xun()` after confirmation. After the call, immediately refresh the HUD labels from live runtime state and open the xun-summary dialog using the exact content order `本旬行动摘要 -> 主要数值变化 -> 关系变化摘要 -> 新提示`. Also update the right-side hint text so the next xun's planning prompt is visible after closing the summary. Create `scripts/tests/phase2_xun_loop_regression.gd` as a headless integration test that boots the real `MainScene`, executes at least one real action before each xun end, confirms the end-xun flow three times, asserts the time progression `190年 / 1月 / 第1旬 -> 190年 / 1月 / 第2旬 -> 190年 / 1月 / 第3旬 -> 190年 / 2月 / 第1旬`, checks that AP resets to `3` after each end, and fails if the summary dialog omits action lines, stat deltas, or relation-change text. Reuse the explicit Godot console binary path for verification.</action>
  <acceptance_criteria>
    - `MainHUD.gd` contains logic tied to `EndTurnButton` and an end-xun confirmation flow.
    - `MainHUD.gd` contains the exact summary section labels `本旬行动摘要`, `主要数值变化`, `关系变化摘要`, and `新提示`.
    - `phase2_xun_loop_regression.gd` exists and asserts the exact time rollover to `190年 / 2月 / 第1旬`.
    - The headless regression exits `0` with the explicit Godot 4.6.1 console binary.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$hud = Get-Content 'three-kingdoms-simulator/scripts/ui/MainHUD.gd' -Raw; if (-not ($hud.Contains('EndTurnButton') -and $hud.Contains('本旬行动摘要') -and $hud.Contains('主要数值变化') -and $hud.Contains('关系变化摘要') -and $hud.Contains('新提示') -and (Test-Path 'three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd'))) { exit 1 }; & 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase2_xun_loop_regression.gd'; exit $LASTEXITCODE"</automated>
  </verify>
  <done>The complete Phase 2 loop is repeatable: actions feed a readable xun summary, time advances cleanly for three transitions, and the HUD is ready for the player to plan the next xun.</done>
</task>

</tasks>

<verification>
The final gate is the headless multi-xun regression. If it cannot prove three stable transitions plus summary visibility, Phase 2 is not done.</verification>

<success_criteria>
- `结束本旬` is no longer a disabled placeholder.
- Xun-end summaries explain what changed and why.
- Three consecutive xun transitions work, including month rollover.
- AP resets each xun while meaningful state changes persist.
</success_criteria>

<output>
After completion, create `.planning/phases/02-旬内行动—关系闭环/02-04-SUMMARY.md`
</output>
