---
phase: 02-旬内行动—关系闭环
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd
  - three-kingdoms-simulator/scripts/systems/Phase2ActionResolver.gd
  - three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
  - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
  - three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd
autonomous: true
requirements:
  - CORE-03
  - ACTN-03
  - ACTN-04
  - ACTN-05
  - CHAR-03
  - RELA-01
  - RELA-03
must_haves:
  truths:
    - The session exposes exactly five foundational actions with deterministic availability and settlement rules.
    - Executing an action changes AP, energy, stress, fame, merit, or relationships in visible, testable ways.
    - Failed actions still return a structured result with a reason and limited consequence instead of silent rejection.
  artifacts:
    - path: three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd
      provides: One catalog containing the five Phase 2 actions and their UI-facing metadata.
    - path: three-kingdoms-simulator/scripts/systems/Phase2ActionResolver.gd
      provides: Deterministic settlement rules and disabled-reason generation.
    - path: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      provides: HUD-facing orchestration methods for available actions, execution, and relation snapshots.
    - path: three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd
      provides: Headless regression of exact action outcomes.
  key_links:
    - from: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      to: three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd
      via: available-action query for the current protagonist
      pattern: get_available_phase2_actions|Phase2ActionCatalog
    - from: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      to: three-kingdoms-simulator/scripts/systems/Phase2ActionResolver.gd
      via: execute-action API returning ActionResolution
      pattern: execute_phase2_action|ActionResolution
---

<objective>
Implement the backend gameplay rules for the five Phase 2 actions and expose them through stable `GameRoot` APIs.

Purpose: Give the HUD a real action loop instead of placeholder copy while keeping the logic deterministic and verifier-friendly.
Output: A five-action catalog, an execution resolver, updated runtime character fields, GameRoot orchestration methods, and a headless backend regression.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-旬内行动—关系闭环/02-CONTEXT.md
@.planning/phases/02-旬内行动—关系闭环/02-RESEARCH.md
@.planning/phases/02-旬内行动—关系闭环/02-01-PLAN.md
@three-kingdoms-simulator/scripts/autoload/GameRoot.gd
@three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
@three-kingdoms-simulator/data/generated/190/characters.json

<interfaces>
Expected from Plan 01:

```gdscript
class_name Phase2ActionSpec
var id: String
var display_name: String
var category_id: String
var ap_cost: int
var energy_delta: int
var target_type: String
var effect_summary: String
var required_permission_tags: Array[String]
var disabled_reason: String
var hidden_when_locked: bool

class_name ActionResolution
var success: bool
var reason_text: String
var stat_deltas: Dictionary
var relation_deltas: Dictionary
var clue_text: String
var summary_line: String
```

Existing from `RuntimeCharacterState.gd`:

```gdscript
var ap: int = 0
var energy: int = 0
var stress: int = 0
var fame: int = 0
var merit: int = 0
var current_city_id: String = ""
```

This plan may extend `RuntimeCharacterState`, but it must preserve the existing fields and `from_definition(...)` bootstrap path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the five-action catalog with exact Phase 2 metadata and availability rules</name>
  <read_first>
    .planning/phases/02-旬内行动—关系闭环/02-CONTEXT.md
    .planning/phases/02-旬内行动—关系闭环/02-UI-SPEC.md
    .planning/phases/02-旬内行动—关系闭环/02-RESEARCH.md
    three-kingdoms-simulator/data/generated/190/characters.json
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
  </read_first>
  <files>
    three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
  </files>
  <behavior>
    - Test 1: `get_available_phase2_actions()` for `cao_cao` returns visible actions for all five locked categories, with the shipped actions `训练 / 读书 / 休整 / 拜访 / 巡察` carrying exact AP, energy, target-type, and effect-summary values.
    - Test 2: permission-locked actions such as `巡察` are hidden for characters lacking `inspect` and `lead`, while AP/energy/target failures leave the action visible with exact disabled reasons like `AP 不足` or `暂无可拜访对象`.
  </behavior>
  <action>Create `Phase2ActionCatalog.gd` as the only source of truth for the five shipped actions per D-07..D-13. Encode these exact records: `train/训练/成长/AP1/energy-10/none/效果: 武艺历练 +6，压力 +3，功绩 +1`; `study/读书/成长/AP1/energy-8/none/效果: 智略历练 +6，压力 +2，名望 +1`; `rest/休整/成长/AP1/energy+20/none/效果: 精力 +20，压力 -12`; `visit/拜访/关系/AP1/energy-8/character/效果: 好感 +10，信任 +6，敬重 +2，戒备 -4`; `inspect/巡察/政务/AP1/energy-10/none/效果: 功绩 +5，政务历练 +4，压力 +4`. Expose a catalog method that always returns the five fixed top-level categories and filters only the action rows. In `GameRoot.gd`, add exact query APIs `get_available_phase2_actions() -> Array[Phase2ActionSpec]` and `get_relation_overview() -> Array[RuntimeRelationState]`, both based on `current_session.protagonist_id`. Honor D-11 by hiding permission-locked actions; honor D-12 by keeping visible-but-disabled actions with exact reason strings `AP 不足`, `精力不足`, `当前地点不可执行`, or `暂无可拜访对象`.</action>
  <acceptance_criteria>
    - `Phase2ActionCatalog.gd` contains the five exact IDs `train`, `study`, `rest`, `visit`, and `inspect`.
    - `Phase2ActionCatalog.gd` contains the five exact category labels `成长`, `关系`, `政务`, `军事`, and `家族`.
    - `GameRoot.gd` contains exact methods `get_available_phase2_actions` and `get_relation_overview`.
    - `GameRoot.gd` or the catalog code contains the exact disabled reason strings `AP 不足`, `精力不足`, `当前地点不可执行`, and `暂无可拜访对象`.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$catalog = Get-Content 'three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd' -Raw; $root = Get-Content 'three-kingdoms-simulator/scripts/autoload/GameRoot.gd' -Raw; $ok = $catalog.Contains('train') -and $catalog.Contains('study') -and $catalog.Contains('rest') -and $catalog.Contains('visit') -and $catalog.Contains('inspect') -and $catalog.Contains('成长') -and $catalog.Contains('关系') -and $catalog.Contains('政务') -and $catalog.Contains('军事') -and $catalog.Contains('家族'); $ok = $ok -and $root.Contains('func get_available_phase2_actions') -and $root.Contains('func get_relation_overview') -and ($catalog.Contains('AP 不足') -or $root.Contains('AP 不足')) -and ($catalog.Contains('暂无可拜访对象') -or $root.Contains('暂无可拜访对象')); if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>The repo has one canonical Phase 2 action catalog and `GameRoot` exposes HUD-safe query methods that already respect hidden-vs-disabled rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic action settlement, partial-failure feedback, and a headless resolver regression</name>
  <read_first>
    three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    .planning/phases/02-旬内行动—关系闭环/02-RESEARCH.md
    D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe
  </read_first>
  <files>
    three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
    three-kingdoms-simulator/scripts/systems/Phase2ActionResolver.gd
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd
  </files>
  <behavior>
    - Test 1: `train` from the default Cao Cao state spends 1 AP, 10 energy, adds 3 stress, 1 merit, and records a success `ActionResolution`.
    - Test 2: `rest` spends 1 AP, restores 20 energy, reduces stress by 12, and does not increase merit.
    - Test 3: `visit` against `chen_gong` updates the directional relation values by the exact deltas `favor +10`, `trust +6`, `respect +2`, `vigilance -4`, `obligation +1`.
    - Test 4: a stale/invalid `visit` target returns `success = false`, keeps the action visible as failed feedback, adds at least `stress +2`, and fills `clue_text` with a non-empty explanation.
    - Test 5: the resolver appends each result to the session history so later xun-summary code can consume it.
  </behavior>
  <action>Extend `RuntimeCharacterState.gd` with exact numeric fields `martial_exp`, `strategy_exp`, and `governance_exp`, initializing them to `0` inside `from_definition(...)`. Create `Phase2ActionResolver.gd` with exact settlement rules from research: `train` => `ap -1, energy -10, stress +3, merit +1, martial_exp +6`; `study` => `ap -1, energy -8, stress +2, fame +1, strategy_exp +6`; `rest` => `ap -1, energy +20, stress -12`; `inspect` => `ap -1, energy -10, stress +4, merit +5, governance_exp +4`; `visit` on valid local target => `ap -1, energy -8, fame +1` plus relation deltas `favor +10, trust +6, respect +2, vigilance -4, obligation +1`. For failed `visit`, return `success=false`, `title='行动失败'`, a concrete `reason_text`, `stress +2`, and non-empty `clue_text`. In `GameRoot.gd`, add exact methods `execute_phase2_action(action_id: String, target_character_id: String = "") -> ActionResolution` and `get_latest_action_resolution() -> ActionResolution`, and append every resolution into `current_session.current_xun_action_history`. Create `scripts/tests/phase2_action_resolver_test.gd` as a headless script that boots the real autoload path, runs the behaviors above, and exits non-zero on any mismatch. Use the explicit Godot console binary path shown in `read_first` for verification.</action>
  <acceptance_criteria>
    - `RuntimeCharacterState.gd` contains exact fields `martial_exp`, `strategy_exp`, and `governance_exp`.
    - `GameRoot.gd` contains exact methods `execute_phase2_action` and `get_latest_action_resolution`.
    - `Phase2ActionResolver.gd` contains the exact delta strings or keys for `martial_exp`, `strategy_exp`, `governance_exp`, `favor`, `trust`, `respect`, `vigilance`, and `obligation`.
    - `phase2_action_resolver_test.gd` exists and asserts both successful and failed action behavior.
    - The headless test exits `0` with the explicit Godot 4.6.1 console binary.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$root = Get-Content 'three-kingdoms-simulator/scripts/autoload/GameRoot.gd' -Raw; $state = Get-Content 'three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd' -Raw; $resolver = Get-Content 'three-kingdoms-simulator/scripts/systems/Phase2ActionResolver.gd' -Raw; if (-not ($state.Contains('martial_exp') -and $state.Contains('strategy_exp') -and $state.Contains('governance_exp') -and $root.Contains('func execute_phase2_action') -and $root.Contains('func get_latest_action_resolution') -and $resolver.Contains('favor') -and $resolver.Contains('trust') -and $resolver.Contains('respect') -and $resolver.Contains('vigilance') -and $resolver.Contains('obligation'))) { exit 1 }; & 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase2_action_resolver_test.gd'; exit $LASTEXITCODE"</automated>
  </verify>
  <done>Phase 2 has deterministic, test-backed action execution with concrete stat and relation mutations plus structured failed-action feedback.</done>
</task>

</tasks>

<verification>
The backend is only complete when the headless resolver regression proves exact settlement deltas for success and failure paths.</verification>

<success_criteria>
- The five required actions exist as real gameplay logic, not just UI labels.
- `GameRoot` exposes callable Phase 2 action APIs for the HUD.
- Action execution visibly mutates runtime state and relation state.
- Failed actions still generate a non-silent result object.
</success_criteria>

<output>
After completion, create `.planning/phases/02-旬内行动—关系闭环/02-02-SUMMARY.md`
</output>
