---
phase: 01-190
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - three-kingdoms-simulator/project.godot
  - three-kingdoms-simulator/scenes/main/MainScene.tscn
  - three-kingdoms-simulator/themes/PrototypeTheme.tres
  - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
  - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
  - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
autonomous: true
requirements:
  - CORE-01
  - CORE-02
must_haves:
  truths:
    - Game starts into the Phase 1 main scene instead of a menu or empty viewport.
    - The approved HUD shell is present with top bar, left overview, center summary, right context, and bottom navigation.
    - Core manager entrypoints exist before data and gameplay wiring begins.
  artifacts:
    - three-kingdoms-simulator/project.godot
    - three-kingdoms-simulator/scenes/main/MainScene.tscn
    - three-kingdoms-simulator/themes/PrototypeTheme.tres
    - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
    - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
  key_links:
    - three-kingdoms-simulator/project.godot -> res://scenes/main/MainScene.tscn via run/main_scene
    - three-kingdoms-simulator/project.godot -> autoload GameRoot/DataRepository/TimeManager
---

<objective>
Establish the Godot runtime shell for Phase 1 inside the existing `three-kingdoms-simulator/` project.

Purpose: Lock the project entrypoint, approved HUD frame, and autoload contracts before data and runtime wiring begin.
Output: A bootable `MainScene`, one shared `Theme`, and three registered manager skeletons.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-190/01-CONTEXT.md
@.planning/phases/01-190/01-RESEARCH.md
@.planning/phases/01-190/01-UI-SPEC.md
@design/原型与实现/Godot 原型开发拆解 v1.md
@design/UIUX/原型 UI 流程图 v1.md
@three-kingdoms-simulator/project.godot
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the approved MainScene and shared Phase 1 theme</name>
  <files>three-kingdoms-simulator/project.godot, three-kingdoms-simulator/scenes/main/MainScene.tscn, three-kingdoms-simulator/themes/PrototypeTheme.tres</files>
  <read_first>
    - three-kingdoms-simulator/project.godot
    - .planning/phases/01-190/01-CONTEXT.md
    - .planning/phases/01-190/01-UI-SPEC.md
    - design/UIUX/原型 UI 流程图 v1.md
  </read_first>
  <action>Per D-02, D-04, D-05, D-06, D-13, and D-14, keep the existing Godot root at `three-kingdoms-simulator/` and set `run/main_scene="res://scenes/main/MainScene.tscn"` in `project.godot`. Create `MainScene.tscn` with a full-screen `Control` root using the UI-SPEC container contract: `MarginContainer -> VBoxContainer`, then top bar, middle body, and bottom bar. The top bar must reserve 64px height and include labels for `190年 / 月 / 旬`, `当前城市`, `当前身份`, `所属势力`, and `当前官职`. The middle body must be an `HBoxContainer` with left and right `PanelContainer` columns using `custom_minimum_size.x = 320`, a center `PanelContainer` that expands horizontally, and the center card text `已进入 190 样本`. The bottom bar must reserve 72px height and render visible-but-disabled buttons named `行动`, `角色`, `关系`, `势力`, `家族/士族`, and `日志`, plus a persistent explanation label `灰色入口将在后续阶段开放`. Create `PrototypeTheme.tres` and apply the UI-SPEC values directly: background `#F5F0E6`, panel surfaces `#E4D8C5`, accent `#2F5D62`, destructive `#B42318`, body size 16, label size 14, heading size 20, display size 28, and disabled text `#9B9284`. Do not create placeholder pages or activate future navigation.</action>
  <acceptance_criteria>
    - `three-kingdoms-simulator/project.godot` contains `run/main_scene="res://scenes/main/MainScene.tscn"`
    - `three-kingdoms-simulator/scenes/main/MainScene.tscn` contains `MainScene`, `Control`, `MarginContainer`, `VBoxContainer`, `HBoxContainer`, `PanelContainer`, and the labels `已进入 190 样本` and `灰色入口将在后续阶段开放`
    - `three-kingdoms-simulator/themes/PrototypeTheme.tres` contains `2f5d62`, `f5f0e6`, and `e4d8c5`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$a = Select-String -Path 'three-kingdoms-simulator/project.godot' -Pattern 'run/main_scene=\"res://scenes/main/MainScene.tscn\"' -Quiet; $b = Select-String -Path 'three-kingdoms-simulator/scenes/main/MainScene.tscn' -Pattern '已进入 190 样本|灰色入口将在后续阶段开放|PanelContainer' -Quiet; $c = Select-String -Path 'three-kingdoms-simulator/themes/PrototypeTheme.tres' -Pattern '2f5d62|F5F0E6|E4D8C5' -Quiet; if ($a -and $b -and $c) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>`project.godot` opens `MainScene`, the HUD shell matches the approved Phase 1 layout, and all future module entries are present but disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Register autoload manager contracts without implementing gameplay logic yet</name>
  <files>three-kingdoms-simulator/project.godot, three-kingdoms-simulator/scripts/autoload/GameRoot.gd, three-kingdoms-simulator/scripts/autoload/DataRepository.gd, three-kingdoms-simulator/scripts/autoload/TimeManager.gd</files>
  <read_first>
    - three-kingdoms-simulator/project.godot
    - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
    - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
    - design/原型与实现/Godot 系统模块拆分清单 v1.md
    - .planning/phases/01-190/01-CONTEXT.md
  </read_first>
  <action>Per D-03, D-08, D-13, and the architecture guidance in `Godot 系统模块拆分清单 v1.md`, create typed GDScript autoload skeletons named `GameRoot`, `DataRepository`, and `TimeManager` under `scripts/autoload/`. Register all three in `project.godot` as autoloads. `GameRoot.gd` must expose exact method names `bootstrap_default_entry()` and `show_boot_error(message: String)`; `DataRepository.gd` must expose `load_phase1_smoke_sample()`, `get_scenario(id: String)`, `get_character(id: String)`, `get_faction(id: String)`, and `get_city(id: String)`; `TimeManager.gd` must expose `initialize(start_year: int, start_month: int, start_xun: int)` and `get_current_label() -> String`. Keep these as contracts and placeholders only in this plan: do not implement JSON parsing, runtime state, or live HUD binding here.</action>
  <acceptance_criteria>
    - `three-kingdoms-simulator/project.godot` contains autoload entries for `GameRoot`, `DataRepository`, and `TimeManager`
    - `three-kingdoms-simulator/scripts/autoload/GameRoot.gd` contains `func bootstrap_default_entry()` and `func show_boot_error(message: String)`
    - `three-kingdoms-simulator/scripts/autoload/DataRepository.gd` contains `func load_phase1_smoke_sample()` and all four `get_*` query methods
    - `three-kingdoms-simulator/scripts/autoload/TimeManager.gd` contains `func initialize(start_year: int, start_month: int, start_xun: int)` and `func get_current_label() -> String`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$a = Select-String -Path 'three-kingdoms-simulator/project.godot' -Pattern 'GameRoot|DataRepository|TimeManager' -Quiet; $b = Select-String -Path 'three-kingdoms-simulator/scripts/autoload/GameRoot.gd' -Pattern 'bootstrap_default_entry|show_boot_error' -Quiet; $c = Select-String -Path 'three-kingdoms-simulator/scripts/autoload/DataRepository.gd' -Pattern 'load_phase1_smoke_sample|get_scenario|get_character|get_faction|get_city' -Quiet; $d = Select-String -Path 'three-kingdoms-simulator/scripts/autoload/TimeManager.gd' -Pattern 'initialize\(start_year: int, start_month: int, start_xun: int\)|get_current_label' -Quiet; if ($a -and $b -and $c -and $d) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>The project has stable autoload entrypoints for later plans, and downstream work can target fixed method names instead of inventing manager APIs.</done>
</task>

</tasks>

<verification>
Confirm the shell is ready for downstream work by checking the main scene path, UI contract strings, and autoload registrations.
</verification>

<success_criteria>
- `project.godot` points at `res://scenes/main/MainScene.tscn`
- `MainScene.tscn` contains the approved Phase 1 HUD shell
- `PrototypeTheme.tres` captures the Phase 1 design tokens
- `GameRoot`, `DataRepository`, and `TimeManager` are registered autoloads with fixed method contracts
</success_criteria>

<output>
After completion, create `.planning/phases/01-190/01-190-01-SUMMARY.md`
</output>
