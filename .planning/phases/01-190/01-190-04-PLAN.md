---
phase: 01-190
plan: "04"
type: execute
wave: 3
depends_on:
  - 01-190-01
  - 01-190-02
  - 01-190-03
files_modified:
  - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
  - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
  - three-kingdoms-simulator/scripts/ui/MainHUD.gd
  - three-kingdoms-simulator/scenes/main/MainScene.tscn
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CHAR-01
  - CHAR-02
  - CHAR-04
must_haves:
  truths:
    - Launching the game auto-loads the default 190 smoke sample and enters the HUD as one historical character.
    - The HUD shows time, city, identity, faction, office, AP, energy, stress, fame, and merit on first load.
    - Loading, success, and failure states are visible inside the HUD frame instead of blank or broken screens.
  artifacts:
    - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
    - three-kingdoms-simulator/scripts/ui/MainHUD.gd
    - three-kingdoms-simulator/scenes/main/MainScene.tscn
  key_links:
    - GameRoot -> DataRepository.load_phase1_smoke_sample -> bootstrap_session("scenario_190_smoke", "cao_cao")
    - MainHUD -> GameSession and TimeManager -> visible field labels
---

<objective>
Wire the Phase 1 shell into a real default-protagonist entry flow and live HUD display.

Purpose: Complete the phase outcome by turning the shell and repository into a working single-character 190 sample entrypoint.
Output: Automatic boot into `scenario_190_smoke` as `cao_cao` with HUD-bound time and character state.</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-190/01-CONTEXT.md
@.planning/phases/01-190/01-RESEARCH.md
@.planning/phases/01-190/01-UI-SPEC.md
@three-kingdoms-simulator/scripts/autoload/GameRoot.gd
@three-kingdoms-simulator/scripts/autoload/TimeManager.gd
@three-kingdoms-simulator/scripts/autoload/DataRepository.gd
@three-kingdoms-simulator/scripts/runtime/GameSession.gd
@three-kingdoms-simulator/scenes/main/MainScene.tscn

<interfaces>
Required repository and manager methods:

```gdscript
func bootstrap_default_entry()
func show_boot_error(message: String)
func load_phase1_smoke_sample()
func bootstrap_session(scenario_id: String, protagonist_id: String) -> GameSession
func initialize(start_year: int, start_month: int, start_xun: int)
func get_current_label() -> String
```

Required concrete boot IDs:

```text
scenario_190_smoke
cao_cao
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the default 190 protagonist boot path</name>
  <files>three-kingdoms-simulator/scripts/autoload/GameRoot.gd, three-kingdoms-simulator/scripts/autoload/TimeManager.gd</files>
  <read_first>
    - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
    - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
    - .planning/phases/01-190/01-CONTEXT.md
  </read_first>
  <action>Per D-01, D-02, D-07, and D-08, implement `GameRoot.bootstrap_default_entry()` to call `DataRepository.load_phase1_smoke_sample()`, then `DataRepository.bootstrap_session("scenario_190_smoke", "cao_cao")`. After that, call `TimeManager.initialize()` with the session's starting year/month/xun and expose the current session to the HUD layer. Implement `show_boot_error(message: String)` so the HUD can render a Phase 1 failure card instead of leaving the scene half-built. `TimeManager.get_current_label()` must return a string in the visible Phase 1 format `190年 / {month}月 / 第{xun}旬`. Do not add scenario selection, character selection, or multi-identity UI in this plan.</action>
  <acceptance_criteria>
    - `GameRoot.gd` contains `scenario_190_smoke` and `cao_cao`
    - `GameRoot.gd` calls `load_phase1_smoke_sample` and `bootstrap_session`
    - `TimeManager.gd` contains the visible label format `年 /` and `旬`
    - `GameRoot.gd` contains `show_boot_error(message: String)` and uses it for load failure handling
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$a = Select-String -Path 'three-kingdoms-simulator/scripts/autoload/GameRoot.gd' -Pattern 'scenario_190_smoke|cao_cao|load_phase1_smoke_sample|bootstrap_session|show_boot_error' -Quiet; $b = Select-String -Path 'three-kingdoms-simulator/scripts/autoload/TimeManager.gd' -Pattern '年 /|旬|get_current_label' -Quiet; if ($a -and $b) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>The game has one concrete default boot path into the 190 smoke sample and a formatted time label ready for HUD binding.</done>
</task>

<task type="auto">
  <name>Task 2: Bind the MainHUD to live session data and render loading/success/error states</name>
  <files>three-kingdoms-simulator/scripts/ui/MainHUD.gd, three-kingdoms-simulator/scenes/main/MainScene.tscn</files>
  <read_first>
    - three-kingdoms-simulator/scenes/main/MainScene.tscn
    - .planning/phases/01-190/01-UI-SPEC.md
    - .planning/phases/01-190/01-CONTEXT.md
    - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
    - three-kingdoms-simulator/scripts/runtime/GameSession.gd
  </read_first>
  <action>Attach `scripts/ui/MainHUD.gd` to the HUD root and bind it to the live session created by `GameRoot`. Populate the exact first-screen fields required by D-05 and CORE-02: top bar shows formatted time, current city, identity, faction, and office; left overview shows name, AP, energy, stress, fame, and merit; any missing optional values must display `—` instead of `null`, `N/A`, or raw keys. Use the right column for exactly three modules: `当前任务`, `最近事件`, and `重要提示`, with Phase 1 empty-state text from the UI-SPEC. The center card must support three explicit states: loading text `正在加载 190 样本…`, success heading `已进入 190 样本`, and failure text `190 样本加载失败。请检查 Luban JSON 导出文件、默认主角 ID 与数据路径配置，然后重新启动项目。` Keep the bottom navigation buttons disabled and keep the persistent line `灰色入口将在后续阶段开放`. Do not add hidden tabs, popup placeholders, or map-first navigation.</action>
  <acceptance_criteria>
    - `MainHUD.gd` contains the field labels `当前城市`, `当前身份`, `所属势力`, `当前官职`, `AP`, `精力`, `压力`, `名望`, and `功绩`
    - `MainHUD.gd` contains `正在加载 190 样本…`, `已进入 190 样本`, and `190 样本加载失败`
    - `MainHUD.gd` contains logic that renders `—` for missing values
    - `MainScene.tscn` references `MainHUD.gd`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$a = Select-String -Path 'three-kingdoms-simulator/scripts/ui/MainHUD.gd' -Pattern '当前城市|当前身份|所属势力|当前官职|AP|精力|压力|名望|功绩' -Quiet; $b = Select-String -Path 'three-kingdoms-simulator/scripts/ui/MainHUD.gd' -Pattern '正在加载 190 样本…|已进入 190 样本|190 样本加载失败' -Quiet; $c = Select-String -Path 'three-kingdoms-simulator/scripts/ui/MainHUD.gd' -Pattern '—' -Quiet; $d = Select-String -Path 'three-kingdoms-simulator/scenes/main/MainScene.tscn' -Pattern 'MainHUD.gd' -Quiet; if ($a -and $b -and $c -and $d) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>On boot, the Phase 1 HUD can show a concrete historical character and all required first-screen state inside the approved UI frame.</done>
</task>

</tasks>

<verification>
Verify that the default boot path is concrete (`scenario_190_smoke` + `cao_cao`) and that the HUD script contains all Phase 1 required field labels plus loading/error text.</verification>

<success_criteria>
- `GameRoot` auto-loads the smoke sample and creates a session for `cao_cao`
- `TimeManager` produces a visible Phase 1 time label
- `MainHUD` binds to live session values and renders all required first-screen fields
- Failure and loading states remain inside the HUD instead of producing a blank scene
</success_criteria>

<output>
After completion, create `.planning/phases/01-190/01-190-04-SUMMARY.md`
</output>
