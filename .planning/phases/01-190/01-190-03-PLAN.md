---
phase: 01-190
plan: "03"
type: execute
wave: 2
depends_on:
  - 01-190-01
  - 01-190-02
files_modified:
  - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
  - three-kingdoms-simulator/scripts/data/JsonDefinitionLoader.gd
  - three-kingdoms-simulator/scripts/data/ScenarioRepository.gd
  - three-kingdoms-simulator/scripts/data/definitions/ScenarioDefinition.gd
  - three-kingdoms-simulator/scripts/data/definitions/CharacterDefinition.gd
  - three-kingdoms-simulator/scripts/data/definitions/FactionDefinition.gd
  - three-kingdoms-simulator/scripts/data/definitions/CityDefinition.gd
  - three-kingdoms-simulator/scripts/runtime/GameSession.gd
  - three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - CHAR-01
  - CHAR-02
  - CHAR-04
must_haves:
  truths:
    - Runtime code can load the Phase 1 JSON definitions and query scenario, character, faction, and city by ID.
    - Static definitions and mutable runtime state are represented by separate classes.
    - Identity differences are modeled as fields or permissions on shared objects, not as separate gameplay modes.
  artifacts:
    - three-kingdoms-simulator/scripts/data/ScenarioRepository.gd
    - three-kingdoms-simulator/scripts/runtime/GameSession.gd
    - three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
  key_links:
    - DataRepository -> JsonDefinitionLoader -> res://data/generated/190/*.json
    - GameSession -> protagonist_id and RuntimeCharacterState by stable character ID
---

<objective>
Implement the data-loading and runtime-state contracts that make Phase 1 more than a static shell.

Purpose: Separate immutable scenario definitions from mutable session state before any boot logic or HUD binding is added.
Output: Typed definition models, repository loading code, and runtime session objects built on stable IDs.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-190/01-CONTEXT.md
@.planning/phases/01-190/01-RESEARCH.md
@three-kingdoms-simulator/project.godot
@three-kingdoms-simulator/scripts/autoload/DataRepository.gd
@three-kingdoms-simulator/data/generated/190/index.json
@three-kingdoms-simulator/data/generated/190/scenario_190_smoke.json
@three-kingdoms-simulator/data/generated/190/characters.json
@three-kingdoms-simulator/data/generated/190/factions.json
@three-kingdoms-simulator/data/generated/190/cities.json

<interfaces>
From Plan 01 contract:

```gdscript
func load_phase1_smoke_sample()
func get_scenario(id: String)
func get_character(id: String)
func get_faction(id: String)
func get_city(id: String)
```

Required runtime IDs from Plan 02:

```text
scenario_190_smoke
cao_cao
cao_cao_faction
chenliu
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define typed definition and runtime-state contracts</name>
  <files>three-kingdoms-simulator/scripts/data/definitions/ScenarioDefinition.gd, three-kingdoms-simulator/scripts/data/definitions/CharacterDefinition.gd, three-kingdoms-simulator/scripts/data/definitions/FactionDefinition.gd, three-kingdoms-simulator/scripts/data/definitions/CityDefinition.gd, three-kingdoms-simulator/scripts/runtime/GameSession.gd, three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd</files>
  <read_first>
    - design/数据/Godot 数据结构草案 v1.md
    - .planning/phases/01-190/01-CONTEXT.md
    - three-kingdoms-simulator/data/generated/190/scenario_190_smoke.json
    - three-kingdoms-simulator/data/generated/190/characters.json
  </read_first>
  <action>Per D-03, D-08, and the Phase 1 requirement set, create typed GDScript model files for `ScenarioDefinition`, `CharacterDefinition`, `FactionDefinition`, and `CityDefinition`. Include only the fields needed by Phase 1, but include the exact core fields required by the docs: `ScenarioDefinition` must include `id`, `name`, `start_year`, `start_month`, `start_xun`, `title`, `description`, `city_ids`, `faction_ids`, `character_ids`; `CharacterDefinition` must include `id`, `name`, `identity_type`, `faction_id`, `city_id`, `office_id`, `family_id`, `clan_id`, `personality_tags`, `stats`, `status_values`, `reputation_values`, and a shared-rule permissions field such as `available_action_tags` or `permission_tags`; `FactionDefinition` must include `id`, `name`, `ruler_id`, `capital_city_id`, `city_ids`, `officer_ids`, and `resources`; `CityDefinition` must include `id`, `name`, `region`, `owner_faction_id`, `governor_id`, `character_ids`, and `values`. Then create `GameSession.gd` and `RuntimeCharacterState.gd` as runtime-only classes. `GameSession` must contain `schema_version = 1`, `scenario_id`, `current_year`, `current_month`, `current_xun`, `protagonist_id`, and a character-state dictionary keyed by character ID. `RuntimeCharacterState` must contain mutable AP, energy, stress, fame, merit, loyalty, honor/infamy-style values, current city, and flags. Do not let runtime classes inherit from or overwrite definition classes.</action>
  <acceptance_criteria>
    - `CharacterDefinition.gd` contains `identity_type`, `status_values`, `reputation_values`, and a permissions field
    - `GameSession.gd` contains `schema_version`, `scenario_id`, `protagonist_id`, and time fields
    - `RuntimeCharacterState.gd` contains mutable AP, energy, stress, fame, merit, loyalty, and current-city fields
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$a = Select-String -Path 'three-kingdoms-simulator/scripts/data/definitions/CharacterDefinition.gd' -Pattern 'identity_type|status_values|reputation_values|permission_tags|available_action_tags' -Quiet; $b = Select-String -Path 'three-kingdoms-simulator/scripts/runtime/GameSession.gd' -Pattern 'schema_version|scenario_id|protagonist_id|current_year|current_month|current_xun' -Quiet; $c = Select-String -Path 'three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd' -Pattern 'ap|energy|stress|fame|merit|loyalty|current_city' -Quiet; if ($a -and $b -and $c) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>Static definitions and runtime state are represented by separate typed scripts with the exact Phase 1 fields needed for loading and HUD display.</done>
</task>

<task type="auto">
  <name>Task 2: Implement JSON loading, ID queries, and session bootstrap without mutating definitions</name>
  <files>three-kingdoms-simulator/scripts/autoload/DataRepository.gd, three-kingdoms-simulator/scripts/data/JsonDefinitionLoader.gd, three-kingdoms-simulator/scripts/data/ScenarioRepository.gd, three-kingdoms-simulator/scripts/runtime/GameSession.gd, three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd</files>
  <read_first>
    - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
    - three-kingdoms-simulator/data/generated/190/index.json
    - three-kingdoms-simulator/data/generated/190/scenario_190_smoke.json
    - three-kingdoms-simulator/data/generated/190/characters.json
    - three-kingdoms-simulator/data/generated/190/factions.json
    - three-kingdoms-simulator/data/generated/190/cities.json
  </read_first>
  <action>Implement a thin JSON-loading path for Phase 1. `JsonDefinitionLoader.gd` must read `res://data/generated/190/index.json` and load the scenario, character, faction, and city JSON files with Godot JSON parsing. `ScenarioRepository.gd` must normalize them into dictionaries keyed by stable string ID. Update `DataRepository.gd` so `load_phase1_smoke_sample()` loads the `scenario_190_smoke` dataset exactly once, caches dictionaries for scenario/characters/factions/cities, and exposes working `get_scenario`, `get_character`, `get_faction`, and `get_city` methods that return by ID. Add a repository method `bootstrap_session(scenario_id: String, protagonist_id: String) -> GameSession` that copies mutable status values from the chosen `CharacterDefinition` into `RuntimeCharacterState`, copies the scenario start year/month/xun into `GameSession`, and keeps the definition cache immutable. Do not write back into JSON-derived definition objects. Keep identity handling in shared data fields (`identity_type` plus permission/action tags), not separate branches or classes per identity.</action>
  <acceptance_criteria>
    - `DataRepository.gd` contains working methods `load_phase1_smoke_sample`, `get_scenario`, `get_character`, `get_faction`, `get_city`, and `bootstrap_session`
    - `JsonDefinitionLoader.gd` contains `index.json` and `JSON`
    - `ScenarioRepository.gd` contains dictionary caches keyed by `id`
    - `GameSession.gd` is used as the return type for runtime bootstrapping instead of returning raw definition dictionaries
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$a = Select-String -Path 'three-kingdoms-simulator/scripts/autoload/DataRepository.gd' -Pattern 'load_phase1_smoke_sample|get_scenario|get_character|get_faction|get_city|bootstrap_session' -Quiet; $b = Select-String -Path 'three-kingdoms-simulator/scripts/data/JsonDefinitionLoader.gd' -Pattern 'index.json|JSON' -Quiet; $c = Select-String -Path 'three-kingdoms-simulator/scripts/data/ScenarioRepository.gd' -Pattern 'Dictionary|id' -Quiet; $d = Select-String -Path 'three-kingdoms-simulator/scripts/autoload/DataRepository.gd' -Pattern 'GameSession' -Quiet; if ($a -and $b -and $c -and $d) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>The repository can load the Phase 1 sample and produce a separate runtime session object without mutating source definitions.</done>
</task>

</tasks>

<verification>
Confirm the runtime code now separates immutable definitions from mutable session state and exposes ID-based queries for all four core Phase 1 object types.</verification>

<success_criteria>
- The loader targets the checked-in JSON files under `res://data/generated/190/`
- `DataRepository` exposes all required query methods and a session bootstrap method
- Runtime state is modeled separately from definition objects
- Identity variation remains a shared-rule data field, not a mode fork
</success_criteria>

<output>
After completion, create `.planning/phases/01-190/01-190-03-SUMMARY.md`
</output>
