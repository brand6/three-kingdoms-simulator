---
phase: 01-190
plan: "01"
subsystem: ui
tags: [godot, gdscript, theme, autoload, main-scene, hud]
requires: []
provides:
  - MainScene Godot entry shell for the 190 prototype
  - Shared PrototypeTheme tokens for Phase 1 HUD controls
  - Autoload manager contracts for GameRoot, DataRepository, and TimeManager
affects: [phase-1-shell, phase-1-data-loading, phase-1-hud-binding]
tech-stack:
  added: [Godot Theme resource, typed GDScript autoload skeletons]
  patterns: [single MainScene HUD shell, project-level shared theme, autoload manager contracts]
key-files:
  created:
    - three-kingdoms-simulator/scenes/main/MainScene.tscn
    - three-kingdoms-simulator/themes/PrototypeTheme.tres
    - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
    - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
  modified:
    - three-kingdoms-simulator/project.godot
key-decisions:
  - "Kept the Godot runtime rooted in three-kingdoms-simulator and pointed run/main_scene at MainScene."
  - "Encoded Phase 1 HUD spacing, color, and disabled-navigation behavior in one shared Theme resource instead of node-local overrides."
  - "Registered GameRoot, DataRepository, and TimeManager as typed autoload contracts while leaving gameplay and data loading for later plans."
patterns-established:
  - "MainScene owns the full-screen HUD shell with top, middle, and bottom container bands."
  - "Future systems should wire through fixed autoload method names instead of inventing new bootstrap APIs."
requirements-completed: [CORE-01, CORE-02]
duration: 6.5min
completed: 2026-04-05
---

# Phase 1 Plan 1: 建立 Godot 入口壳、HUD 骨架与管理器契约 Summary

**Godot now boots directly into a themed Phase 1 HUD shell with fixed autoload manager contracts for later data and runtime wiring.**

## Performance

- **Duration:** 6.5 min
- **Started:** 2026-04-05T04:29:55Z
- **Completed:** 2026-04-05T04:36:23Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Set `project.godot` to launch straight into `res://scenes/main/MainScene.tscn`.
- Built the approved Phase 1 HUD shell with top status bar, left overview, center summary, right context, and disabled bottom navigation.
- Added typed autoload skeletons for `GameRoot`, `DataRepository`, and `TimeManager` with the exact contract methods required by downstream plans.

## Task Commits

Each task was committed atomically:

1. **Task 1: Build the approved MainScene and shared Phase 1 theme** - `2c6bbcc` (feat)
2. **Task 2: Register autoload manager contracts without implementing gameplay logic yet** - `85b5b10` (feat)

**Plan metadata:** pending

## Files Created/Modified
- `three-kingdoms-simulator/project.godot` - Sets the main scene entrypoint and registers Phase 1 autoload managers.
- `three-kingdoms-simulator/scenes/main/MainScene.tscn` - Defines the full-screen HUD shell using the approved container hierarchy.
- `three-kingdoms-simulator/themes/PrototypeTheme.tres` - Stores Phase 1 color, typography, and disabled button theme tokens.
- `three-kingdoms-simulator/scripts/autoload/GameRoot.gd` - Provides boot and error contract methods for later runtime wiring.
- `three-kingdoms-simulator/scripts/autoload/DataRepository.gd` - Provides smoke-sample load and ID query contract methods.
- `three-kingdoms-simulator/scripts/autoload/TimeManager.gd` - Provides time initialization and current-label contract methods.

## Decisions Made
- Kept the existing `three-kingdoms-simulator/` subproject as the only Godot runtime root, matching the phase context and roadmap.
- Used a single `Theme` resource to centralize HUD visual rules and disabled navigation styling instead of local control overrides.
- Left manager behavior intentionally skeletal so later plans can add real loading and runtime state without changing public method names.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The shell command form in the plan's PowerShell verifier did not execute cleanly in this environment, so verification was rerun with equivalent PowerShell expressions through the executor shell.

## User Setup Required

None - no external service configuration required.

## Known Stubs

- `three-kingdoms-simulator/scenes/main/MainScene.tscn` - static `头像占位` card is an intentional non-interactive portrait stub for Phase 1 shell validation.
- `three-kingdoms-simulator/scenes/main/MainScene.tscn` - top-bar and left-panel values currently render `—` until Plan 04 binds live data.
- `three-kingdoms-simulator/scripts/autoload/DataRepository.gd` - `get_scenario`, `get_character`, `get_faction`, and `get_city` return `null` as contract placeholders until data loading lands in later Phase 1 plans.
- `three-kingdoms-simulator/scripts/autoload/GameRoot.gd` - `bootstrap_default_entry()` is a contract-only placeholder until default protagonist bootstrapping is implemented.

## Next Phase Readiness
- Main scene, theme, and autoload anchors are stable for the Luban/JSON pipeline and repository loading plans.
- Downstream plans can target fixed UI nodes and manager method names without revisiting boot architecture.

## Self-Check: PASSED

- Verified `.planning/phases/01-190/01-190-01-SUMMARY.md` exists.
- Verified task commits `2c6bbcc` and `85b5b10` exist in git history.
