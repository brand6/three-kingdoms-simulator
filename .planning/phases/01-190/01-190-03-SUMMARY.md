---
phase: 01-190
plan: "03"
subsystem: api
tags: [godot, gdscript, json-loader, repository, runtime-state]
requires:
  - phase: 01-190
    provides: [generated phase 1 JSON definitions and stable IDs]
provides:
  - Typed definition models for Scenario Character Faction and City
  - JSON loader and ID-keyed repository cache
  - Runtime GameSession bootstrap separate from immutable definitions
affects: [phase-1-default-boot, phase-1-hud-binding]
tech-stack:
  added: [typed GDScript data models, JSON parsing loader, runtime session classes]
  patterns: [definition-runtime separation, ID-keyed repository lookup, session bootstrap from definitions]
key-files:
  created:
    - three-kingdoms-simulator/scripts/data/definitions/ScenarioDefinition.gd
    - three-kingdoms-simulator/scripts/data/definitions/CharacterDefinition.gd
    - three-kingdoms-simulator/scripts/data/definitions/FactionDefinition.gd
    - three-kingdoms-simulator/scripts/data/definitions/CityDefinition.gd
    - three-kingdoms-simulator/scripts/data/JsonDefinitionLoader.gd
    - three-kingdoms-simulator/scripts/data/ScenarioRepository.gd
    - three-kingdoms-simulator/scripts/runtime/GameSession.gd
    - three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
  modified:
    - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
key-decisions:
  - "Used RefCounted typed models for definitions and runtime state to keep data lightweight and independent from scene ownership."
  - "Made DataRepository a thin facade over JsonDefinitionLoader and ScenarioRepository instead of mutating raw JSON dictionaries in place."
  - "Bootstrapped GameSession by copying status and reputation values into RuntimeCharacterState so immutable definitions stay untouched."
patterns-established:
  - "Definition queries return typed objects keyed by stable string IDs."
  - "Runtime state is created by explicit copy from definition dictionaries, never by sharing mutable references."
requirements-completed: [DATA-01, DATA-02, DATA-03, DATA-04, CHAR-01, CHAR-02, CHAR-04]
duration: 7.6min
completed: 2026-04-05
---

# Phase 1 Plan 3: 实现定义加载、ID 查询与 Definition/Runtime 分离 Summary

**The Godot runtime now loads Phase 1 JSON definitions into typed repositories and bootstraps a separate mutable GameSession keyed by stable IDs.**

## Performance

- **Duration:** 7.6 min
- **Started:** 2026-04-05T04:40:00Z
- **Completed:** 2026-04-05T04:47:34Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- Added typed GDScript definitions for scenario, character, faction, and city data.
- Added `JsonDefinitionLoader` and `ScenarioRepository` to normalize checked-in JSON files into ID-keyed caches.
- Upgraded `DataRepository` to expose real load/query methods and a `bootstrap_session()` path that copies mutable runtime state safely.

## Task Commits

Each task was committed atomically:

1. **Task 1: Define typed definition and runtime-state contracts** - `9f6c0cb` (feat)
2. **Task 2: Implement JSON loading, ID queries, and session bootstrap without mutating definitions** - `24047b5` (feat)

**Plan metadata:** pending

## Files Created/Modified
- `three-kingdoms-simulator/scripts/data/definitions/*.gd` - Typed immutable models for Phase 1 definition objects.
- `three-kingdoms-simulator/scripts/runtime/GameSession.gd` - Runtime session container with schema version and ID-keyed state map.
- `three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd` - Mutable protagonist state copied from definition values.
- `three-kingdoms-simulator/scripts/data/JsonDefinitionLoader.gd` - Reads `index.json` and related JSON files from `res://data/generated/190/`.
- `three-kingdoms-simulator/scripts/data/ScenarioRepository.gd` - Stores typed caches keyed by `id`.
- `three-kingdoms-simulator/scripts/autoload/DataRepository.gd` - Public repository facade for loading, lookup, and session bootstrap.

## Decisions Made
- Used `permission_tags` as the shared-rule identity expansion field instead of mode-specific classes.
- Kept the repository cache internal and returned typed objects through fixed autoload methods.
- Copied mutable values with `duplicate(true)` before constructing runtime state to avoid definition mutation bugs.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Known Stubs

- `three-kingdoms-simulator/scripts/autoload/DataRepository.gd` - bootstraps only the protagonist runtime state for Phase 1; non-protagonist mutable state remains deferred until later gameplay plans.

## Next Phase Readiness
- `GameRoot` can now bootstrap a concrete session from the smoke sample.
- HUD binding can safely read time, character, faction, and city data from typed repository/session objects.

## Self-Check: PASSED

- Verified `.planning/phases/01-190/01-190-03-SUMMARY.md` exists.
- Verified task commits `9f6c0cb` and `24047b5` exist in git history.
