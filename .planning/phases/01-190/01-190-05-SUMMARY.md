---
phase: 01-190
plan: "05"
subsystem: ui
tags: [godot, gdscript, hud, regression, time-label]
requires:
  - phase: 01-190
    provides: [main hud binding, default boot flow, phase 1 top-bar shell]
provides:
  - Value-only top-bar time rendering that uses TimeManager's formatted label directly
  - Main scene TimeLabel placeholder aligned with loading and error HUD states
  - Headless cold-start regression covering the merged duplicated-time UAT defect
affects: [phase-1-uat-closure, phase-2-hud-extensions]
tech-stack:
  added: [headless scene regression script]
  patterns: [value-only time label contract, cold-start ui regression checks]
key-files:
  created:
    - three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd
  modified:
    - three-kingdoms-simulator/scripts/ui/MainHUD.gd
    - three-kingdoms-simulator/scenes/main/MainScene.tscn
key-decisions:
  - "TimeLabel remains always visible but is treated as a value-only field instead of a label-value pair."
  - "The duplicated-year UAT reports are closed with one shared regression that validates the full top bar after a true cold start."
patterns-established:
  - "MainHUD reads formatted time directly from TimeManager for success state and uses an em dash placeholder for empty/error states."
  - "Phase 1 UI regressions can boot the real MainScene headlessly and assert exact top-bar text without scene-specific mocks."
requirements-completed: [CORE-02]
duration: 4min
completed: 2026-04-05
---

# Phase 1 Plan 5: 修复顶栏重复年份缺陷 Summary

**The Phase 1 HUD now shows a single `190年 / 1月 / 第1旬` time value on cold start, with the other top-bar fields unchanged and protected by a headless regression.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-05T14:18:31+08:00
- **Completed:** 2026-04-05T14:22:15+08:00
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Removed the duplicated time-prefix rendering path from `MainHUD.gd` and kept the time field on a value-only contract.
- Aligned `MainScene.tscn` with the repaired contract by changing the default `TimeLabel` text to `—`.
- Added a headless cold-start regression that proves the top bar renders the exact expected time, city, identity, faction, and office values.

## Task Commits

Each task was committed atomically:

1. **Task 1: Normalize the HUD time field to a value-only rendering contract** - `283346e` (fix)
2. **Task 2: Align the scene default TimeLabel with the repaired HUD contract** - `c8a66a0` (test)

**Plan metadata:** pending

## Files Created/Modified
- `three-kingdoms-simulator/scripts/ui/MainHUD.gd` - Renders the time field directly from `TimeManager.get_current_label()` and keeps empty/error states value-only.
- `three-kingdoms-simulator/scenes/main/MainScene.tscn` - Seeds `TimeLabel` with an em dash instead of the misleading template text.
- `three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd` - Boots the real scene headlessly and asserts the repaired top-bar values.

## Decisions Made
- Kept `TimeManager.gd` unchanged because the formatter already returned the correct final display string.
- Fixed both UAT reports as one defect because they shared the same rendering root cause in `MainHUD.gd`.
- Treated cold-start verification as a runtime regression, not a static string check, so future HUD refactors must still satisfy the exact top-bar output.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Used the installed Godot console binary directly for regression execution**
- **Found during:** Task 2 (Align the scene default TimeLabel with the repaired HUD contract)
- **Issue:** The plan's generic `Get-Command godot` lookup failed even though Godot 4.6.1 was installed outside PATH, which blocked the required runtime regression.
- **Fix:** Located the installed console executable under `D:\Godot\Godot_v4.6.1-stable_mono_win64\Godot_v4.6.1-stable_mono_win64_console.exe` and ran the headless regression with that explicit binary.
- **Files modified:** None
- **Verification:** The headless script completed successfully with the explicit Godot executable.
- **Committed in:** c8a66a0 (part of task verification)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** No scope change. The deviation only ensured the required runtime regression could execute in this environment.

## Issues Encountered
- The repository had many unrelated local modifications and untracked files outside this plan. I staged and committed only the three plan files.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 1's remaining UAT gap is closed with a runtime guard against duplicated time prefixes.
- Later HUD changes can reuse the regression script to protect the top bar while Phase 2 expands gameplay surfaces.

## Self-Check: PASSED

- Verified `.planning/phases/01-190/01-190-05-SUMMARY.md` exists.
- Verified task commits `283346e` and `c8a66a0` exist in git history.
