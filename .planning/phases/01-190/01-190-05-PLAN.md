---
phase: 01-190
plan: "05"
type: execute
wave: 4
depends_on:
  - 01-190-04
files_modified:
  - three-kingdoms-simulator/scripts/ui/MainHUD.gd
  - three-kingdoms-simulator/scenes/main/MainScene.tscn
  - three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd
autonomous: true
requirements:
  - CORE-02
gap_closure: true
must_haves:
  truths:
    - Cold start HUD top bar shows exactly one time string, `190年 / 1月 / 第1旬`, instead of a duplicated prefix plus value.
    - Current city, identity, faction, and office remain unchanged while the time field alone follows the value-only rendering contract.
    - Loading, error, and empty HUD states never render `190年 / 月 / 旬：...` or any equivalent duplicate time prefix.
  artifacts:
    - path: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      provides: TimeLabel rendering that uses the formatted TimeManager value directly in success and empty states.
    - path: three-kingdoms-simulator/scenes/main/MainScene.tscn
      provides: TimeLabel default text aligned with the HUD's value-only time contract.
    - path: three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd
      provides: Headless cold-start regression that asserts the repaired top bar text at runtime.
  key_links:
    - from: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      to: three-kingdoms-simulator/scripts/autoload/TimeManager.gd
      via: direct TimeLabel assignment from `get_current_label()`
      pattern: _time_label\.text\s*=\s*_time_manager\(\)\.call\("get_current_label"\)
    - from: three-kingdoms-simulator/scenes/main/MainScene.tscn
      to: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      via: shared value-only TimeLabel default and empty-state contract
      pattern: TimeLabel
    - from: three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd
      to: three-kingdoms-simulator/scenes/main/MainScene.tscn
      via: headless scene load and label assertions after bootstrap
      pattern: 190年 / 1月 / 第1旬
---

<objective>
Fix the single top-bar time rendering defect that caused both diagnosed Phase 1 UAT issues.

Purpose: Close the duplicated-year display gap with one minimal repair instead of planning two separate fixes for the same root cause.
Output: MainHUD renders one formatted time value, and MainScene's default TimeLabel no longer suggests a label-plus-value pattern.</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-190/01-UAT.md
@.planning/debug/uat2-topbar-double-year.md
@.planning/phases/01-190/01-CONTEXT.md
@.planning/phases/01-190/01-190-04-SUMMARY.md
@three-kingdoms-simulator/scripts/ui/MainHUD.gd
@three-kingdoms-simulator/scripts/autoload/TimeManager.gd
@three-kingdoms-simulator/scenes/main/MainScene.tscn

<interfaces>
From `three-kingdoms-simulator/scripts/autoload/TimeManager.gd`:

```gdscript
func get_current_label() -> String:
	if _current_year <= 0 or _current_month <= 0 or _current_xun <= 0:
		return "—"
	return "%d年 / %d月 / 第%d旬" % [_current_year, _current_month, _current_xun]
```

From `three-kingdoms-simulator/scripts/ui/MainHUD.gd`:

```gdscript
func _pair_text(label_text: String, value: Variant) -> String
func _display_value(value: Variant) -> String
```

Use `_pair_text()` only for true key-value HUD fields. `TimeLabel` is a value-only field and must display the already-formatted string from `TimeManager` directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the HUD time field to a value-only rendering contract</name>
  <files>three-kingdoms-simulator/scripts/ui/MainHUD.gd</files>
  <action>Use the diagnosed root cause from UAT tests 1 and 2 as one shared fix. Per D-05, keep the time field always visible, but stop treating `TimeLabel` like the other `字段名：值` labels. In `show_success_state()`, assign `_time_label.text` directly from `TimeManager.get_current_label()` instead of wrapping it with `_pair_text()`. In `_render_empty_fields()` and any other non-success path that currently reuses the time template, render a value-only placeholder via `_display_value(null)` or an equivalent helper so loading/error states also avoid duplicated prefixes. Do not change `TimeManager.gd`, and do not alter the existing key-value behavior for city, identity, faction, office, AP, energy, stress, fame, or merit.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$direct = Select-String -Path 'three-kingdoms-simulator/scripts/ui/MainHUD.gd' -Pattern '_time_label\.text\s*=\s*_time_manager\(\)\.call\("get_current_label"\)' -Quiet; $legacy = Select-String -Path 'three-kingdoms-simulator/scripts/ui/MainHUD.gd' -Pattern '_pair_text\("190年 / 月 / 旬"' -Quiet; $others = Select-String -Path 'three-kingdoms-simulator/scripts/ui/MainHUD.gd' -Pattern '_city_label\.text = _pair_text\("当前城市"|_identity_label\.text = _pair_text\("当前身份"|_faction_label\.text = _pair_text\("所属势力"|_office_label\.text = _pair_text\("当前官职"' -Quiet; if ($direct -and -not $legacy -and $others) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>`MainHUD.gd` renders one formatted time value in success state and no longer contains the duplicated-prefix pattern that caused both UAT failures.</done>
</task>

<task type="auto">
  <name>Task 2: Align the scene default TimeLabel with the repaired HUD contract</name>
  <files>three-kingdoms-simulator/scenes/main/MainScene.tscn, three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd</files>
  <action>Update `MainScene.tscn` so `TimeLabel` no longer starts with the template-like text `190年 / 月 / 旬`, which implied a label prefix and reinforced the defect. Make the default text a value-only placeholder that matches the repaired HUD contract (for example `—`) so cold start, loading, and error states remain semantically consistent before live session data arrives. In the same task, add `three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd` as a headless regression harness that performs a true cold-start check for both diagnosed UAT issues at once: load the real `MainScene`, allow the normal autoload bootstrap path from Plan 04 to run, then assert the top bar values are exactly `190年 / 1月 / 第1旬`, `当前城市：陈留`, `当前身份：ruler`, `所属势力：曹操集团`, and `当前官职：lord`. The regression script must fail if the time label contains the old duplicated-prefix pattern, if the exact time text differs, or if any of the other top-bar fields regress. Keep this as one shared regression for the merged UAT defect, not two separate tests.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$sceneOk = Select-String -Path 'three-kingdoms-simulator/scenes/main/MainScene.tscn' -Pattern 'text = "—"' -Quiet; $legacyScene = Select-String -Path 'three-kingdoms-simulator/scenes/main/MainScene.tscn' -Pattern 'text = "190年 / 月 / 旬"' -Quiet; $testScript = Test-Path 'three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd'; if (-not ($sceneOk -and -not $legacyScene -and $testScript)) { exit 1 }; $godot = (Get-Command godot, godot4, Godot* -ErrorAction SilentlyContinue | Select-Object -First 1); if (-not $godot) { throw 'Godot CLI not found for runtime regression' }; & $godot.Source --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase1_topbar_time_regression.gd'; exit $LASTEXITCODE"</automated>
  </verify>
  <done>`MainScene.tscn` no longer encodes the misleading time template, and a headless cold-start regression proves the repaired HUD shows exactly one `190年 / 1月 / 第1旬` while the other top-bar fields stay correct.</done>
</task>

</tasks>

<verification>
Run both automated checks above. The final gate is the headless cold-start regression script, which must boot the real scene and prove the top bar shows exactly one `190年 / 1月 / 第1旬` plus unchanged city/identity/faction/office values.</verification>

<success_criteria>
- The two diagnosed UAT issues are closed by one shared fix rather than duplicate work.
- `MainHUD.gd` assigns the top-bar time directly from `TimeManager.get_current_label()`.
- `MainScene.tscn` no longer seeds `TimeLabel` with `190年 / 月 / 旬`.
- A cold-start headless regression script fails on duplicate time prefixes and passes only when the runtime HUD shows the expected top-bar values.
- The rest of the top bar keeps the existing key-value label format and displayed data unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-190/01-190-05-SUMMARY.md`
</output>
