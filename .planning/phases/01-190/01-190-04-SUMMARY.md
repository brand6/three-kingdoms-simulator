---
phase: 01-190
plan: "04"
subsystem: ui
tags: [godot, gdscript, hud, boot-flow, autoload]
requires:
  - phase: 01-190
    provides: [main scene shell, shared theme, repository loading, runtime session bootstrap]
provides:
  - Automatic boot into scenario_190_smoke as cao_cao
  - Formatted time label and boot error routing through GameRoot
  - MainHUD session binding for Phase 1 loading success and failure states
affects: [phase-1-completion, phase-2-hud-extensions]
tech-stack:
  added: [MainHUD scene script]
  patterns: [autoload-driven boot flow, HUD-owned state rendering, in-frame error handling]
key-files:
  created:
    - three-kingdoms-simulator/scripts/ui/MainHUD.gd
  modified:
    - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
    - three-kingdoms-simulator/scenes/main/MainScene.tscn
key-decisions:
  - "Boot flow remains fixed to scenario_190_smoke plus cao_cao with no selection UI in Phase 1."
  - "MainHUD owns loading success and error rendering inside the approved scene shell instead of swapping scenes or opening modal placeholders."
  - "Missing values render as em dashes so the HUD never exposes null raw keys or debugy placeholders."
patterns-established:
  - "HUD boots itself by registering with GameRoot and triggering deferred bootstrap once the scene is ready."
  - "GameRoot keeps the current session and last boot error as shared autoload state for later systems."
requirements-completed: [CORE-01, CORE-02, CHAR-01, CHAR-02, CHAR-04]
duration: 7.6min
completed: 2026-04-05
---

# Phase 1 Plan 4: 接通默认主角开局与主 HUD 实时绑定 Summary

**The project now boots directly into the 190 smoke sample as Cao Cao and renders live time, location, identity, faction, office, AP, energy, stress, fame, and merit inside the Phase 1 HUD.**

## Performance

- **Duration:** 7.6 min
- **Started:** 2026-04-05T04:40:00Z
- **Completed:** 2026-04-05T04:47:34Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Implemented the concrete default boot path for `scenario_190_smoke` and protagonist `cao_cao`.
- Updated `TimeManager` to emit the visible Phase 1 label format `190年 / {month}月 / 第{xun}旬`.
- Attached `MainHUD.gd` to the main scene and bound live repository/session values into the approved HUD layout, including in-frame loading and error states.

## Task Commits

Each task was committed atomically:

1. **Task 1: Implement the default 190 protagonist boot path** - `bb758d0` (feat)
2. **Task 2: Bind the MainHUD to live session data and render loading/success/error states** - `eca97a0` (feat)

**Plan metadata:** pending

## Files Created/Modified
- `three-kingdoms-simulator/scripts/autoload/GameRoot.gd` - Boots the smoke sample, stores the current session, and routes failures to the HUD.
- `three-kingdoms-simulator/scripts/autoload/TimeManager.gd` - Formats the always-visible Phase 1 time label.
- `three-kingdoms-simulator/scripts/ui/MainHUD.gd` - Renders loading, success, and failure states plus the required first-screen fields.
- `three-kingdoms-simulator/scenes/main/MainScene.tscn` - Attaches the HUD script to the approved scene shell.

## Decisions Made
- Kept the boot path single-character and non-interactive to match D-01, D-02, and D-07.
- Used autoload coordination rather than scene swapping so the HUD frame stays visible during loading and failures.
- Reused exact UI-SPEC copy for empty state and error guidance so the shell remains verifier-friendly.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Known Stubs

- `three-kingdoms-simulator/scenes/main/MainScene.tscn` - portrait remains a deliberate Phase 1 placeholder card because no image asset pipeline exists yet.
- `three-kingdoms-simulator/scripts/ui/MainHUD.gd` - right-column modules still use Phase 1 empty-state copy rather than task/event data because those systems land in later phases.

## Next Phase Readiness
- Phase 1 core entry loop is now complete and visible on first boot.
- Phase 2 can extend the same HUD with action panels and relation views without reworking the entry architecture.

## Self-Check: PASSED

- Verified `.planning/phases/01-190/01-190-04-SUMMARY.md` exists.
- Verified task commits `bb758d0` and `eca97a0` exist in git history.
