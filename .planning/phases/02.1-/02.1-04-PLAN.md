---
phase: 02.1-office-and-task-deployment
plan: "04"
type: execute
wave: 2
depends_on: ["02.1-01", "02.1-03"]
files_modified:
  - three-kingdoms-simulator/scripts/data/definitions/ScenarioDefinition.gd
  - three-kingdoms-simulator/scripts/runtime/GameSession.gd
  - three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
  - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
  - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
  - three-kingdoms-simulator/scripts/tests/phase21_bootstrap_month_gate_regression.gd
autonomous: true
requirements: [CARE-03]
must_haves:
  truths:
    - "新开局会以荀彧进入，而不是曹操。"
    - "新月初始化后，系统已持有候选任务与月度 career/task runtime state。"
    - "未锁定主任务时，本月行动入口会被系统层拦住。"
  artifacts:
    - path: three-kingdoms-simulator/scripts/autoload/DataRepository.gd
      provides: Phase 2.1 resource loading and patch lookup
    - path: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      provides: bootstrap and month gating orchestration
    - path: three-kingdoms-simulator/scripts/tests/phase21_bootstrap_month_gate_regression.gd
      provides: headless bootstrap regression
  key_links:
    - from: three-kingdoms-simulator/scripts/autoload/DataRepository.gd
      to: three-kingdoms-simulator/data/scenario_patches/xunyu_default_start_patch.tres
      via: patch load during bootstrap
      pattern: xunyu_default_start_patch
    - from: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      to: three-kingdoms-simulator/scripts/runtime/MonthlyTaskState.gd
      via: current month gate state
      pattern: current_month_task
---

<objective>
Load the new Phase 2.1 data and make bootstrap/month gating real.

Purpose: Move Phase 2.1 from document-only definitions into the live session bootstrap path with deterministic Xun Yu start and pre-action month gating.
Output: DataRepository loaders, extended session/runtime state, GameRoot month-start bootstrap, and a headless regression proving the gate.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-/02.1-CONTEXT.md
@.planning/phases/02.1-/02.1-RESEARCH.md
@.planning/phases/02-旬内行动—关系闭环/02-04-SUMMARY.md
@three-kingdoms-simulator/scripts/autoload/GameRoot.gd
@three-kingdoms-simulator/scripts/autoload/DataRepository.gd
@three-kingdoms-simulator/scripts/runtime/GameSession.gd

<interfaces>
From `GameRoot.gd` today:

```gdscript
const DEFAULT_SCENARIO_ID := "scenario_190_smoke"
const DEFAULT_PROTAGONIST_ID := "cao_cao"

func bootstrap_default_entry() -> void
func execute_phase2_action(action_id: String, target_character_id: String = "") -> Variant
func end_current_xun() -> Variant
```

From `DataRepository.gd` today:

```gdscript
func load_phase1_smoke_sample() -> void
func bootstrap_session(scenario_id: String, protagonist_id: String) -> GameSession
```

Extend these exact entry points; do not replace them with a new bootstrap subsystem.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend repository and session contracts to hold Phase 2.1 data and runtime state</name>
  <files>
    three-kingdoms-simulator/scripts/data/definitions/ScenarioDefinition.gd
    three-kingdoms-simulator/scripts/runtime/GameSession.gd
    three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
    three-kingdoms-simulator/scripts/autoload/DataRepository.gd
  </files>
  <read_first>
    three-kingdoms-simulator/scripts/data/definitions/ScenarioDefinition.gd
    three-kingdoms-simulator/scripts/runtime/GameSession.gd
    three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
    three-kingdoms-simulator/scripts/autoload/DataRepository.gd
    .planning/phases/02.1-/02.1-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: DataRepository can load offices, task templates, task-pool rules, promotion rules, and the Xun Yu setup patch by fixed ID.
    - Test 2: GameSession exposes `player_career_state`, `current_month_task`, `pending_month_task_candidates`, and `last_month_evaluation` after bootstrap.
    - Test 3: RuntimeCharacterState carries `trust` so month-end rewards can write directly to current runtime values.
  </behavior>
  <action>Extend `ScenarioDefinition.gd` only where needed to cache Phase 2.1 scenario-level availability fields without breaking existing JSON parsing. Extend `RuntimeCharacterState.gd` with `trust` and initialize it from either patch data or a safe fallback. Extend `GameSession.gd` with typed fields for `player_career_state`, `current_month_task`, `pending_month_task_candidates`, `last_month_evaluation`, `month_action_locked`, and a vacancy-state dictionary used by promotion checks. Update `DataRepository.gd` to load `.tres` resources from `res://data/offices`, `res://data/office_rules`, `res://data/tasks`, `res://data/task_rules`, and `res://data/scenario_patches`, expose fixed-ID getters for each new type, and apply the Xun Yu patch during `bootstrap_session(...)`. Preserve existing JSON scenario loading; do not migrate phase-1 sample data to Resource in this plan.</action>
  <verify>
    <automated>godot --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase21_bootstrap_month_gate_regression.gd"</automated>
  </verify>
  <acceptance_criteria>
    - `DataRepository.gd` contains getters for office, task template, task pool rule, promotion rule, and setup patch
    - `GameSession.gd` contains `player_career_state`, `current_month_task`, `pending_month_task_candidates`, and `month_action_locked`
    - `RuntimeCharacterState.gd` contains `trust`
  </acceptance_criteria>
  <done>Repository and session bootstrap can now carry all Phase 2.1 data/state needed before monthly logic starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch default entry to Xun Yu and enforce month-start action gating</name>
  <files>
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    three-kingdoms-simulator/scripts/tests/phase21_bootstrap_month_gate_regression.gd
  </files>
  <read_first>
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd
    .planning/phases/02.1-/02.1-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: boot protagonist is `xun_yu`, current office is `office_congshi`, and start merit/fame/trust match patch values.
    - Test 2: month start immediately populates 2–3 candidate tasks and sets `month_action_locked = true` until one is selected.
    - Test 3: trying to execute a phase-2 action before monthly task selection returns a structured blocked result instead of mutating AP/progress.
  </behavior>
  <action>Update `GameRoot.gd` so `bootstrap_default_entry()` reads the start patch instead of hardcoding `cao_cao`, initializes the Phase 2.1 month-start state, and calls a new month-start helper that generates candidate tasks and sets `month_action_locked = true` per D-01 and D-03. Add or extend a headless regression `phase21_bootstrap_month_gate_regression.gd` that instantiates `MainScene`, waits for bootstrap, asserts protagonist/office/merit/fame/trust values, asserts candidate-count range `2..3`, and proves action execution is blocked before task selection. Keep the old Phase 2 action APIs intact; layer the gate in front of them instead of rewriting the resolver.</action>
  <verify>
    <automated>godot --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase21_bootstrap_month_gate_regression.gd"</automated>
  </verify>
  <acceptance_criteria>
    - `GameRoot.gd` no longer hardcodes `DEFAULT_PROTAGONIST_ID := "cao_cao"` as the effective Phase 2.1 boot protagonist
    - regression exits `0`
    - regression asserts blocked action behavior before task selection
  </acceptance_criteria>
  <done>New games boot as 荀彧 and the system blocks monthly actions until one main task is selected.</done>
</task>

</tasks>

<verification>
Running the bootstrap regression proves Phase 2.1 data loads, the session contains month-state contracts, and month-start gating happens before any action execution.
</verification>

<success_criteria>
- Phase 2.1 data loads from Resources through DataRepository.
- Default entry uses the Xun Yu patch instead of the old Cao Cao constant.
- The session enforces “pick one monthly task before acting.”
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-/02.1-04-SUMMARY.md`
</output>
