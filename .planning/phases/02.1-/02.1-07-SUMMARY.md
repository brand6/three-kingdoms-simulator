---
phase: 02.1-office-and-task-deployment
plan: "07"
subsystem: ui
tags: [godot, gdscript, hud, popup, regression]
requires:
  - phase: 02.1-06
    provides: 月初任务选择弹窗、月末月报/任命弹窗与 HUD 月任务摘要基线
provides:
  - 月初任务卡点击后的即时领取按钮刷新
  - 跨旬后仍保持当前主任务摘要的 HUD 展示
  - 月末反馈完成前阻止新月任务弹窗提前出现的 UI gate
affects: [ui-feedback, monthly-loop, phase-03-career-politics]
tech-stack:
  added: []
  patterns: [Task picker relayout on selection, month-end feedback gate inside MainHUD sync loop]
key-files:
  created: []
  modified:
    - three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
    - three-kingdoms-simulator/scripts/ui/MainHUD.gd
    - three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
key-decisions:
  - "月初任务领取按钮改为点击任务卡后才显示，避免默认预选掩盖真实交互状态。"
  - "月末反馈中的月报/升官链本身就是新月任务弹窗的 gate，不新增后端状态字段。"
patterns-established:
  - "Popup relayout pattern: 动态内容更新后通过 deferred reset_size + popup_centered 立即刷新布局。"
  - "HUD authority pattern: 旬总结可更新事件区，但不再覆盖右上角月任务摘要。"
requirements-completed: [UI-03, CARE-03, CARE-04, CARE-05]
duration: 41 min
completed: 2026-04-07
---

# Phase 02.1 Plan 07: HUD Gap Closure Summary

**月初任务领取按钮、跨旬任务 HUD 与月末弹窗时序三处 UI 回归现已被真实 HUD 回归重新锁定。**

## Performance

- **Duration:** 41 min
- **Started:** 2026-04-07T17:26:00+08:00
- **Completed:** 2026-04-07T18:07:19.4584252+08:00
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- 任务卡点击后会立即显示并启用“领取主任务”按钮，不再依赖弹窗外额外点击。
- 普通跨旬后右上角 HUD 继续显示当前主任务、当前进度与剩余旬数，不再被占位建议覆盖。
- 新月任务领取弹窗被延后到月报与升官确认完成之后，避免叠层时序错误。

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix task-card selection so the claim CTA appears immediately** - `a3aa1a7` (fix)
2. **Task 2: Preserve the monthly HUD summary and defer the next-month picker until feedback is finished** - `cf2f9fc` (fix)

## Files Created/Modified
- `three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd` - 月初任务卡选择后即时显示确认按钮并刷新弹窗布局。
- `three-kingdoms-simulator/scripts/ui/MainHUD.gd` - 保持月任务 HUD 摘要权威显示，并在月末反馈期间 gate 新月任务弹窗。
- `three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd` - 增补真实卡片点击、普通跨旬 HUD 保留、以及升官前不得弹新月任务的回归断言。

## Decisions Made
- 将确认按钮显示绑定到“已明确点击任务卡”的交互，而非默认选中第一个任务。
- 使用现有 `MonthReportPanel` / `PromotionPopup` 可见状态与 `_active_month_end_evaluation` 作为 UI gate，避免引入额外后端状态复杂度。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] 改用当前 Godot 兼容的弹窗 relayout 路径**
- **Found during:** Task 1 (Fix task-card selection so the claim CTA appears immediately)
- **Issue:** 初始尝试使用不存在于当前基类的尺寸更新 API，导致 `TaskSelectPanel.gd` 解析失败。
- **Fix:** 收敛为当前项目可运行的 deferred `reset_size()` + `popup_centered(size)` relayout 路径。
- **Files modified:** `three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd`
- **Verification:** `phase21_monthly_hud_regression.gd` 无头回归通过。
- **Committed in:** `a3aa1a7` (part of task commit)

**2. [Rule 1 - Bug] 收窄跨旬 HUD 回归断言，匹配真实剩余旬数变化**
- **Found during:** Task 2 (Preserve the monthly HUD summary and defer the next-month picker until feedback is finished)
- **Issue:** 早期回归把整段 HUD 文本判定为必须完全不变，但普通跨旬后“剩余旬数”理应更新。
- **Fix:** 改为断言任务名/进度/剩余旬数字段仍存在且不含占位建议文案。
- **Files modified:** `three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd`
- **Verification:** `phase21_monthly_hud_regression.gd` 无头回归通过。
- **Committed in:** `cf2f9fc` (part of task commit)

---

**Total deviations:** 2 auto-fixed (2 bug)
**Impact on plan:** 均为让 HUD gap 修复在当前 Godot/UI 约束下稳定落地的必要修正，没有超出计划范围。

## Issues Encountered
- 当前运行时为 Godot 4.6.1 控制台，某些预想中的尺寸更新 API 在现有脚本上下文不可用，需要以已验证的 popup relayout 方式实现同一目标。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 2.1 的三处 HUD 级 UAT 缺口已被同一条无头回归覆盖，可继续与 inspect 任务推进修复合流后做整体验证。
- 后续 UI 扩展应继续遵守“右上角任务区只显示月任务权威摘要，旬建议不覆盖该区”的模式。

---
*Phase: 02.1-office-and-task-deployment*
*Completed: 2026-04-07*
