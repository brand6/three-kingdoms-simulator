---
phase: 02.1-office-and-task-deployment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - three-kingdoms-simulator/scripts/data/resources/OfficeData.gd
  - three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd
  - three-kingdoms-simulator/scripts/data/resources/TaskPoolRuleData.gd
  - three-kingdoms-simulator/scripts/data/resources/PromotionRuleData.gd
  - three-kingdoms-simulator/scripts/data/resources/CharacterSetupPatchData.gd
  - three-kingdoms-simulator/scripts/runtime/PlayerCareerState.gd
  - three-kingdoms-simulator/scripts/runtime/TaskProgressSnapshot.gd
  - three-kingdoms-simulator/scripts/runtime/MonthlyTaskState.gd
  - three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
autonomous: true
requirements: [CARE-03, CARE-05]
must_haves:
  truths:
    - "月度仕途闭环拥有稳定的静态定义与运行时状态契约。"
    - "任务、官职、升官结果不再依赖 Dictionary 猜字段。"
  artifacts:
    - path: three-kingdoms-simulator/scripts/data/resources/OfficeData.gd
      provides: 官职定义 Resource
    - path: three-kingdoms-simulator/scripts/runtime/MonthlyTaskState.gd
      provides: 当月主任务运行时状态
    - path: three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
      provides: 月末结算结果对象
  key_links:
    - from: three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd
      to: three-kingdoms-simulator/scripts/runtime/TaskProgressSnapshot.gd
      via: success/excellent progress fields
      pattern: current_value|target_value|bonus_value
    - from: three-kingdoms-simulator/scripts/data/resources/PromotionRuleData.gd
      to: three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
      via: promotion result payload
      pattern: required_merit|required_fame|notification_source
---

<objective>
Define the exact static and runtime contracts Phase 2.1 will build on.

Purpose: Freeze typed interfaces first so later plans can implement monthly task and promotion logic without ad hoc fields.
Output: Godot `Resource` classes for office/task/promotion/setup-patch data plus typed runtime state objects for career, month task, progress snapshot, and monthly evaluation.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-/02.1-CONTEXT.md
@.planning/phases/02.1-/02.1-RESEARCH.md
@design/数据/官职与任务原型部署数据字段设计 v1.md
@design/原型与实现/Phase 2.1 Godot 实现映射表 v1.md
@three-kingdoms-simulator/scripts/runtime/GameSession.gd
@three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd

<interfaces>
From `three-kingdoms-simulator/scripts/runtime/GameSession.gd`:

```gdscript
var scenario_id: String = ""
var current_year: int = 0
var current_month: int = 0
var current_xun: int = 0
var protagonist_id: String = ""
var character_states: Dictionary = {}
var relation_states: Dictionary = {}
```

From `three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd`:

```gdscript
var ap: int = 0
var energy: int = 0
var stress: int = 0
var fame: int = 0
var merit: int = 0
var current_city_id: String = ""
```

Executor should extend these contracts instead of inventing a parallel state tree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write static Resource contracts for office/task/promotion data</name>
  <files>
    three-kingdoms-simulator/scripts/data/resources/OfficeData.gd
    three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd
    three-kingdoms-simulator/scripts/data/resources/TaskPoolRuleData.gd
    three-kingdoms-simulator/scripts/data/resources/PromotionRuleData.gd
    three-kingdoms-simulator/scripts/data/resources/CharacterSetupPatchData.gd
  </files>
  <read_first>
    design/数据/官职与任务原型部署数据字段设计 v1.md
    design/原型与实现/Phase 2.1 Godot 实现映射表 v1.md
  </read_first>
  <action>Per D-05, D-06, D-13, D-16, and D-17, create five typed custom Resource classes with exported fields matching the frozen Phase 2.1 schema. `OfficeData` must expose at least `id`, `name`, `tier`, `description`, `unlock_task_tags`, `blocked_task_tags`, `merit_threshold`, `next_office_id`, `prev_office_id`, `address_title`, `stipend_text`, `is_phase2_1_available`, `sort_order`. `TaskTemplateData` must expose `id`, `name`, `issuer_character_id`, `issuer_role_tag`, `task_type`, `task_tags`, `min_office_tier`, `max_office_tier`, `description`, `objective_summary`, `duration_months`, `difficulty`, `progress_rule_id`, `success_condition`, `excellent_condition`, `base_rewards`, `bonus_rewards`, `fail_result`, `followup_tags`, `ui_priority`, `is_phase2_1_available`. `TaskPoolRuleData` must expose `candidate_count`, `include_task_tags`, `exclude_task_tags`, `fallback_task_ids`, repeat policy, character/scenario filters. `PromotionRuleData` must expose `from_office_id`, `to_office_id`, `required_merit`, `required_fame`, `require_task_success_this_month`, `min_months_in_office`, `vacancy_key`, `notification_source_character_id`, `success_note`, `failure_note`. `CharacterSetupPatchData` must expose `default_player_character_id`, `start_city_id`, `start_faction_id`, `start_office_id`, `start_merit`, `start_fame`, `start_trust`, `start_relation_overrides`, and `start_flags`. Use `class_name` + `extends Resource` on every file; do not use loose Dictionary-only definitions.</action>
  <verify>
    <automated>rg -n "class_name (OfficeData|TaskTemplateData|TaskPoolRuleData|PromotionRuleData|CharacterSetupPatchData)|extends Resource|@export" "three-kingdoms-simulator/scripts/data/resources"</automated>
  </verify>
  <acceptance_criteria>
    - `three-kingdoms-simulator/scripts/data/resources/OfficeData.gd` contains `class_name OfficeData` and `extends Resource`
    - `three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd` contains exported `success_condition` and `excellent_condition`
    - `three-kingdoms-simulator/scripts/data/resources/PromotionRuleData.gd` contains `required_merit`, `required_fame`, `vacancy_key`, and `notification_source_character_id`
    - `three-kingdoms-simulator/scripts/data/resources/CharacterSetupPatchData.gd` contains `default_player_character_id` and `start_trust`
  </acceptance_criteria>
  <done>All five static definition types exist as typed Godot Resources and expose the exact fields later plans will load.</done>
</task>

<task type="auto">
  <name>Task 2: Write runtime contracts for player career, current month task, and month-end result</name>
  <files>
    three-kingdoms-simulator/scripts/runtime/PlayerCareerState.gd
    three-kingdoms-simulator/scripts/runtime/TaskProgressSnapshot.gd
    three-kingdoms-simulator/scripts/runtime/MonthlyTaskState.gd
    three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
  </files>
  <read_first>
    three-kingdoms-simulator/scripts/runtime/GameSession.gd
    three-kingdoms-simulator/scripts/runtime/RuntimeCharacterState.gd
    design/数据/官职与任务原型部署数据字段设计 v1.md
  </read_first>
  <action>Per D-06, D-07, D-08, D-12, D-14, and D-18, add four typed RefCounted runtime contracts with `static create(...)` helpers, following the Phase 2 DTO pattern. `PlayerCareerState` must hold `character_id`, `current_office_id`, `office_tier`, `total_merit`, `current_fame`, `current_trust`, `months_in_current_office`, `promotion_history`, `last_evaluation_result`, `unlocked_task_tags`, `career_flags`. `TaskProgressSnapshot` must hold `current_value`, `target_value`, `bonus_value`, `step_records`, `last_update_time`, `derived_status`. `MonthlyTaskState` must hold `month_key`, `task_template_id`, `issuer_character_id`, `accepted_at_xun`, `deadline_month_key`, `status`, `progress_snapshot`, `selected_option_index`, `completion_note`, and `reward_snapshot`. `MonthlyEvaluationResult` must hold `month_key`, `character_id`, `task_result`, `merit_delta`, `fame_delta`, `trust_delta`, `office_changed`, `old_office_id`, `new_office_id`, `promotion_triggered_by_rule_id`, `summary_lines`, `next_goal_hint`, and a promotion-missing-values payload used by D-12. Keep these files data-only; do not put resolver logic here.</action>
  <verify>
    <automated>rg -n "class_name (PlayerCareerState|TaskProgressSnapshot|MonthlyTaskState|MonthlyEvaluationResult)|static func create|current_value|next_goal_hint|current_trust" "three-kingdoms-simulator/scripts/runtime"</automated>
  </verify>
  <acceptance_criteria>
    - `PlayerCareerState.gd` contains `current_office_id`, `current_trust`, and `months_in_current_office`
    - `TaskProgressSnapshot.gd` contains `current_value`, `target_value`, and `bonus_value`
    - `MonthlyTaskState.gd` contains `status` and `reward_snapshot`
    - `MonthlyEvaluationResult.gd` contains `office_changed`, `summary_lines`, and `next_goal_hint`
  </acceptance_criteria>
  <done>Later plans can store career/month-task/month-report state in typed runtime objects instead of free-form dictionaries.</done>
</task>

</tasks>

<verification>
Contract files exist, expose stable exported fields, and match the frozen Phase 2.1 data model without embedding business logic.
</verification>

<success_criteria>
- Phase 2.1 static definitions are represented by typed Godot `Resource` classes.
- Phase 2.1 runtime state has explicit contracts for career, monthly task progress, and monthly evaluation.
- Downstream plans can load and mutate these contracts without schema guessing.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-/02.1-01-SUMMARY.md`
</output>
