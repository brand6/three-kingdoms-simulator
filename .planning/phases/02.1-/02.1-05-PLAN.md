---
phase: 02.1-office-and-task-deployment
plan: "05"
type: execute
wave: 3
depends_on: ["02.1-02", "02.1-03", "02.1-04"]
files_modified:
  - three-kingdoms-simulator/scripts/systems/TaskSystem.gd
  - three-kingdoms-simulator/scripts/systems/CareerSystem.gd
  - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
  - three-kingdoms-simulator/scripts/autoload/TimeManager.gd
  - three-kingdoms-simulator/scripts/tests/phase21_monthly_career_regression.gd
autonomous: true
requirements: [CARE-02, CARE-03, CARE-04, CARE-05]
must_haves:
  truths:
    - "真实行动会推进当月主任务进度。"
    - "第三旬结束后会自动生成任务结果、数值变化和升官判定。"
    - "升官失败时系统能给出差值/阻塞原因，而不是只说未达成。"
    - "升官成功后当前官职和下月任务层级发生变化。"
  artifacts:
    - path: three-kingdoms-simulator/scripts/systems/TaskSystem.gd
      provides: 月任务生成、推进和结算
    - path: three-kingdoms-simulator/scripts/systems/CareerSystem.gd
      provides: 简化任命判定
    - path: three-kingdoms-simulator/scripts/tests/phase21_monthly_career_regression.gd
      provides: 月度 career regression
  key_links:
    - from: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      to: three-kingdoms-simulator/scripts/systems/TaskSystem.gd
      via: post-action progress update and month-end settlement
      pattern: execute_phase2_action|end_current_xun
    - from: three-kingdoms-simulator/scripts/systems/CareerSystem.gd
      to: three-kingdoms-simulator/data/office_rules/promotion_congshi_to_zhubu.tres
      via: promotion rule lookup
      pattern: promotion_congshi_to_zhubu
---

<objective>
Implement the backend monthly task and simplified appointment pipeline.

Purpose: Make the Phase 2.1 loop produce real monthly progression, reward deltas, and office changes instead of only bootstrap scaffolding.
Output: TaskSystem, CareerSystem, GameRoot month-end orchestration, and a real monthly career regression.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-/02.1-CONTEXT.md
@.planning/phases/02.1-/02.1-RESEARCH.md
@.planning/phases/02-旬内行动—关系闭环/02-02-SUMMARY.md
@three-kingdoms-simulator/scripts/autoload/GameRoot.gd
@three-kingdoms-simulator/scripts/autoload/TimeManager.gd
@three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement month-task generation, selection lock, and progress updates from existing actions</name>
  <files>
    three-kingdoms-simulator/scripts/systems/TaskSystem.gd
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    three-kingdoms-simulator/scripts/tests/phase21_monthly_career_regression.gd
  </files>
  <read_first>
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd
    three-kingdoms-simulator/scripts/runtime/MonthlyTaskState.gd
    three-kingdoms-simulator/data/tasks/task_grain_audit.tres
  </read_first>
  <behavior>
    - Test 1: month start chooses from repository candidates and locks exactly one `MonthlyTaskState` when the user/system selects a card, while guaranteeing at least one stable first-promotion path in the first month pool.
    - Test 2: executing `visit / train / study / inspect / rest` updates `current_value` through the task’s `progress_rule_id` without creating new action IDs.
    - Test 3: before task selection, action execution returns a blocked result; after selection, real actions mutate AP and task progress.
    - Test 4: month-end task notes or summary payload expose a visible trust-flavored narrative line instead of leaving trust as numeric-only feedback.
  </behavior>
  <action>Create `TaskSystem.gd` with concrete methods to generate candidates from `TaskPoolRuleData`, lock a selected task into `MonthlyTaskState`, expose remaining xun count, and update `TaskProgressSnapshot.current_value` from exact progress-rule mappings: `logistics_balanced = {inspect:4, study:2, visit:1, rest:1, train:0}`, `clan_pacify_social = {visit:4, study:1, rest:1, inspect:0, train:0}`, `document_cleanup_admin = {study:4, inspect:3, rest:1, visit:1, train:0}`, `recommend_talent_network = {visit:3, study:2, inspect:1, rest:0, train:0}`. Candidate generation must preserve the first-month stable positive path required by D-08a instead of allowing it to be shuffled out. Update `GameRoot.gd` to expose methods for fetching pending task cards, selecting one task index, and appending task progress after successful phase-2 action execution. Task settlement output must include one short political-summary string that can surface trust gain/loss as visible narrative feedback. Add regression coverage that boots, selects a task, executes real actions, and asserts progress changes from the expected weights. Do not add task-exclusive action buttons or alternate action IDs.</action>
  <verify>
    <automated>godot --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase21_monthly_career_regression.gd"</automated>
  </verify>
  <acceptance_criteria>
    - `TaskSystem.gd` contains `generate_month_candidates`, `select_month_task`, and a progress-rule map with the four exact IDs
    - `GameRoot.gd` contains a public task-selection method and a post-action task-progress update path
    - regression proves at least one first-month candidate can stably satisfy first-promotion thresholds when vacancy is open
    - regression exits `0` after proving progress changes from real actions
  </acceptance_criteria>
  <done>Monthly tasks can be selected and progressed entirely through the existing five foundational actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement month-end evaluation, reward writeback, and simplified appointment results</name>
  <files>
    three-kingdoms-simulator/scripts/systems/CareerSystem.gd
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    three-kingdoms-simulator/scripts/autoload/TimeManager.gd
    three-kingdoms-simulator/scripts/tests/phase21_monthly_career_regression.gd
  </files>
  <read_first>
    three-kingdoms-simulator/scripts/autoload/GameRoot.gd
    three-kingdoms-simulator/scripts/autoload/TimeManager.gd
    three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
    three-kingdoms-simulator/data/office_rules/promotion_congshi_to_zhubu.tres
  </read_first>
  <behavior>
    - Test 1: ending the third xun auto-triggers month-end settlement before the new month’s first xun begins.
    - Test 2: settlement writes merit/fame/trust deltas into runtime state and stores `last_month_evaluation`.
    - Test 3: promotion success requires vacancy + data thresholds + notification sender; failure payload includes missing values/hard blocker text per D-12.
    - Test 4: failure classification resolves to one standardized player-facing label only: `功绩不足` / `名望不足` / `无空缺` / `任务未达标`.
  </behavior>
  <action>Create `CareerSystem.gd` to evaluate promotion rules in this exact order: next-office existence, vacancy open, task success required, merit threshold, fame threshold, min months in office, notification sender availability. Update `GameRoot.end_current_xun()` so when `current_xun == 3` it settles the monthly task first, writes `merit/fame/trust` deltas back to `RuntimeCharacterState` and `PlayerCareerState`, then runs `CareerSystem` and stores a `MonthlyEvaluationResult`, then advances to the new month and immediately regenerates next-month candidates while re-locking actions until the next task is selected. The stored result payload must separate machine detail from player copy: internal reason data may keep precise deltas, but the player-facing failure label must normalize to exactly one of `功绩不足` / `名望不足` / `无空缺` / `任务未达标`. Trust may shape the political-summary sentence, but it must not become a new hard promotion gate in Phase 2.1. Update `TimeManager.gd` only as needed to keep month rollover labels correct; do not move business logic into `TimeManager`. Extend the monthly career regression to prove one success path to `office_zhubu` and one failure path that emits a concrete missing-value hint such as remaining merit/fame or closed vacancy.</action>
  <verify>
    <automated>godot --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase21_monthly_career_regression.gd"</automated>
  </verify>
  <acceptance_criteria>
    - `CareerSystem.gd` contains the ordered checks for vacancy, merit, fame, and notification sender
    - `GameRoot.gd` writes `last_month_evaluation` before the month rolls forward
    - regression proves failed promotion labels are standardized to the four allowed top-line phrases
    - regression exits `0` and covers both promotion success and failure hint payloads
  </acceptance_criteria>
  <done>The backend now delivers a full monthly task/result/promotion loop with explainable success and failure outcomes.</done>
</task>

</tasks>

<verification>
The monthly career regression proves real actions feed a monthly task, month-end settlement writes back rewards, and simplified appointment outcomes are explainable.
</verification>

<success_criteria>
- Real actions progress the selected monthly task.
- Third-xun month-end settlement happens automatically and in the correct order.
- Promotion success/failure is data-driven, produces a machine-verifiable reason payload, and collapses player-visible failure copy to the four approved labels.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-/02.1-05-SUMMARY.md`
</output>
