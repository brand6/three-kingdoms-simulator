---
phase: 02.1-office-and-task-deployment
plan: "08"
type: execute
wave: 5
depends_on: ["02.1-05", "02.1-06"]
files_modified:
  - three-kingdoms-simulator/data/config/phase2_action_menu_config.tres
  - three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd
  - three-kingdoms-simulator/scripts/tests/phase21_monthly_career_regression.gd
autonomous: true
requirements: [CARE-02, CARE-03]
gap_closure: true
must_haves:
  truths:
    - "荀彧作为默认文官主角时，巡察不会再因为身份限制而一直置灰。"
    - "领取需要巡察推进的主任务后，执行巡察会真实推进该月任务进度。"
    - "回归测试不再把荀彧巡察禁用当作正确行为。"
  artifacts:
    - path: three-kingdoms-simulator/data/config/phase2_action_menu_config.tres
      provides: inspect 对 civil_official 的菜单开放配置
    - path: three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd
      provides: 荀彧 inspect 可用性的菜单回归
    - path: three-kingdoms-simulator/scripts/tests/phase21_monthly_career_regression.gd
      provides: inspect 推进月任务的端到端回归
  key_links:
    - from: three-kingdoms-simulator/data/config/phase2_action_menu_config.tres
      to: three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd
      via: allowed_identity_types -> disabled_reason resolution
      pattern: inspect|civil_official|allowed_identity_types
    - from: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      to: three-kingdoms-simulator/scripts/systems/TaskSystem.gd
      via: execute_phase2_action success -> append_progress_from_action
      pattern: execute_phase2_action|append_progress_from_action
---

<objective>
Close the patrol-action gap so the default civil-official protagonist can advance inspect-based monthly work using the existing action set.

Purpose: Preserve D-05 by fixing the mismatched action-availability configuration instead of inventing a new task-only action.
Output: Updated inspect availability config and regressions proving inspect is usable and advances monthly progress for 荀彧.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-/02.1-CONTEXT.md
@.planning/phases/02.1-/02.1-UAT.md
@.planning/phases/02.1-/02.1-05-SUMMARY.md
@three-kingdoms-simulator/data/config/phase2_action_menu_config.tres
@three-kingdoms-simulator/scripts/systems/Phase2ActionCatalog.gd
@three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd
@three-kingdoms-simulator/scripts/tests/phase21_monthly_career_regression.gd

<interfaces>
From `Phase2ActionCatalog.gd`:

```gdscript
func get_available_actions(
	protagonist: CharacterDefinition,
	runtime_state: RuntimeCharacterState,
	visit_targets: Array[CharacterDefinition]
) -> Array
```

`inspect` is already a standard Phase 2 action and `GameRoot.execute_phase2_action()` already routes successful actions into `TaskSystem.append_progress_from_action(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reopen inspect for the default civil-official protagonist</name>
  <files>
    three-kingdoms-simulator/data/config/phase2_action_menu_config.tres
    three-kingdoms-simulator/scripts/tests/phase2_action_resolver_test.gd
  </files>
  <behavior>
    - Test 1: bootstrapped `xun_yu` still sees `inspect` in the action list.
    - Test 2: `inspect.disabled_reason` is empty for `xun_yu` when no AP/energy/location blocker applies.
  </behavior>
  <action>Fix UAT gap 4 at the configuration layer, per D-05. In `phase2_action_menu_config.tres`, add `civil_official` to `inspect.allowed_identity_types` so Phase 2.1’s default protagonist is not blocked from the one governance action that the task system already treats as valid progress. Update `phase2_action_resolver_test.gd` to remove the old assumption that `xun_yu` inspect should be disabled by identity, while keeping the existing visibility/disabled-reason coverage for AP, energy, visit-target, and wrong-location cases intact.</action>
  <verify>
    <automated>& "D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe" --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase2_action_resolver_test.gd"</automated>
  </verify>
  <done>Inspect is no longer identity-locked for the default civil-official path, and the baseline action-catalog regression matches that behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove inspect advances an inspect-based monthly task after selection</name>
  <files>
    three-kingdoms-simulator/scripts/tests/phase21_monthly_career_regression.gd
  </files>
  <behavior>
    - Test 1: after selecting an inspect-driven monthly task for `xun_yu`, `inspect` is available and not greyed out.
    - Test 2: executing `inspect` increases `current_month_task.progress_snapshot.current_value`.
  </behavior>
  <action>Extend `phase21_monthly_career_regression.gd` with a targeted regression for UAT gap 4: choose a pending monthly candidate whose progress rule accepts `inspect` (for example the first-month governance/patrol-style task already present in data), assert `GameRoot.get_available_phase2_actions()` returns `inspect` with no disabled reason after task selection, then execute `inspect` and verify the selected month task’s progress snapshot increases. Keep the regression focused on the existing Phase 2.1 data and loop; do not add a new task template, a new action, or broader task-pool reshuffling.</action>
  <verify>
    <automated>& "D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe" --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase21_monthly_career_regression.gd"</automated>
  </verify>
  <done>The inspect action is proven end-to-end as a valid way for 荀彧 to push an inspect-based monthly task forward.</done>
</task>

</tasks>

<verification>
The action-catalog regression and the monthly career regression together prove inspect is both available and useful for the default Phase 2.1 protagonist.
</verification>

<success_criteria>
- UAT 4: inspect is not greyed out for 荀彧 when identity is the only blocker.
- UAT 4: after selecting an inspect-eligible month task, using inspect changes monthly progress.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-/02.1-08-SUMMARY.md`
</output>
