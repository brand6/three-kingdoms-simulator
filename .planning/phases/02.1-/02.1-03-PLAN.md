---
phase: 02.1-office-and-task-deployment
plan: "03"
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - three-kingdoms-simulator/data/tasks/task_grain_audit.tres
  - three-kingdoms-simulator/data/tasks/task_clan_pacify.tres
  - three-kingdoms-simulator/data/tasks/task_document_cleanup.tres
  - three-kingdoms-simulator/data/tasks/task_recommend_talent.tres
  - three-kingdoms-simulator/data/task_rules/task_pool_xunyu_early_career.tres
  - three-kingdoms-simulator/data/scenario_patches/xunyu_default_start_patch.tres
autonomous: true
requirements: [CARE-02, CARE-03]
must_haves:
  truths:
    - "新月能稳定生成 2–3 个候选任务。"
    - "默认开局可切到荀彧并带上 Phase 2.1 仕途初始值。"
  artifacts:
    - path: three-kingdoms-simulator/data/tasks/task_grain_audit.tres
      provides: 基础后勤任务模板
    - path: three-kingdoms-simulator/data/task_rules/task_pool_xunyu_early_career.tres
      provides: 荀彧前期月初任务池规则
    - path: three-kingdoms-simulator/data/scenario_patches/xunyu_default_start_patch.tres
      provides: 荀彧默认开局补丁
  key_links:
    - from: three-kingdoms-simulator/data/task_rules/task_pool_xunyu_early_career.tres
      to: three-kingdoms-simulator/data/tasks/task_document_cleanup.tres
      via: fallback task ids
      pattern: fallback_task_ids
---

<objective>
Author the monthly task sample data and the 荀彧 bootstrap patch.

Purpose: Make the phase executable with real candidate tasks and a fixed default-protagonist career start.
Output: Four task templates, one early-career task-pool rule, and one Xun Yu start patch resource.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-/02.1-CONTEXT.md
@.planning/phases/02.1-/02.1-RESEARCH.md
@design/总纲/官职与任务原型部署 Phase 2.1 v1.md
@design/数据/Phase 2.1 最小数据录入清单 v1.md
@three-kingdoms-simulator/data/generated/190/characters.json
@three-kingdoms-simulator/data/generated/190/scenario_190_smoke.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the four task templates with fixed progress-rule IDs and reward payloads</name>
  <files>
    three-kingdoms-simulator/data/tasks/task_grain_audit.tres
    three-kingdoms-simulator/data/tasks/task_clan_pacify.tres
    three-kingdoms-simulator/data/tasks/task_document_cleanup.tres
    three-kingdoms-simulator/data/tasks/task_recommend_talent.tres
  </files>
  <read_first>
    three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd
    design/总纲/官职与任务原型部署 Phase 2.1 v1.md
    design/数据/官职与任务原型部署数据字段设计 v1.md
  </read_first>
<action>Per D-05, D-06, D-07, D-08, D-08a, D-09, and D-12a, create four task-template `.tres` files with exact IDs `task_grain_audit`, `task_clan_pacify`, `task_document_cleanup`, and `task_recommend_talent`. Use `cao_cao` as `issuer_character_id` for the first three and `xun_yu`'s higher-tier superior placeholder only if such a character exists in current sample data; otherwise keep `cao_cao` for all four to avoid dangling IDs. Assign `progress_rule_id` values `logistics_balanced`, `clan_pacify_social`, `document_cleanup_admin`, and `recommend_talent_network`. Set `success_condition.current_value = 8`, `excellent_condition.current_value = 11`, and fixed 1-month duration for all four so the executor can prove D-06 with one unified progress shape. Among the four templates, at least one early-career task must be tuned as the stable first-month positive path: when paired with the Xun Yu start patch and a normal success result, it should reliably bridge the remaining merit/fame gap for `office_congshi -> office_zhubu` if `vacancy_zhubu` is open. Set concrete rewards: grain/document tasks give stable merit (`+7` / bonus `+3`) with low fame/trust, clan pacify gives `merit +6 / fame +2 / trust +1`, recommend talent gives `merit +8 / fame +1 / trust +3`. At least one task's completion/failure notes must explicitly produce visible trust-flavored political text such as gaining confidence or easing doubts, while keeping trust as narrative feedback rather than a new hard gate. Set failure payloads with explicit negative or zero deltas and a human-readable `failure_note`; do not leave reward dictionaries empty.</action>
  <verify>
    <automated>rg -n "task_(grain_audit|clan_pacify|document_cleanup|recommend_talent)|progress_rule_id|current_value|base_rewards|bonus_rewards|fail_result" "three-kingdoms-simulator/data/tasks"</automated>
  </verify>
  <acceptance_criteria>
    - all four task `.tres` files exist
    - each task file contains a `progress_rule_id`
    - each task file contains success and excellent thresholds reflecting `8` and `11`
    - each task file contains `base_rewards` and `fail_result`
  </acceptance_criteria>
  <done>Monthly task generation has concrete prototype tasks with explicit progress rules and rewards.</done>
</task>

<task type="auto">
  <name>Task 2: Record the early-career task-pool rule and Xun Yu start patch</name>
  <files>
    three-kingdoms-simulator/data/task_rules/task_pool_xunyu_early_career.tres
    three-kingdoms-simulator/data/scenario_patches/xunyu_default_start_patch.tres
  </files>
  <read_first>
    three-kingdoms-simulator/scripts/data/resources/TaskPoolRuleData.gd
    three-kingdoms-simulator/scripts/data/resources/CharacterSetupPatchData.gd
    three-kingdoms-simulator/data/generated/190/characters.json
    three-kingdoms-simulator/data/generated/190/scenario_190_smoke.json
  </read_first>
<action>Per D-01, D-03, D-04, D-04a, D-08a, and the research defaults, create `task_pool_xunyu_early_career.tres` so it targets `scenario_190_smoke`, `character_id = xun_yu`, `office_tier_min = 1`, `office_tier_max = 1`, `candidate_count = 3`, `allow_repeat_in_consecutive_months = false`, `include_task_tags = ["logistics", "admin", "politics_basic"]`, and `fallback_task_ids = ["task_document_cleanup"]`. The first-month pool must guarantee that at least one candidate is the stable positive path task described above, rather than leaving all three candidates equally likely to underfeed promotion. Create `xunyu_default_start_patch.tres` with `default_player_character_id = xun_yu`, `start_office_id = office_congshi`, `start_faction_id` and `start_city_id` matching the existing 190 sample data, `start_merit = 18`, `start_fame = 11`, `start_trust = 14`, plus any safe relation override needed for Cao Cao trust text. Do not add a second patch or mid-career rule in this plan; Phase 2.1 P0 only needs the early-career path first.</action>
  <verify>
    <automated>rg -n "task_pool_xunyu_early_career|candidate_count|xun_yu|start_office_id|start_merit|start_fame|start_trust" "three-kingdoms-simulator/data/task_rules" "three-kingdoms-simulator/data/scenario_patches"</automated>
  </verify>
  <acceptance_criteria>
- `task_pool_xunyu_early_career.tres` contains `candidate_count` with `3` and `xun_yu`
- `task_pool_xunyu_early_career.tres` contains `fallback_task_ids`
- `xunyu_default_start_patch.tres` contains `default_player_character_id = xun_yu`
- `xunyu_default_start_patch.tres` contains `office_congshi`, `18`, `11`, and `14`
    - first-month pool data guarantees at least one stable first-promotion path when `vacancy_zhubu` is open
  </acceptance_criteria>
  <done>The phase has concrete month-start task selection data and a deterministic Xun Yu bootstrap patch.</done>
</task>

</tasks>

<verification>
Task templates, task-pool rule, and start patch exist with fixed IDs and enough concrete values to drive bootstrap and month-start generation.
</verification>

<success_criteria>
- Four prototype monthly task definitions exist.
- The early-career rule always yields candidates or a fallback, and first month includes one stable first-promotion path.
- Default entry can switch to 荀彧 with explicit merit/fame/trust and office values.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-/02.1-03-SUMMARY.md`
</output>
