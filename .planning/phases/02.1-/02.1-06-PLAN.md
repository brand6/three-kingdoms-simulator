---
phase: 02.1-office-and-task-deployment
plan: "06"
type: execute
wave: 4
depends_on: ["02.1-04", "02.1-05"]
files_modified:
  - three-kingdoms-simulator/scenes/main/MainScene.tscn
  - three-kingdoms-simulator/scripts/ui/MainHUD.gd
  - three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
  - three-kingdoms-simulator/scripts/ui/MonthReportPanel.gd
  - three-kingdoms-simulator/scripts/ui/PromotionPopup.gd
  - three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
autonomous: true
requirements: [UI-03, CARE-03, CARE-04, CARE-05]
must_haves:
  truths:
    - "新月开始时任务选择弹窗自动打开，并且玩家必须先选 1 个任务。"
    - "HUD 常驻显示当前主任务、当前进度、剩余旬数。"
    - "月末先显示任务结果月报，再显示升官弹窗。"
    - "未升官时，界面会明确显示距离目标官职还差多少关键数值。"
  artifacts:
    - path: three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
      provides: 月初任务选择交互
    - path: three-kingdoms-simulator/scripts/ui/MonthReportPanel.gd
      provides: 月报渲染
    - path: three-kingdoms-simulator/scripts/ui/PromotionPopup.gd
      provides: 第二段升官反馈弹窗
    - path: three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
      provides: HUD monthly regression
  key_links:
    - from: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      to: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      via: task card list, task selection, month-end report sequence
      pattern: get_pending_month_tasks|select_month_task|get_last_month_evaluation
    - from: three-kingdoms-simulator/scenes/main/MainScene.tscn
      to: three-kingdoms-simulator/scripts/ui/MonthReportPanel.gd
      via: dialog node mount
      pattern: MonthReportPanel
---

<objective>
Wire the Phase 2.1 monthly HUD flow and lock it with a headless UI regression.

Purpose: Make the player-facing experience match the locked month-start and month-end interaction contract instead of leaving the new backend invisible.
Output: Month-start task picker, HUD task summary rail, month report panel, promotion popup, and a monthly HUD regression.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-/02.1-CONTEXT.md
@.planning/phases/02.1-/02.1-RESEARCH.md
@.planning/phases/02.1-/02.1-UI-SPEC.md
@three-kingdoms-simulator/scenes/main/MainScene.tscn
@three-kingdoms-simulator/scripts/ui/MainHUD.gd
@three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd

<interfaces>
UI copy and order locked by context/UI-SPEC:

```text
Primary CTA: 领取主任务
Month-start gate copy: 本月尚未领受公事，请先择定一项主任务。
Month-end order: 先任务结果月报 -> 再升官结果弹窗
HUD summary fields: 当前主任务 / 当前进度 / 剩余旬数
Month report fields: 任务名称 / 结果 / 进度 / merit-fame-trust变化 / 一句政治含义总结
```

Use the existing `MainScene` overlay pattern; do not switch to scene changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the month-start task picker and persistent HUD task summary</name>
  <files>
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
    three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
  </files>
  <read_first>
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    .planning/phases/02.1-/02.1-UI-SPEC.md
    .planning/phases/02.1-/02.1-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: booting a new month auto-opens the task picker and blocks further interaction until one card is confirmed.
    - Test 2: task cards show exactly task name, issuer, description, and expected reward per D-09.
    - Test 3: after selection, the right-side task summary shows task name, numeric progress, and remaining xun count per D-10.
  </behavior>
  <action>Add a dedicated `TaskSelectPanel.gd` mounted under `MainScene` as a month-start overlay. In `MainHUD.gd`, open it automatically when `GameRoot` reports `month_action_locked = true`, render 2–3 candidate cards, and make `领取主任务` the only primary CTA. The panel must display exactly these card fields: task name, issuer, one short description block, expected reward. It must not foreground thresholds, difficulty math, or hidden formulas. The lock explanation must use the institutional copy `本月尚未领受公事，请先择定一项主任务。`, not a hard system-block message. Update the existing right-column task area to replace placeholder text with `当前主任务：{name}` / `当前进度：{current}/{target or bonus}` / `剩余旬数：{remaining}`. If no task is available, use the UI-SPEC empty-state copy verbatim. Do not allow dismissing the month-start picker into free action selection before one task is locked.</action>
  <verify>
    <automated>godot --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase21_monthly_hud_regression.gd"</automated>
  </verify>
  <acceptance_criteria>
    - `TaskSelectPanel.gd` exists and `MainScene.tscn` mounts it
    - `MainHUD.gd` contains the copy `领取主任务` and `本月尚未领受公事，请先择定一项主任务。`
    - regression exits `0` after proving auto-open + forced selection + HUD summary refresh
  </acceptance_criteria>
  <done>The player now starts each month by selecting exactly one main task and can always see monthly task progress in the HUD.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the month report and promotion popup sequence with cause-first feedback</name>
  <files>
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    three-kingdoms-simulator/scripts/ui/MonthReportPanel.gd
    three-kingdoms-simulator/scripts/ui/PromotionPopup.gd
    three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
  </files>
  <read_first>
    three-kingdoms-simulator/scenes/main/MainScene.tscn
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    .planning/phases/02.1-/02.1-UI-SPEC.md
    three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
  </read_first>
  <behavior>
    - Test 1: when the third xun ends, the month report opens before any promotion popup.
    - Test 2: month report shows exactly task name, result, progress, merit/fame/trust deltas, and one political-meaning summary line.
    - Test 3: failed promotion popup includes a standardized failure label plus a concrete missing-value line such as remaining merit/fame or blocked vacancy, satisfying D-12 and UI-03.
  </behavior>
  <action>Create `MonthReportPanel.gd` and `PromotionPopup.gd`, mount them in `MainScene.tscn`, and wire `MainHUD.gd` so the end-of-month flow is strictly two-step per D-11: first show the task-result month report, then after confirmation show the promotion popup. The month report must render exactly these fields: selected task name, verdict (`success / excellent / failed`), progress versus threshold, merit delta, fame delta, trust delta, and one short political-meaning summary line. Do not append next-office tracker rows, extra reward breakdown tables, or Phase 3-style recommendation context. The promotion popup must render either the new office title + issuer + concise appointment reason, or `未获任命` plus one standardized top-line blocker label from `功绩不足` / `名望不足` / `无空缺` / `任务未达标`, followed by a concrete shortfall line such as `距离主簿级辅官仍差 3 功绩` / `名望仍差 1` / `当前无空缺`. Keep current office as the only always-visible HUD office field; do not add an always-on next-office tracker. Extend the headless HUD regression to prove dialog order, required copy presence, standardized failure labels, and close/reopen stability across a full month rollover.</action>
  <verify>
    <automated>godot --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase21_monthly_hud_regression.gd"</automated>
  </verify>
  <acceptance_criteria>
    - `MonthReportPanel.gd` and `PromotionPopup.gd` both exist and are mounted in `MainScene.tscn`
    - `MainHUD.gd` opens the month report before the promotion popup
    - regression exits `0` after proving dialog order, constrained month-report fields, and standardized failed-promotion copy
  </acceptance_criteria>
  <done>The player-facing monthly feedback loop is fully visible, causal, and matches the locked two-dialog contract.</done>
</task>

</tasks>

<verification>
The monthly HUD regression proves month-start selection, persistent task summary, and cause-first month-end feedback all work through the real HUD shell.
</verification>

<success_criteria>
- Month-start task picking is mandatory and auto-opened.
- HUD always shows main task, progress, and remaining xun.
- Month-end feedback is split into task report first, promotion popup second, with constrained report fields and standardized failure labels.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-/02.1-06-SUMMARY.md`
</output>
