---
phase: 02.1-office-and-task-deployment
plan: "07"
type: execute
wave: 5
depends_on: ["02.1-06"]
files_modified:
  - three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
  - three-kingdoms-simulator/scripts/ui/MainHUD.gd
  - three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
autonomous: true
requirements: [UI-03, CARE-03, CARE-04, CARE-05]
gap_closure: true
must_haves:
  truths:
    - "点击月初任务卡后，领取主任务按钮会立即出现并可点击，不需要额外点击弹窗外部。"
    - "跨旬后只要当月主任务仍未结算，右侧 HUD 继续显示当前主任务、当前进度、剩余旬数。"
    - "月末升官结果未确认前，新月任务领取弹窗不会提前叠在月报/升官反馈之上。"
  artifacts:
    - path: three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
      provides: 月初任务卡选择后的即时按钮与布局刷新
    - path: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      provides: 跨旬任务摘要保留与月末反馈期间的新月任务弹窗 gate
    - path: three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
      provides: 覆盖 UAT 1 / 6 / 8 的 HUD 回归
  key_links:
    - from: three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
      to: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      via: task_confirmed + popup relayout after card selection
      pattern: task_confirmed|minimum_size_changed|reset_size|popup_centered
    - from: three-kingdoms-simulator/scripts/ui/MainHUD.gd
      to: three-kingdoms-simulator/scripts/autoload/GameRoot.gd
      via: consume_last_month_evaluation and month_action_locked gating
      pattern: consume_last_month_evaluation|month_action_locked|_open_month_task_picker_if_needed
---

<objective>
Close the Phase 2.1 HUD regressions that break month-start confirmation, persistent task visibility, and month-end popup order.

Purpose: Restore the locked Phase 2.1 monthly flow without changing backend scope or adding new systems.
Output: Immediate month-task confirm CTA refresh, persistent in-month task HUD, delayed next-month task picker, and updated HUD regression coverage.
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-/02.1-CONTEXT.md
@.planning/phases/02.1-/02.1-UAT.md
@.planning/phases/02.1-/02.1-06-SUMMARY.md
@three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
@three-kingdoms-simulator/scripts/ui/MainHUD.gd
@three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd

<interfaces>
From `TaskSelectPanel.gd`:

```gdscript
signal task_confirmed(selected_index: int)
func show_task_picker(candidates: Array, repository: Node) -> void
func _on_card_pressed(index: int, repository: Node) -> void
```

From `MainHUD.gd`:

```gdscript
func _on_end_xun_confirmed() -> void
func _show_xun_summary(summary: Variant) -> void
func _open_month_task_picker_if_needed(session: GameSession) -> void
func _sync_month_task_ui_state() -> void
func _on_month_report_confirmed() -> void
```

Keep the existing MainScene overlay pattern. Do not add scene switches or new month-loop systems.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix task-card selection so the claim CTA appears immediately</name>
  <files>
    three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
    three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
  </files>
  <behavior>
    - Test 1: clicking a task card inside `TaskSelectPanel` immediately makes `领取主任务` visible and enabled.
    - Test 2: the picker does not require any extra click outside the popup to finish its relayout.
  </behavior>
  <action>In `TaskSelectPanel.gd`, fix the render path identified in UAT gap 1 so task-card selection updates button state and popup layout in the same interaction cycle, per D-01 and D-03. After `_on_card_pressed()` and the initial `show_task_picker()` render, force a deferred popup relayout (`minimum_size_changed`, `reset_size`, container sort, or equivalent Godot-safe refresh path) so the confirm row is recomputed before the player clicks elsewhere. Bind the confirm button visibility/availability to that same refresh path instead of relying on incidental focus changes. Extend `phase21_monthly_hud_regression.gd` to simulate the real card-click path and assert the confirm CTA is immediately visible and clickable.</action>
  <verify>
    <automated>& "D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe" --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase21_monthly_hud_regression.gd"</automated>
  </verify>
  <done>Task card selection consistently reveals an actionable `领取主任务` button without any extra click outside the popup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the monthly HUD summary and defer the next-month picker until feedback is finished</name>
  <files>
    three-kingdoms-simulator/scripts/ui/MainHUD.gd
    three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
  </files>
  <behavior>
    - Test 1: after a normal xun advance inside the same month, the HUD still shows current task / progress / remaining xun.
    - Test 2: when month-end evaluation starts, the next-month task picker stays hidden until the promotion popup is confirmed.
    - Test 3: after promotion confirmation, if the new month is locked, the task picker can then open normally.
  </behavior>
  <action>In `MainHUD.gd`, fix UAT gaps 6 and 8 with the smallest UI-layer change set. First, stop `_show_xun_summary()` from overwriting `_task_list` with placeholder text whenever `current_month_task` still exists; keep xun advice inside the summary dialog and/or event panel so D-10 remains true. Second, add an explicit month-end feedback gate in `_sync_month_task_ui_state()` / `_open_month_task_picker_if_needed()` so `month_action_locked` alone is not enough to auto-open `TaskSelectPanel` while the month report / promotion confirmation chain is still active, per D-11. Do not move the monthly rollover earlier or add new backend flags unless MainHUD cannot safely derive the gate from existing popup state and `_active_month_end_evaluation`. Extend `phase21_monthly_hud_regression.gd` to cover both regressions: the task HUD surviving a normal cross-xun refresh, and the new-month picker remaining hidden until promotion confirmation.</action>
  <verify>
    <automated>& "D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe" --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase21_monthly_hud_regression.gd"</automated>
  </verify>
  <done>Right-side task HUD remains authoritative throughout the month, and new-month task pickup no longer races ahead of month-end feedback confirmation.</done>
</task>

</tasks>

<verification>
The headless HUD regression proves all three diagnosed UI failures are closed through the real MainScene overlay flow.
</verification>

<success_criteria>
- UAT 1: task card click immediately reveals the claim button.
- UAT 6: normal cross-xun updates do not replace the current monthly task summary with placeholder copy.
- UAT 8: the next-month task picker stays hidden until the promotion popup is confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-/02.1-07-SUMMARY.md`
</output>
