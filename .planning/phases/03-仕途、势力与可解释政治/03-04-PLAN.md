---
phase: 03-仕途、势力与可解释政治
plan: "04"
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - three-kingdoms-simulator/scripts/systems/AppointmentResolver.gd
  - three-kingdoms-simulator/scripts/systems/CareerSystem.gd
  - three-kingdoms-simulator/scripts/runtime/PlayerCareerState.gd
  - three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
  - three-kingdoms-simulator/scripts/runtime/GameSession.gd
  - three-kingdoms-simulator/scripts/autoload/GameRoot.gd
  - three-kingdoms-simulator/scripts/tests/phase3_appointment_resolver_regression.gd
autonomous: true
requirements:
  - CARE-03
  - CARE-04
  - POLI-02
  - POLI-03
must_haves:
  truths:
    - "月末结果可以落到任命、升迁、驳回或错失机会四类顶层结论。"
    - "失败 headline 按最早阻断层判定，并保留 2–3 条关键原因与下月建议。"
  artifacts:
    - path: "three-kingdoms-simulator/scripts/systems/AppointmentResolver.gd"
      provides: "五层原因树与候选竞争结算"
    - path: "three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd"
      provides: "月报/任命 UI 消费的 explainable result payload"
  key_links:
    - from: "three-kingdoms-simulator/scripts/autoload/GameRoot.gd"
      to: "three-kingdoms-simulator/scripts/systems/AppointmentResolver.gd"
      via: "month-end pipeline"
      pattern: "settle_month_task.*finalize_month_snapshot.*evaluate_month_end"
    - from: "three-kingdoms-simulator/scripts/systems/AppointmentResolver.gd"
      to: "three-kingdoms-simulator/scripts/systems/FactionSystem.gd"
      via: "consume frozen faction overview / bloc query outputs from 03-03"
      pattern: "get_player_position_summary|get_bloc_rows|get_resource_summary"
    - from: "three-kingdoms-simulator/scripts/systems/AppointmentResolver.gd"
      to: "three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd"
      via: "headline + reason lines + next hint"
      pattern: "earliest blocking layer|reason_lines|next_goal_hint"
---

<objective>
把 Phase 2.1 的阈值式升官反馈升级为“五层原因树 + 派系修正 + 有限多候选竞争”的 explainable appointment resolver。

Purpose: 让玩家能区分“没资格”“没空缺”“没人推”“有人压”“竞争落败”，并把 03-03 已冻结的政治快照 / faction query contracts 真正接入月末主链。
Output: `AppointmentResolver.gd`、扩展后的月末结果对象、运行时缓存接线，以及成功/失败/落败三类回归。
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-仕途、势力与可解释政治/03-CONTEXT.md
@.planning/phases/03-仕途、势力与可解释政治/03-RESEARCH.md
@.planning/phases/02.1-/02.1-05-SUMMARY.md
@three-kingdoms-simulator/scripts/autoload/GameRoot.gd
@three-kingdoms-simulator/scripts/systems/CareerSystem.gd
@three-kingdoms-simulator/scripts/systems/FactionSystem.gd
@three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
@three-kingdoms-simulator/scripts/runtime/AppointmentCandidateEvaluation.gd

<interfaces>
Current Phase 2.1 promotion result:
```gdscript
func evaluate_promotion(session: GameSession, repository: Node, settlement: Dictionary) -> Dictionary
```

Expected 03-03 faction query layer consumed here:
```gdscript
func get_player_position_summary(session: GameSession) -> Dictionary
func get_bloc_rows(faction_id: String, session: GameSession) -> Array[Dictionary]
func get_faction_overview(faction_id: String, session: GameSession) -> Dictionary
func get_resource_summary(faction_id: String) -> Dictionary
```

Current month-end order in `GameRoot._process_month_end_evaluation()`:
```gdscript
settle_month_task -> merit/fame/trust writeback -> CareerSystem.evaluate_promotion()
-> set_last_month_evaluation(MonthlyEvaluationResult.create(...))
```

Phase 3 must keep the same high-level order, but insert `PoliticalSupportSnapshot finalize -> AppointmentResolver candidate comparison` between writeback and office consequence application.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the five-stage appointment resolver on top of the faction and political query layers</name>
  <files>
three-kingdoms-simulator/scripts/systems/AppointmentResolver.gd
  </files>
  <read_first>
- .planning/phases/03-仕途、势力与可解释政治/03-CONTEXT.md
- .planning/phases/03-仕途、势力与可解释政治/03-RESEARCH.md
- three-kingdoms-simulator/scripts/systems/FactionSystem.gd
- three-kingdoms-simulator/scripts/runtime/AppointmentCandidateEvaluation.gd
- three-kingdoms-simulator/scripts/runtime/PoliticalSupportSnapshot.gd
- three-kingdoms-simulator/data/politics/recommendations/reco_cao_cao_admin_success.tres
  </read_first>
  <behavior>
    - The resolver compares the player with only 1–2 AI candidates for one vacancy; no full-realm ranking table is built.
    - Headline stage order is fixed to qualification → vacancy/system → recommendation → opposition → competition per D-01 and D-03.
    - UI-facing output contains one top-line conclusion, 2–3 major reason lines, and one next-month hint per D-02.
  </behavior>
  <action>Implement `AppointmentResolver.gd` so it consumes `PoliticalSupportSnapshot`, player career state, vacancy state, and the thin `FactionSystem` outputs already created in 03-03 to emit `AppointmentCandidateEvaluation` objects and a final month-end conclusion. Keep the five-stage explanation tree strict and deterministic, choose the top-line failure by the earliest blocking stage, and reuse the frozen 2 recommendation + 2 opposition + 3 bloc sample set from 03-03 rather than recalibrating data again here. Do not build a ruler console, nationwide politics graph, or more than 2–3 bloc attitudes.</action>
  <acceptance_criteria>
- `rg -n "class_name AppointmentResolver|qualification|vacancy|recommendation|opposition|competition|next_goal_hint" "three-kingdoms-simulator/scripts/systems/AppointmentResolver.gd"` returns matches.
- `rg -n "competition_rank|reason_lines|final_decision|evaluation_status" "three-kingdoms-simulator/scripts/runtime/AppointmentCandidateEvaluation.gd"` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "rg -n 'class_name AppointmentResolver|qualification|vacancy|recommendation|opposition|competition|next_goal_hint' 'three-kingdoms-simulator/scripts/systems/AppointmentResolver.gd'; rg -n 'competition_rank|reason_lines|final_decision|evaluation_status' 'three-kingdoms-simulator/scripts/runtime/AppointmentCandidateEvaluation.gd'"</automated>
  </verify>
  <done>月末任命比较不再只有单阈值 if/else，而是具备有限候选比较、bloc 修正、五层理由分解的独立后端模块，并明确消费 03-03 冻结的 faction/politics 查询层。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace simplified promotion output with explainable month-end evaluation flow and runtime snapshot persistence</name>
  <files>
three-kingdoms-simulator/scripts/systems/CareerSystem.gd
three-kingdoms-simulator/scripts/runtime/PlayerCareerState.gd
three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
three-kingdoms-simulator/scripts/runtime/GameSession.gd
three-kingdoms-simulator/scripts/autoload/GameRoot.gd
three-kingdoms-simulator/scripts/tests/phase3_appointment_resolver_regression.gd
  </files>
  <read_first>
- three-kingdoms-simulator/scripts/systems/CareerSystem.gd
- three-kingdoms-simulator/scripts/runtime/PlayerCareerState.gd
- three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
- three-kingdoms-simulator/scripts/runtime/GameSession.gd
- three-kingdoms-simulator/scripts/autoload/GameRoot.gd
- three-kingdoms-simulator/scripts/tests/phase21_monthly_career_regression.gd
- .planning/phases/03-仕途、势力与可解释政治/03-CONTEXT.md
  </read_first>
  <behavior>
    - Existing month-end order stays: task settlement first, promotion popup second; Phase 3 only deepens the explanation layer.
    - `GameRoot` now performs the month-end `finalize_month_snapshot()` integration and persists the finalized snapshot/evaluation into runtime state after 03-03 deliberately kept wiring out.
    - `MonthlyEvaluationResult` can carry success, earliest-blocked failure, and lost-to-rival cases with the same UI-readable shape.
    - Regression proves one success path, one earliest-stage failure path, and one competition-loss path.
  </behavior>
  <action>Refactor `CareerSystem.gd` so it keeps qualification and office writeback responsibilities, but delegates multi-candidate ranking and reason-tree assembly to `AppointmentResolver`. Extend `PlayerCareerState.gd`, `GameSession.gd`, and `MonthlyEvaluationResult.gd` to persist the finalized political snapshot, candidate evaluations, `appointment_result`, `primary_support_lines`, `primary_blocker_lines`, `missed_opportunity_note`, `next_month_political_hint`, and any office consequence tags needed later by UI. Update `GameRoot.gd` to keep the Phase 2.1 sequence (`task settlement -> writeback -> month report -> promotion popup`) while inserting `PoliticalSystem.finalize_month_snapshot()` and `AppointmentResolver.evaluate_month_end()` before the final result object is built. Add `phase3_appointment_resolver_regression.gd` covering: successful appointment, failure at the earliest blocking layer, and losing a vacancy to a rival candidate. Do not regress to the old four-label-only backend.</action>
  <acceptance_criteria>
- `rg -n "appointment_result|candidate_evaluation_results|primary_support_lines|primary_blocker_lines|missed_opportunity_note|next_month_political_hint" "three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd"` returns matches.
- `rg -n "finalize_month_snapshot|evaluate_month_end|candidate_evaluation_results" "three-kingdoms-simulator/scripts/autoload/GameRoot.gd"` returns matches.
- `phase3_appointment_resolver_regression.gd` exits 0 and proves success, earliest-blocked failure, and competition-loss scenarios.
  </acceptance_criteria>
  <verify>
    <automated>& "D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe" --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase3_appointment_resolver_regression.gd"</automated>
  </verify>
  <done>Phase 3 月末任命已经能输出“为什么成/为什么败/输给了谁/下月该补什么”的统一 explainable payload，同时没有打破 Phase 2.1 的月报先行顺序，并正式接上 03-03 预先缓存的 snapshot。</done>
</task>

</tasks>

<verification>
通过 `phase3_appointment_resolver_regression.gd` 锁住三类关键真值：成功样本、最早阻断样本、竞争落败样本。</verification>

<success_criteria>
- 玩家可以把月末结果理解成政治过程，而不是黑箱阈值。
- 派系态度与推荐/反对已经真实影响最终任命，而不是只显示在文案里。
</success_criteria>

<output>
After completion, create `.planning/phases/03-仕途、势力与可解释政治/03-04-SUMMARY.md`
</output>
