---
phase: 03-仕途、势力与可解释政治
plan: "01"
type: execute
wave: 1
depends_on:
  - 02.1-08
files_modified:
  - three-kingdoms-simulator/scripts/data/resources/RecommendationRuleData.gd
  - three-kingdoms-simulator/scripts/data/resources/OppositionRuleData.gd
  - three-kingdoms-simulator/scripts/data/resources/FactionBlocData.gd
  - three-kingdoms-simulator/scripts/runtime/PoliticalReasonLine.gd
  - three-kingdoms-simulator/scripts/runtime/PoliticalSupportSnapshot.gd
  - three-kingdoms-simulator/scripts/runtime/AppointmentCandidateEvaluation.gd
  - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
  - three-kingdoms-simulator/scripts/tests/phase3_contract_regression.gd
autonomous: true
requirements:
  - RELA-04
  - CARE-04
  - FACT-03
  - POLI-01
  - POLI-02
  - POLI-03
must_haves:
  truths:
    - "月初任务、月内政治摘要与月末任命反馈可以围绕同一组推荐 / 阻力 / 派系态度事实展开，不会各说各话。"
    - "同一次政治结果可以稳定产出顶层结论、关键原因行与下月建议所需字段。"
  artifacts:
    - path: "three-kingdoms-simulator/scripts/runtime/PoliticalSupportSnapshot.gd"
      provides: "月内政治快照 DTO"
    - path: "three-kingdoms-simulator/scripts/runtime/AppointmentCandidateEvaluation.gd"
      provides: "候选比较与五层解释 DTO"
    - path: "three-kingdoms-simulator/scripts/autoload/DataRepository.gd"
      provides: "政治规则与派系块加载入口"
  key_links:
    - from: "three-kingdoms-simulator/scripts/autoload/DataRepository.gd"
      to: "res://data/politics/recommendations"
      via: "_load_resources_by_id + getter methods"
      pattern: "get_recommendation_rules|get_opposition_rules|get_faction_blocs"
    - from: "three-kingdoms-simulator/scripts/runtime/AppointmentCandidateEvaluation.gd"
      to: "three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd"
      via: "shared reason-line and next-hint payload contract"
      pattern: "reason_lines|final_decision|next_goal_hint"
---

<objective>
冻结 Phase 3 的政治静态合同与运行时合同，让后续 6 个计划都建立在同一份 explainable-politics 数据骨架上。

Purpose: 先锁字段、命名和载入入口，避免后续继续猜“推荐/反对/派系/原因行”该放哪一层，确保 Phase 3 是扩展 Phase 2.1，而不是重做月循环。
Output: 新政治 Resource 合同、核心运行时 DTO、`DataRepository` 查询入口、以及一份 DTO 回归脚本。
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-仕途、势力与可解释政治/03-CONTEXT.md
@.planning/phases/03-仕途、势力与可解释政治/03-RESEARCH.md
@.planning/phases/03-仕途、势力与可解释政治/03-UI-SPEC.md
@.planning/phases/02.1-/02.1-01-SUMMARY.md
@three-kingdoms-simulator/scripts/autoload/DataRepository.gd
@three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd

<interfaces>
From `three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd`:
```gdscript
var task_result: String = ""
var summary_lines: Array[String] = []
var next_goal_hint: String = ""
var promotion_missing_values: Dictionary = {}
var promotion_failure_label: String = ""
```

Phase 3 contracts must feed this file later instead of introducing a second month-end result object.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze static political rule resources and repository loaders</name>
  <files>
three-kingdoms-simulator/scripts/data/resources/RecommendationRuleData.gd
three-kingdoms-simulator/scripts/data/resources/OppositionRuleData.gd
three-kingdoms-simulator/scripts/data/resources/FactionBlocData.gd
three-kingdoms-simulator/scripts/autoload/DataRepository.gd
  </files>
  <read_first>
- .planning/phases/03-仕途、势力与可解释政治/03-CONTEXT.md
- .planning/phases/03-仕途、势力与可解释政治/03-RESEARCH.md
- three-kingdoms-simulator/scripts/autoload/DataRepository.gd
- three-kingdoms-simulator/scripts/data/resources/OfficeData.gd
- three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd
  </read_first>
  <behavior>
    - `RecommendationRuleData` exposes `id`, `source_type`, `trigger_phase`, `target_scope`, relation/trust/merit thresholds, bloc filters, `support_delta`, `reason_text_key`, `priority`, `sort_order`.
    - `OppositionRuleData` exposes parallel blocker fields plus `opposition_delta`, `blocker_tags`, `reason_text_key`, `priority`.
    - `FactionBlocData` exposes `id`, `faction_id`, `name`, `bloc_type`, `core_character_ids`, `influence_weight`, `agenda_tags`, `default_attitude`.
  </behavior>
  <action>Per D-01, D-02, D-03 and the 03-RESEARCH data-contract section, create the three new `Resource` classes as typed Godot contracts under `scripts/data/resources/`. Then extend `DataRepository.gd` to load `.tres` files from `res://data/politics/recommendations`, `res://data/politics/oppositions`, and `res://data/factions/blocs`, exposing query methods such as `get_recommendation_rules()`, `get_opposition_rules()`, and `get_faction_blocs_for_faction(faction_id)`. Keep the existing Phase 2.1 generated-JSON path for offices/tasks/actions unchanged; new political rules are additive and must not replace the current `TaskTemplateData` / `OfficeData` source path. Do not introduce a second repository or UI-local cache.</action>
  <acceptance_criteria>
- `rg -n "class_name RecommendationRuleData|support_delta|reason_text_key" "three-kingdoms-simulator/scripts/data/resources/RecommendationRuleData.gd"` returns matches.
- `rg -n "class_name OppositionRuleData|opposition_delta|blocker_tags" "three-kingdoms-simulator/scripts/data/resources/OppositionRuleData.gd"` returns matches.
- `rg -n "class_name FactionBlocData|default_attitude|core_character_ids" "three-kingdoms-simulator/scripts/data/resources/FactionBlocData.gd"` returns matches.
- `rg -n "get_recommendation_rules|get_opposition_rules|get_faction_blocs_for_faction|data/politics|data/factions/blocs" "three-kingdoms-simulator/scripts/autoload/DataRepository.gd"` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "rg -n 'class_name RecommendationRuleData|support_delta|reason_text_key' 'three-kingdoms-simulator/scripts/data/resources/RecommendationRuleData.gd'; rg -n 'class_name OppositionRuleData|opposition_delta|blocker_tags' 'three-kingdoms-simulator/scripts/data/resources/OppositionRuleData.gd'; rg -n 'class_name FactionBlocData|default_attitude|core_character_ids' 'three-kingdoms-simulator/scripts/data/resources/FactionBlocData.gd'; rg -n 'get_recommendation_rules|get_opposition_rules|get_faction_blocs_for_faction|data/politics|data/factions/blocs' 'three-kingdoms-simulator/scripts/autoload/DataRepository.gd'"</automated>
  </verify>
  <done>静态政治规则有正式 typed contract，`DataRepository` 可以成为后续任务来源、派系态度与任命解释的唯一规则读取入口。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create core Phase 3 DTOs for political snapshots, reason lines, and candidate evaluation</name>
  <files>
three-kingdoms-simulator/scripts/runtime/PoliticalReasonLine.gd
three-kingdoms-simulator/scripts/runtime/PoliticalSupportSnapshot.gd
three-kingdoms-simulator/scripts/runtime/AppointmentCandidateEvaluation.gd
three-kingdoms-simulator/scripts/tests/phase3_contract_regression.gd
  </files>
  <read_first>
- three-kingdoms-simulator/scripts/runtime/MonthlyEvaluationResult.gd
- .planning/phases/03-仕途、势力与可解释政治/03-RESEARCH.md
  </read_first>
  <behavior>
    - `PoliticalReasonLine` is the only reason-unit contract later plans may pass into HUD / 月报 / 任命反馈.
    - `PoliticalSupportSnapshot` carries recommenders, opposers, bloc attitudes, blockers, and opportunities in one JSON-safe shape.
    - `AppointmentCandidateEvaluation` carries one vacancy decision with layered reasons and one next-goal hint, ready to be embedded into the existing `MonthlyEvaluationResult` later.
  </behavior>
  <action>Create `PoliticalReasonLine.gd`, `PoliticalSupportSnapshot.gd`, and `AppointmentCandidateEvaluation.gd` as typed `RefCounted` DTOs with `create(...)` helpers and JSON-safe arrays/dictionaries. Freeze field names to match the 03-RESEARCH contract so later plans can extend `MonthlyTaskState`, `PlayerCareerState`, `GameSession`, and `MonthlyEvaluationResult` against these DTOs without renaming. Add `phase3_contract_regression.gd` to instantiate the DTOs, assert their arrays/dictionaries duplicate safely, and prove one candidate evaluation can produce one top-line decision plus 2–3 visible reason lines and one next-goal hint. Do not create UI-only structures or a second month-end payload object.</action>
  <acceptance_criteria>
- `rg -n "class_name PoliticalReasonLine|class_name PoliticalSupportSnapshot|class_name AppointmentCandidateEvaluation" "three-kingdoms-simulator/scripts/runtime"` returns matches.
- `phase3_contract_regression.gd` exits 0 with the explicit Godot console path.
  </acceptance_criteria>
  <verify>
    <automated>& "D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe" --headless --path "three-kingdoms-simulator" --script "res://scripts/tests/phase3_contract_regression.gd"</automated>
  </verify>
  <done>Phase 3 的核心政治 DTO 已冻结，后续计划只需要把这些合同接入既有月度主链，而不必再猜字段名或重造解释结构。</done>
</task>

</tasks>

<verification>
通过 `phase3_contract_regression.gd` 证明 Phase 3 新 DTO 与 Phase 2.1 既有月度 DTO 可以共存，且 `DataRepository` 已有明确的政治规则查询入口。</verification>

<success_criteria>
- Phase 3 关键字段不再散落在 UI 文案或临时 Dictionary 中。
- 后续 03-02 至 03-07 可以直接消费同一份静态与运行时合同，不再猜字段名或再造结果对象。
</success_criteria>

<output>
After completion, create `.planning/phases/03-仕途、势力与可解释政治/03-01-SUMMARY.md`
</output>
