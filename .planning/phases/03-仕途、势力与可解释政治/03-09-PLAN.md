---
phase: 03-仕途、势力与可解释政治
plan: "09"
type: execute
wave: 1
depends_on:
  - 03-06
files_modified:
  - three-kingdoms-simulator/scripts/tests/phase3_politics_hud_regression.gd
  - three-kingdoms-simulator/scenes/main/MainScene.tscn
  - three-kingdoms-simulator/themes/PrototypeTheme.tres
autonomous: true
requirements:
  - FACT-01
  - FACT-03
gap_closure: true
must_haves:
  truths:
    - "势力总览 popup 与人物详情 popup 打开后都是不透明面板，不会透出底层 HUD。"
    - "玩家仍能从势力总览下钻到人物详情，不改变单场景 popup 流程。"
  artifacts:
    - path: "three-kingdoms-simulator/themes/PrototypeTheme.tres"
      provides: "共享 PopupPanel 不透明样式"
    - path: "three-kingdoms-simulator/scenes/main/MainScene.tscn"
      provides: "FactionPanel / CharacterProfilePanel 的不透明 popup 配置"
    - path: "three-kingdoms-simulator/scripts/tests/phase3_politics_hud_regression.gd"
      provides: "不透明 popup 与下钻链路回归"
  key_links:
    - from: "three-kingdoms-simulator/themes/PrototypeTheme.tres"
      to: "three-kingdoms-simulator/scenes/main/MainScene.tscn"
      via: "PopupPanel panel style + transparency flags"
      pattern: "PopupPanel|transparent_bg = false|transparent = false"
    - from: "three-kingdoms-simulator/scripts/tests/phase3_politics_hud_regression.gd"
      to: "three-kingdoms-simulator/scenes/main/MainScene.tscn"
      via: "FactionButton -> officer row -> CharacterProfilePanel"
      pattern: "FactionPanel|CharacterProfilePanel"
---

<objective>
修复势力总览与人物详情 popup 的透明外观，让 Phase 3 的政治详情保持同一套不透明卡片视觉。

Purpose: 保住 D-08/D-09 的“详细政治信息放进 popup/detail panel”路线，同时把当前默认半透明 PopupPanel 外观收敛到项目共享 Theme，而不是继续靠裸节点默认样式。
Output: 新的 popup 样式回归，以及接入共享 Theme 的不透明 FactionPanel / CharacterProfilePanel。
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-仕途、势力与可解释政治/03-UAT.md
@.planning/phases/03-仕途、势力与可解释政治/03-CONTEXT.md
@.planning/phases/03-仕途、势力与可解释政治/03-06-SUMMARY.md
@.planning/debug/opaque-faction-and-character-popups.md
@three-kingdoms-simulator/scenes/main/MainScene.tscn
@three-kingdoms-simulator/themes/PrototypeTheme.tres
@three-kingdoms-simulator/scripts/tests/phase3_politics_hud_regression.gd

<interfaces>
Current reference node that already has the desired opaque treatment:
```tscn
[node name="TaskSelectPanel" type="PopupPanel" parent="."]
transparent_bg = false
transparent = false
theme_override_styles/panel = SubResource("StyleBoxFlat_hi3im")
```

Current problematic nodes are plain popups:
```tscn
[node name="CharacterProfilePanel" type="PopupPanel" parent="."]
[node name="FactionPanel" type="PopupPanel" parent="."]
```

Shared theme currently has no PopupPanel style fallback.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the Phase 3 HUD regression to require opaque faction and character popups</name>
  <files>three-kingdoms-simulator/scripts/tests/phase3_politics_hud_regression.gd</files>
  <behavior>
    - The regression proves `FactionPanel` opens from `FactionButton` and `CharacterProfilePanel` still opens from an officer row.
    - Both popup nodes must assert non-transparent flags and an opaque panel style source.
    - The test continues to cover the existing single-scene drilldown flow.
  </behavior>
  <action>Update `phase3_politics_hud_regression.gd` so it stops validating only copy presence and also validates the diagnosed visual contract: `FactionPanel` and `CharacterProfilePanel` must have transparency disabled and an opaque panel style available from the scene/theme. Keep the officer drilldown assertions in the same script so this gap stays tied to the actual Phase 3 user flow rather than a disconnected style-only smoke test.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase3_politics_hud_regression.gd'; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>UI 回归已锁定“不透明 popup + 可下钻人物”的新真值，并在当前实现上先失败。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move faction and character popup styling onto the shared opaque PopupPanel theme path</name>
  <files>
three-kingdoms-simulator/scenes/main/MainScene.tscn
three-kingdoms-simulator/themes/PrototypeTheme.tres
  </files>
  <behavior>
    - `FactionPanel` and `CharacterProfilePanel` render as opaque popups.
    - They reuse one shared style source instead of duplicating ad-hoc node-local styling.
    - The FactionButton → officer row → CharacterProfilePanel flow remains unchanged.
  </behavior>
  <action>Implement the diagnosed fix by moving an opaque PopupPanel style into `PrototypeTheme.tres` and wiring `FactionPanel` plus `CharacterProfilePanel` in `MainScene.tscn` to use it with `transparent_bg = false` and `transparent = false`. Mirror the established TaskSelectPanel look-and-feel rather than inventing a new popup skin, so the project keeps the Phase 1 shared-theme pattern. Do not change navigation architecture, popup sizes, or drilldown behavior; this gap is visual consistency only, per D-08/D-09 and the UAT report.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase3_politics_hud_regression.gd'"</automated>
  </verify>
  <done>势力面板和人物详情面板已经统一为不透明 popup，且共享主题中存在可复用的样式兜底。</done>
</task>

</tasks>

<verification>
运行 `phase3_politics_hud_regression.gd`，确认 FactionButton 打开的 popup 与人物详情 drilldown 都使用不透明面板，并保持原有交互链路。</verification>

<success_criteria>
- 玩家在势力总览与人物详情里读取政治信息时，不会被底层 HUD 透视干扰。
- Popup 视觉规则已上收到共享 Theme，不再依赖裸 PopupPanel 默认样式。
</success_criteria>

<output>
After completion, create `.planning/phases/03-仕途、势力与可解释政治/03-09-SUMMARY.md`
</output>
