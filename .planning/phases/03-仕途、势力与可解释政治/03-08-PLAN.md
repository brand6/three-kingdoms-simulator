---
phase: 03-仕途、势力与可解释政治
plan: "08"
type: execute
wave: 1
depends_on:
  - 03-06
  - 03-07
files_modified:
  - three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
  - three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
autonomous: true
requirements:
  - CARE-01
  - POLI-03
gap_closure: true
must_haves:
  truths:
    - "月初任务卡首屏可直接扫读任务名、来源、请求方、目标、奖励，以及机遇/风险标签。"
    - "未选中任务前确认按钮保持禁用，选中后才可确认。"
  artifacts:
    - path: "three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd"
      provides: "符合 UAT 新布局的结构化任务卡渲染"
    - path: "three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd"
      provides: "锁定新任务卡展示契约的回归测试"
  key_links:
    - from: "three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd"
      to: "three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd"
      via: "月初首张任务卡文本/结构断言"
      pattern: "来源：|请求方：|机遇和风险"
    - from: "three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd"
      to: "GameRoot month-start candidates"
      via: "request_character_id + political tags 渲染"
      pattern: "request_character_id|political_reward_tags|political_risk_tags"
---

<objective>
修复月初任务卡的展示契约，让任务来源信息更易扫读，并与最新 UAT 反馈一致。

Purpose: 保住 D-05/D-06 的“可扫读、不数值化”目标，同时把当前被旧文案锁死的来源字段、单行布局和机遇/风险表达改成用户确认过的新样式。
Output: 更新后的任务卡回归测试，以及不再依赖纯 `Button.text` 旧模板的任务卡渲染。
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-仕途、势力与可解释政治/03-UAT.md
@.planning/phases/03-仕途、势力与可解释政治/03-CONTEXT.md
@.planning/phases/03-仕途、势力与可解释政治/03-02-SUMMARY.md
@.planning/phases/03-仕途、势力与可解释政治/03-06-SUMMARY.md
@.planning/debug/political-task-card-layout.md
@three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
@three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd

<interfaces>
Current task card contract is locked by these old strings:
```gdscript
return "%s\n来源类型：%s\n请求方：%s\n关联派系：%s\n目标：%s\n预计奖励：%s\n政治标签：%s"
```

Current regression helper already supports either a button or a child label body:
```gdscript
func _first_task_card_text(picker: Node) -> String:
	var first_card := container.get_child(0)
	if first_card is BaseButton:
		return first_card.text
	var body := first_card.get_node_or_null("BodyLabel") as Label
	return body.text if body != null else ""
```

Exploit that flexibility: the new card can stay selectable, but must stop depending on one hard-coded plain-text block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the inherited month-start regression to the new task-card contract</name>
  <files>three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd</files>
  <behavior>
    - The first card no longer requires a standalone `来源类型：` row.
    - The visible copy checks for one header line containing task name + `来源：` + `请求方：`.
    - The card still shows `目标：`, `预计奖励：`, and a `机遇和风险` block, but it must reject literal `机会:` / `风险:` prefixes from the old contract.
    - Confirm CTA remains visible-but-disabled before selection, and the area above it stays empty.
  </behavior>
  <action>Update `phase21_monthly_hud_regression.gd` first so it encodes the diagnosed UAT gap instead of the obsolete 03-06 contract. Per D-05/D-06 and the diagnosed gap, keep the test focused on scanability rather than raw backend labels: expect `来源` instead of `关联派系`, expect task name / 来源 / 请求方 in the same first line, and assert the card exposes `机遇和风险` without the old `机会:` / `风险:` textual prefixes. Keep all existing month-start gating assertions unchanged. Make the helper tolerant of a structured card or rich-text body so Task 2 can replace the plain `Button.text` renderer without rewriting the regression again.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase21_monthly_hud_regression.gd'; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>回归脚本已锁定新任务卡契约，并且在旧 renderer 上会先失败，明确暴露本次 gap。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the old plain-text task-card renderer with the new single-line source layout</name>
  <files>three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd</files>
  <behavior>
    - Cards remain clickable monthly-task selectors, not tables and not scene changes.
    - The first visible line presents task name, `来源`, and `请求方` together.
    - `来源类型` is removed as its own line; `关联派系` is renamed to `来源`.
    - `机遇和风险` uses color-distinguished tag text or equivalent structured styling, with no literal `机会:` / `风险:` prefixes.
  </behavior>
  <action>Refactor `TaskSelectPanel.gd` per the diagnosed gap and D-05/D-06. Keep the button-style selection behavior established by 03-06, but stop rendering the card as one hard-coded multiline `Button.text` block. Use structured child controls or `RichTextLabel` so the first row can place task名 / 来源 / 请求方 on one line, the old `来源类型` row disappears, `关联派系` becomes `来源`, and `机遇和风险` is visually separated by color rather than the old `机会:` / `风险:` wording. Preserve the existing confirm-button gating, popup sizing, and empty-state behavior; do not reintroduce the removed reward/info strip above the confirm CTA.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase21_monthly_hud_regression.gd'"</automated>
  </verify>
  <done>月初任务卡已按 UAT 新布局展示政治来源信息，且旧回归已升级为新契约并通过。</done>
</task>

</tasks>

<verification>
运行 `phase21_monthly_hud_regression.gd`，确认新任务卡契约与既有月初门控、月报顺序没有一起回归。</verification>

<success_criteria>
- 玩家打开月初任务选择时，能直接比较“这件事是谁递来的、会带来什么机遇和风险”。
- 任务卡不再被旧版 `来源类型 / 关联派系 / 政治标签` 文案锁死。
</success_criteria>

<output>
After completion, create `.planning/phases/03-仕途、势力与可解释政治/03-08-SUMMARY.md`
</output>
