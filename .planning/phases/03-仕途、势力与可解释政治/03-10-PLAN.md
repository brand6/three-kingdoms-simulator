---
phase: 03-仕途、势力与可解释政治
plan: "10"
type: execute
wave: 8
depends_on:
  - 03-02
  - 03-08
files_modified:
  - three-kingdoms-simulator/scripts/tests/phase3_task_source_regression.gd
  - three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
  - three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd
  - three-kingdoms-simulator/scripts/autoload/DataRepository.gd
  - three-kingdoms-simulator/scripts/systems/TaskSystem.gd
  - three-kingdoms-simulator/scripts/runtime/MonthlyTaskState.gd
  - three-kingdoms-simulator/data/generated/190/task_templates.json
  - three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
autonomous: true
requirements:
  - CARE-01
  - POLI-03
gap_closure: true
must_haves:
  truths:
    - "月初任务卡中的来源显示为权力机构名称，而不是请求方姓名或来源摘要。"
    - "月初任务卡中的请求方显示为具体下达任务的人，并与来源机构语义分离。"
    - "任务卡在当前 popup 宽度下具备更稳定的边距与可读性，同时仍保持未选中前确认按钮禁用。"
  artifacts:
    - path: "three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd"
      provides: "独立的任务来源机构字段合同"
    - path: "three-kingdoms-simulator/scripts/runtime/MonthlyTaskState.gd"
      provides: "冻结来源机构与请求方语义的月任务快照"
    - path: "three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd"
      provides: "正确区分来源机构/请求方且经过二次版式优化的任务卡"
    - path: "three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd"
      provides: "锁定来源机构/请求方语义与卡片排版契约的月初回归"
  key_links:
    - from: "three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd"
      to: "three-kingdoms-simulator/scripts/systems/TaskSystem.gd"
      via: "authority institution field loaded into candidate payload"
      pattern: "authority_institution|source_institution|institution"
    - from: "three-kingdoms-simulator/scripts/systems/TaskSystem.gd"
      to: "three-kingdoms-simulator/scripts/runtime/MonthlyTaskState.gd"
      via: "freeze_source_snapshot preserves institution/requester separation"
      pattern: "freeze_source_snapshot|request_character_id|issuer_character_id"
    - from: "three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd"
      to: "three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd"
      via: "header source/requester text + card spacing assertions"
      pattern: "来源：|请求方：|content_margin|custom_minimum_size"
---

<objective>
修复 03-08 后续暴露的任务卡语义与版式缺口：来源必须是权力机构、请求方必须是具体下达人，并让卡片排版再做一轮可读性收敛。

Purpose: 落实 D-04/D-05/D-06 的“可扫读政治信息”目标，避免再次把人物、机构、摘要三种语义混成同一字段，同时把 UAT 指出的边距/文字拥挤问题转成稳定契约。
Output: 新的任务来源机构字段链路、更新后的回归测试，以及正确显示来源/请求方并改进边距排版的月初任务卡。
</objective>

<execution_context>
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/workflows/execute-plan.md
@D:/Projects/Godot/三国模拟器/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-仕途、势力与可解释政治/03-UAT.md
@.planning/phases/03-仕途、势力与可解释政治/03-CONTEXT.md
@.planning/phases/03-仕途、势力与可解释政治/03-02-SUMMARY.md
@.planning/phases/03-仕途、势力与可解释政治/03-08-SUMMARY.md
@.planning/debug/task-card-source-requester-layout.md
@three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd
@three-kingdoms-simulator/scripts/runtime/MonthlyTaskState.gd
@three-kingdoms-simulator/scripts/systems/TaskSystem.gd
@three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd
@three-kingdoms-simulator/scripts/tests/phase3_task_source_regression.gd
@three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd

<interfaces>
Current task template/source contract has no dedicated institution field:
```gdscript
@export var task_source_type: String = "faction_order"
@export var request_character_id: String = ""
@export var related_bloc_id: String = ""
@export var source_summary: String = ""
```

Current candidate payload also lacks a separate institution value:
```gdscript
return {
  "task_source_type": str(task.task_source_type),
  "request_character_id": str(task.request_character_id),
  "related_bloc_id": str(task.related_bloc_id),
  "source_summary": str(task.source_summary),
}
```

Current header conflates source with requester-derived text:
```gdscript
var requester_name := _resolve_character_name(repository, str(candidate.get("request_character_id", "")))
var source_type := _localized_source_type(str(candidate.get("task_source_type", "faction_order")))
var source_target := _source_target_text(candidate, repository)
return "%s｜来源：%s · %s｜请求方：%s" % [task_name, source_type, source_target, requester_name]
```

Current frozen snapshot preserves requester but not institution:
```gdscript
state.task_source_type = str(candidate.get("task_source_type", ""))
state.request_character_id = str(candidate.get("request_character_id", ""))
state.related_bloc_id = str(candidate.get("related_bloc_id", ""))
state.source_summary = str(candidate.get("source_summary", ""))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite regressions around institution-vs-requester semantics and card readability</name>
  <files>
three-kingdoms-simulator/scripts/tests/phase3_task_source_regression.gd
three-kingdoms-simulator/scripts/tests/phase21_monthly_hud_regression.gd
  </files>
  <behavior>
    - Candidate payload and frozen `MonthlyTaskState` expose one dedicated institution field distinct from requester semantics.
    - Month-start UI regression expects `来源` to read an authority body name such as `尚书台` / `军功集团` / `宗族长老会`, not a character name.
    - `请求方` resolves to the concrete issuing person (`request_character_id` first, `issuer_character_id` fallback).
    - The UI regression also locks a second-pass readability contract through card min-height and content-margin assertions rather than copy-only checks.
  </behavior>
  <action>Update both regressions first so the new gap is encoded before implementation. In `phase3_task_source_regression.gd`, require a single canonical institution field name to exist in candidate payloads and in `MonthlyTaskState` snapshots. In `phase21_monthly_hud_regression.gd`, strengthen the first-card assertions so `来源` must equal an institution label instead of a requester-derived string, `请求方` must show a concrete issuer, and the card style must expose stronger padding/readability than the cramped 03-08 baseline. Keep all existing month-start gating, confirm-button, and month-end sequence assertions intact per D-04/D-05/D-06.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase21_monthly_hud_regression.gd'; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>两份回归脚本已锁定“来源=机构、请求方=具体下达人、卡片排版更可读”的新真值，并在当前实现上先失败。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread an explicit authority-institution field through task data, payloads, and frozen snapshots</name>
  <files>
three-kingdoms-simulator/scripts/data/resources/TaskTemplateData.gd
three-kingdoms-simulator/scripts/autoload/DataRepository.gd
three-kingdoms-simulator/scripts/systems/TaskSystem.gd
three-kingdoms-simulator/scripts/runtime/MonthlyTaskState.gd
three-kingdoms-simulator/data/generated/190/task_templates.json
  </files>
  <behavior>
    - Task definitions, candidate payloads, and frozen month-task snapshots all carry one explicit authority-institution field.
    - `request_character_id` remains requester-only semantics; faction-order tasks fall back to `issuer_character_id` when no request person exists.
    - Existing source-mix, political tags, and snapshot freezing behavior from 03-02 remain intact.
  </behavior>
  <action>Add one explicit authority institution text field to the task template contract and keep that exact field name consistent across `TaskTemplateData`, `DataRepository`, `TaskSystem`, and `MonthlyTaskState` (for example `authority_institution_name`). Update the four generated task samples so source institutions read as power bodies rather than people: keep `task_document_cleanup` and `task_grain_audit` under `尚书台`, and assign the relation-led samples institution labels that reflect the political body behind the request (for example `军功集团` and `宗族长老会`) while keeping the concrete person in requester semantics. Do not overload `related_bloc_id` or `source_summary` to fake this field. Preserve 03-02's `ensure_diversity` behavior and freeze the new institution field into month state for later reuse.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase3_task_source_regression.gd'"</automated>
  </verify>
  <done>后端合同已经能稳定区分来源机构与请求方，且任务来源回归通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render institution-based task cards with concrete requester and improved typography</name>
  <files>three-kingdoms-simulator/scripts/ui/TaskSelectPanel.gd</files>
  <behavior>
    - Header shows `任务名｜来源：{机构名}｜请求方：{具体人物}` in one line and never reuses requester as source.
    - Body keeps `目标` / `预计奖励` / `机遇和风险` with colored tags per D-05.
    - Card readability improves through a deliberate spacing pass (padding, min height, section rhythm) without reintroducing the old info block above the confirm CTA.
  </behavior>
  <action>Refine `TaskSelectPanel.gd` so source rendering reads directly from the explicit authority field from Task 2, while requester uses `request_character_id` first and `issuer_character_id` as the fallback concrete issuer. Remove requester-derived source fallback logic entirely; `来源` must display only the institution body, not a person name or parsed summary. Keep the 03-08 structured-card pattern and color-coded `机遇和风险` block, but do a second typography pass to address the diagnosed readability issue: stronger content margins, a slightly roomier card minimum height, and cleaner header/body spacing that remains easy to scan within the existing popup width. Preserve button-style selection, confirm gating, popup sizing, and the intentionally empty area above the confirm CTA.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "& 'D:/Godot/Godot_v4.6.1-stable_mono_win64/Godot_v4.6.1-stable_mono_win64_console.exe' --headless --path 'three-kingdoms-simulator' --script 'res://scripts/tests/phase21_monthly_hud_regression.gd'"</automated>
  </verify>
  <done>月初任务卡已正确显示来源机构与具体请求方，且边距/排版达到新的可读性契约。</done>
</task>

</tasks>

<verification>
先运行 `phase3_task_source_regression.gd` 验证 institution/requester 数据链路，再运行 `phase21_monthly_hud_regression.gd` 验证任务卡语义、边距与既有月初/月末门控没有回归。</verification>

<success_criteria>
- 玩家打开月初任务选择时，能一眼看出“任务来自哪个权力机构、由谁具体下达”。
- 任务卡不再把来源机构、请求方、来源摘要混为一谈。
- 卡片排版在当前 popup 宽度下比 03-08 更稳定易读，同时未破坏确认按钮门控与既有月循环回归。
</success_criteria>

<output>
After completion, create `.planning/phases/03-仕途、势力与可解释政治/03-10-SUMMARY.md`
</output>
