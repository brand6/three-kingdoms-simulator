---
phase: 03.1-phase1-baseline-verification-and-time-contract
plan: "01"
subsystem: ui
tags: [godot, gdscript, hud, regression, time-contract]
requires:
  - phase: 01-190
    provides: [phase-1 top-bar shell, TimeManager formatter, cold-start hud binding]
  - phase: 02.1-office-and-task-deployment
    provides: [xun_yu default boot patch, month-start task gate]
provides:
  - Shared TimeManager-driven HUD time label contract restored
  - Static regression gate forbidding local HUD time formatting
  - Cold-start and xun-loop regressions aligned to current boot baseline
affects: [phase-1-verification, phase-2-regressions, audit-closure]
tech-stack:
  added: [static contract regression script]
  patterns: [single-source time label, dual-gate regression coverage]
key-files:
  created:
    - three-kingdoms-simulator/scripts/tests/phase1_time_contract_static_check.gd
  modified:
    - three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd
    - three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd
    - three-kingdoms-simulator/scripts/ui/MainHUD.gd
    - three-kingdoms-simulator/scenes/main/MainScene.tscn
key-decisions:
  - "Preserved the downstream Xun Yu default boot baseline instead of forcing Phase 1 back to Cao Cao, because later phase regressions already depend on the scenario patch."
  - "MainHUD now consumes TimeManager.get_current_label() as the only visible time-label source and falls back to an em dash when no autoload/session is available."
  - "Phase 03.1 protects CORE-02 with a dual gate: one static source scan and one runtime regression path."
patterns-established:
  - "HUD time fields must stay value-only and must not prepend 时间： or rebuild year/month/xun locally."
  - "Cold-start regressions may rebaseline around current runtime boot contracts while still enforcing inherited shared-time rules."
requirements-completed: [CORE-01, CORE-02]
duration: 19 min
completed: 2026-04-09
---

# Phase 03.1 Plan 01: 时间共享契约与双层回归 gate Summary

**MainHUD 重新接回 TimeManager 共享时间标签，并以静态 gate + 冷启动/跨旬回归锁住当前默认启动链路。**

## Performance

- **Duration:** 19 min
- **Started:** 2026-04-09T13:49:00+08:00
- **Completed:** 2026-04-09T14:08:32+08:00
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Added a dedicated static contract regression that fails whenever `MainHUD.gd` stops using `TimeManager.get_current_label()` or reintroduces local `时间：...` formatting.
- Restored the HUD and scene shell to a value-only time field with `—` placeholders outside success state.
- Rebaselined the cold-start and xun-loop regressions to the current `xun_yu` boot patch while preserving the shared time-label contract across runtime states.

## Task Commits

Each task was committed atomically:

1. **Task 1: Red-lock the repaired time contract into runtime and static regressions** - `da9982c` (test)
2. **Task 2: Green the HUD and scene shell back to the shared TimeManager label contract** - `d306a88` (fix)

**Plan metadata:** pending

## Files Created/Modified
- `three-kingdoms-simulator/scripts/tests/phase1_time_contract_static_check.gd` - Scans `MainHUD.gd` for required delegation and forbidden local time-format strings.
- `three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd` - Verifies the live top bar against the current Xun Yu default boot shell and shared time string.
- `three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd` - Forces month-task unlock first, then verifies xun advancement keeps HUD time text equal to `TimeManager.get_current_label()`.
- `three-kingdoms-simulator/scripts/ui/MainHUD.gd` - Delegates visible time text to `TimeManager.get_current_label()` and uses `—` during empty/error states.
- `three-kingdoms-simulator/scenes/main/MainScene.tscn` - Seeds `TimeLabel` with `—` instead of a local prefixed template string.

## Decisions Made
- Kept the later-phase `xunyu_default_start_patch` boot path because reverting to Cao Cao would create wider cross-phase breakage unrelated to the audit goal.
- Treated the static source scan as the minimal acceptable contract gate for D-03 instead of building a heavier lint/AST tool.
- Kept the fix scoped to time rendering and regression stability; no unrelated HUD restructuring or setup-patch cleanup was pulled into this plan.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Rebaselined regressions to the current default protagonist patch**
- **Found during:** Task 2 (Green the HUD and scene shell back to the shared TimeManager label contract)
- **Issue:** The repository no longer cold-starts as Cao Cao; Phase 2.1 introduced `xunyu_default_start_patch`, so the new regression expectations from planning no longer matched runtime reality.
- **Fix:** Preserved the current Xun Yu boot baseline in `phase1_topbar_time_regression.gd` and unlocked the month-start task gate in `phase2_xun_loop_regression.gd` before asserting xun-loop behavior.
- **Files modified:** `three-kingdoms-simulator/scripts/tests/phase1_topbar_time_regression.gd`, `three-kingdoms-simulator/scripts/tests/phase2_xun_loop_regression.gd`
- **Verification:** The full Wave 1 suite passed headlessly after rebaselining with the shared-time contract still enforced.
- **Committed in:** `d306a88` (part of task commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** No scope creep. The deviation aligned the plan with the repository's actual downstream boot contract while preserving the intended audit closure on CORE-02.

## Issues Encountered
- The initial Wave 1 green run exposed a real cross-phase contract drift: Phase 03.1 planning assumed the old Cao Cao baseline, while the executable project now boots through the Xun Yu setup patch.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 1 formal verification can now cite both a static gate and runtime regressions for the shared time contract.
- Downstream regressions now reflect the current default boot flow, reducing audit noise for later verification passes.

---
*Phase: 03.1-phase1-baseline-verification-and-time-contract*
*Completed: 2026-04-09*
