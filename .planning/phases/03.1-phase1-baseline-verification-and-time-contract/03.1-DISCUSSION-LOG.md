# Phase 03.1: Phase 1 基线验证与时间契约修复 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-09
**Phase:** 03.1-phase1-baseline-verification-and-time-contract
**Areas discussed:** 时间显示契约, Phase 1 复验范围, 回归测试口径, 技术债处理边界

---

## 时间显示契约

| Option | Description | Selected |
|--------|-------------|----------|
| TimeManager唯一真源 | TimeManager 输出唯一格式化时间字符串，MainHUD 只消费它；若 HUD 需要前缀，也由同一契约统一提供/包装，禁止 HUD 再自行拼年月旬格式。 | ✓ |
| 共享结构+HUD包装 | TimeManager 只提供基础 label 或数值，MainHUD 负责最终展示层包装。 | |
| HUD自主管显示 | 维持 HUD 自己拼展示字符串，TimeManager 只管数值推进。 | |

**User's choice:** TimeManager唯一真源
**Notes:** 用户接受推荐项，要求 Phase 03.1 明确恢复到单一时间真源契约，避免再次出现 Phase 1 与下游 HUD 漂移。

---

## Phase 1 复验范围

| Option | Description | Selected |
|--------|-------------|----------|
| 九项全量重核 | 对 CORE-01, CORE-02, DATA-01..04, CHAR-01, CHAR-02, CHAR-04 全部重新建立 requirement→evidence→verification 闭环。 | ✓ |
| 只补审计阻塞项 | 重点修 CORE-02 和缺失的 01-VERIFICATION.md，其余 requirement 主要引用旧证据。 | |
| 只补验证文档 | 尽量不碰代码，只创建/填写 01-VERIFICATION.md。 | |

**User's choice:** 九项全量重核
**Notes:** 用户明确要求 03.1 是完整 audit closure phase，不接受只写一份 verification 文档的最小补丁做法。

---

## 回归测试口径

| Option | Description | Selected |
|--------|-------------|----------|
| 双层gate | 保留 headless cold-start regression，同时新增静态/契约检查，锁定 MainHUD 必须消费 TimeManager 的唯一时间真源。 | ✓ |
| 只跑headless回归 | 以真实场景冷启动测试为主，不额外做静态契约检查。 | |
| 以静态检查为主 | 主要靠代码结构/字符串检查证明契约。 | |

**User's choice:** 双层gate
**Notes:** 用户同意推荐项，要求同时锁真实启动结果和代码契约，避免只有画面测试通过、但内部重新复制格式逻辑的情况。

---

## 技术债处理边界

| Option | Description | Selected |
|--------|-------------|----------|
| 仅影响证据时修 | 只有在它影响 Phase 1 九项复验、冷启动稳定性或时间契约证据时才纳入 03.1。 | ✓ |
| 顺手一起修 | 将默认主角重复配置等技术债一起纳入 03.1。 | |
| 完全不碰 | 严格只做 verification 与时间契约。 | |

**User's choice:** 仅影响证据时修
**Notes:** 用户要求 03.1 不扩 scope；技术债只有在影响本次 milestone audit closure 时才进入计划。

---

## the agent's Discretion

- `01-VERIFICATION.md` 的最终排版结构与证据表组织方式。
- 静态/契约检查的具体脚本实现形式。

## Deferred Ideas

- default protagonist duplicated cleanup — 若不影响当前证据闭环，则保留为后续 debt。
